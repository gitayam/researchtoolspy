# ResearchTools.net — Roadmap

**Last updated:** 2026-06-30 · **Current release:** `v0.22.0` (+ hardening patches through `v0.22.63`) · **Prod:** [researchtools.net](https://researchtools.net) (Cloudflare Pages + D1)

> **2026-06-26 — fresh COP fix batch injected (`COP-1`…`COP-12`).** A `/team-investigate` pass on a user report (COT export "not working" + "many aspects not working") surfaced a verified backlog of loop-eligible bugs/stubs. Full evidence + AUTO/DECISION split: [`plans/2026-06-26-cop-investigation-findings.md`](plans/2026-06-26-cop-investigation-findings.md). Listed at the top of **Now** below. The prior `/roadmap-step` STOP (drained backlog) is now lifted.

This is the living roadmap — the single source of truth for "what's next." Detailed findings live in [`TECH_DEBT.md`](operations/TECH_DEBT.md), the AI-safety review in [`AI_REFUSAL_REVIEW.md`](operations/AI_REFUSAL_REVIEW.md), and dated design/implementation plans in [`plans/`](plans/). Status legend: ✅ done · 🔄 partial · ⬜ planned.

---

## Direction

The feature build-out phase is largely behind us. The platform already ships a deep analyst toolkit:

- **12+ structured analytic frameworks** — ACH, COG (with network viz + Excel/PDF/PPT export), COM-B / Behaviour Change Wheel, Deception/SATS, Starbursting, SWOT, PEST, PMESII-PT, DIME, DOTMLPF, Stakeholder, Causeway.
- **Content intelligence** — URL / PDF / social-media extraction with AI entity + claim extraction, deception analysis, and a content library.
- **COP workspace** — multi-source incident analysis (entities, RFIs, evidence, timeline, map markers, hypotheses, personas, scrapers).
- **Intelligence synthesis dashboard**, **investigations & packets**, **evidence/entity management**, **network analysis** with exports to Gephi / Neo4j / Maltego / i2 ANB, and **agentic research collection** (Phases 1–4 complete).

The last month of work has been a deliberate **production-hardening pivot** — AI safety (refusal handling, consent gating, de-biased scoring), observability (`event_logs` sink), cost control (rate limiting), data retention (D1 225 MB → ~18 MB), auth resilience, and content-extraction reliability. **That is the current direction and it should continue:** make what exists trustworthy, observable, bounded, and safe before adding net-new surface area.

**Themes driving prioritization (in order):**
1. **Correctness & safety** of AI output and access (refusals, consent, abuse/cost).
2. **Observability** — never fly blind in prod again.
3. **Bounded cost** — D1 size, AI spend, write amplification.
4. **Reliability** of the content-extraction pipeline (the most-used path).
5. **Debt paydown / schema hygiene** so the next feature wave starts clean.

New features are welcome but should ride on top of this hardened base, not around it.

---

## Recently shipped

### v0.22.63 — Schema sprawl cleanup: drop 25 dead tables (TD-06) (2026-06-30)
- ✅ **D1 trimmed from 161 → 136 tables.** Inventoried all tables by row count: 83 had 0 rows. After grepping `functions/` and `src/` for TypeScript references, **25 were confirmed dead** (0 rows + no code refs) and dropped via idempotent `migration 113`. Prod backed up first (`backups/20260630_131918_pre-td06.sql`); applied remote; verified `rate_limits`, `api_keys`, `ach_collaborators` gone and `cop_sessions`, `evidence_items`, `survey_drops` intact. Tables dropped: `ach_collaborators`, `api_keys`, `cop_submissions`, `entity_ratings`, `entity_votes`, `evidence_social_media`, `framework_analytics`, `framework_content_sources`, `framework_exports`, `framework_ratings`, `framework_templates`, `framework_views`, `framework_votes`, `guest_sessions`, `library_collection_items`, `library_collections`, `library_framework_tags`, `library_items`, `library_tags`, `rate_limits`, `research_analysis`, `research_tool_results`, `social_media_analytics`, `social_media_monitors`, `suggestion_analytics`. `td06-dead-tables.spec.ts` 50/50; prod-deployed (HTTP 200). (`8aa6fc9a1`) *Discovered: 58 additional zero-row tables have TS code references (incomplete features like `cop_playbooks`, `social_media_jobs`, `investigation_*`, `comments`) — candidates for separate cleanup or activation.*

### v0.22.62 — SocialMediaPage crash: null count field guard (2026-06-30)
- ✅ **Social Media page no longer crashes when the API returns null count fields.** `formatNumber(num: number)` called `num.toString()` — if the DB stored NULL for `followers_count`, `posts_count`, etc., the page threw `null is not an object (evaluating 's.toString')` and React Router surfaced the bare dev error screen. Fixed with a `num == null → '0'` guard and widened the type to `number | null | undefined`. No data change. Type-check + build clean; prod-deployed (HTTP 200). (`37e32f71e`)

### v0.22.61 — COP error-path toasts (#18) (2026-06-30)
- ✅ **7 previously-silent COP failure paths now surface user-facing toasts.** `CopWorkspacePage.tsx` and `CopPage.tsx` already had `useToast` from COP-1; 7 error branches (across both files) that only called `console.error` now also call `toast({variant: 'destructive', ...})` with short, actionable messages. Also surfaced two completely silent catches discovered during the sweep: `handleToggleLayer` and `handleSessionUpdate` in `CopPage` had no error signal at all — both now get `console.error` + toast. `handleShare` upgraded from a silent `.catch(console.error)` to a proper then/catch with success ("Link copied!") and failure feedback. All 14 toast calls are additive (existing `console.error` kept). `cop-error-toasts.spec.ts` source-guard 14 assertions / 36 runs across browsers; type-check + build clean; prod-deployed (HTTP 200). (`89d7bfd0a`)

### v0.22.60 — Server-side workspace ownership check on research forms list (#19) (2026-06-30)
- ✅ **`GET /api/research/forms/list` now verifies workspace ownership server-side.** The endpoint was accepting a client-supplied `workspaceId` query param and using it to scope the query without verifying the requesting user owns or has access to that workspace — allowing workspace-existence enumeration via empty-result timing (the existing `created_by = ?` clause already prevented actual data reads, so severity was low). Fix adds a `userOwnsWorkspace()` helper that checks both direct ownership (`cop_sessions.created_by`) and collaborator membership (`cop_collaborators`) before scoping the main query. Returns **403** (not 404) on mismatch to avoid leaking whether the workspace ID exists. `research/submissions/list.ts` was audited and confirmed safe (no `workspaceId` param). `research-forms-authz.spec.ts` source-guard 12/12 (asserts ownership check present alongside `workspaceId` reference + 403 path); type-check + build + wrangler functions build clean; prod-deployed (HTTP 200). (`f8231037d`)

### v0.22.59 — Log discarded Apify run-start error (#21) (2026-06-30)
- ✅ **Apify run-start failure details now reach `event_logs`.** `scrape.ts` `onRequestPost` was parsing a detailed `apifyError` string (including the actor-not-rented hint) then discarding it — the client got a generic message and nothing was logged. The variable is now passed to `logEvent(env, buildUpstreamFailureLog('cop/scrape/run-start', {status, error: apifyError}))` using the COP-7 pattern from `_upstream-failure-log.ts`. Two remaining top-level catch-block `console.error` calls (in `onRequestPost` and `onRequestGet`) were also upgraded to `logEvent` with distinct sources (`cop/scrape/run-start`, `cop/scrape/status-check`). HTTP response shape unchanged. `scrape-apify-error.spec.ts` source-guard asserts `apifyError` is referenced in a `logEvent` call and no bare `console.error` remains in error paths; 38 relevant smoke tests pass. (`90ac13adb`) *Discovered: 9 pre-existing lint errors in scrape.ts (no-explicit-any + unused opts param) — candidates for a follow-up cleanup.*

### v0.22.58 — Entity type field read-only in edit mode (#20) (2026-06-30)
- ✅ **Entity type can no longer be silently changed to a no-op value in edit mode.** The `actors` and `sources` PUT handlers ignore `type` in their `UPDATE SET` (by design — D1 CHECK constraints make type changes dangerous mid-investigation), so the edit form now makes the type field read-only when an `editId` is present. `EntityCreateForm.tsx`'s `renderSelect()` extended with `opts.disabled` + `opts.hint`; all five type selectors in `renderTypeFields()` pass `disabled: isEdit`; the selector renders with visual `opacity-50 cursor-not-allowed` and `aria-disabled="true"` + a "Type cannot be changed after creation." helper hint below. Source-guard smoke test `cop-entity-type-readonly.spec.ts` 18/18 (asserts `editId` referenced alongside type field; `disabled` conditionally applied; helper text guard); type-check + build clean; prod-deployed (HTTP 200). (`3858ae71b`)

### v0.22.57 — Journalist drop-spot mode — anonymous tip-line (E-11) (2026-06-30)
- ✅ **ResearchTools now has a journalist-grade anonymous drop spot.** D-E4 decided: **full anonymity — no IP hash, no user-agent, not even for rate-limiting.** A blocked tip is worse than occasional spam. Implementation: (1) **Migration 112** adds `intent TEXT NOT NULL DEFAULT 'survey' CHECK(intent IN ('survey', 'drop'))` + index to `survey_drops` (applied remote, verified). (2) **Separate `drop-submit.ts` endpoint** (`functions/api/surveys/public/[token]/drop-submit.ts`, 186 LOC) — completely distinct from `submit.ts` so the anonymity guarantee is clean + auditable; never reads `CF-Connecting-IP`, `X-Forwarded-For`, or `User-Agent`; Turnstile **fail-open** (a blocked tip is worse than spam; file uploads remain fail-closed); always returns `{ok: true/false}`, never 500. (3) **Builder Survey/Drop toggle** on `ResearchFormBuilderPage`; Drop mode returns `/drop/:token` URL. (4) **`PublicDropFormPage`** (`/drop/:slugOrToken`) — slug resolver + anonymous wrapper. (5) **`CopIntakeForm` extended** with `isDropMode` prop for anonymity-first UI framing. Privacy hard-rule encoded as a top-of-file comment in `drop-submit.ts` — the file itself is the compliance artifact. `e11-drop-spot.spec.ts` 31/31 (includes privacy source-guards: asserts `drop-submit.ts` does NOT match `headers\.get.*CF-Connecting-IP`, `X-Forwarded-For`, `User-Agent`; does not contain `ip_hash` or `submitter_ip_hash` in any INSERT block); type-check + wrangler functions build + full vite build clean; migration applied remotely (verified `intent` column on `survey_drops`); prod-deployed (HTTP 200). (`08c88862e`) *Remaining E-11 depth: D-E5 (SecureDrop-style return codename + replies), D-E6 (drop-mode dedup policy), D-E7 (drop retention/access) — all DECISION-gated.*

### v0.22.56 — Playbook log viewer — read-only log panel (COP-5a) (2026-06-30)
- ✅ **The COP "View Log" button now opens a real log viewer.** The log endpoint (`functions/api/cop/[id]/playbooks/[pbId]/log.ts`) and the `CopPlaybookLog` display component already existed — only the wiring was missing. Added a Dialog + state to `CopWorkspacePage.tsx`'s `ProgressLayout` so clicking "View Log" on any playbook opens the log in a modal showing each execution entry (rule name, trigger event, actions taken, status, duration, errors). New injectable `src/lib/cop-playbook-log-api.ts` (`fetchPlaybookLog`, full `PlaybookLogEntry` type) follows the `cop-persona-api.ts` pattern. `cop5a-playbook-log.spec.ts` 12/12 (API path, header forwarding, auth guard, `onViewLog` now wired in `CopWorkspacePage`, panel still references the prop); wrangler build + type-check + full build clean; prod-deployed (HTTP 200). (`7916483d2`) *Remaining COP-5 sub-units: COP-5b (promote-persona-to-actor) and COP-5c (playbook editor) need new UI flows and are not auto-doable.*

### v0.22.55 — Map tab for geolocated submissions (E-9b) (2026-06-30)
- ✅ **Submissions reviewer gains a Map tab** — analysts can now see all submissions that carry location data plotted on an interactive MapLibre GL map (reuses the existing `maplibre-gl` bundle — no new dep). A new pure helper `src/lib/submission-geo.ts` (`extractGeoPoint`) parses lat/lng from `form_data` in three forms: separate `latitude`/`longitude` or `lat`/`lng` fields, a comma-separated `location` string (`"37.5,-122.1"`), and a `location` object (`{lat, lng}`); strict WGS-84 range validation ([−90,90] / [−180,180]) — invalid/missing → null. The `SubmissionsMap.tsx` component clusters pins by proximity (native MapLibre clustering), shows a popup with submission label + source URL on click, and renders a graceful empty state guiding analysts to add geo fields to their forms. `EvidenceSubmissionsPage` gets the Map tab wired in alongside Review/Forms. No D1 migration (reads from existing `form_data` JSON). `e9b-submissions-map.spec.ts` 16/16 (lat/lng extraction, lat/lng short aliases, comma-separated location, invalid range → null, non-numeric → null, missing fields → null, `_enriched_*` strip note, tab guard, empty-state guard, map component guard); type-check + build clean; prod-deployed (HTTP 200). (`43f9a3564`)

### v0.22.54 — Email header analyzer tool (F-6) (2026-06-30)
- ✅ **New OSINT tool: email header analyzer at `/dashboard/tools/email-header-analyzer`.** Pure client-side parser — no backend, no API key, no external deps. Paste a raw email header block; the tool extracts: (1) **routing chain** — all `Received:` hops in order (from/by/IP/timestamp/per-hop delay) with RFC 5322-compliant multi-line unfolding; (2) **auth badges** — SPF, DKIM, DMARC from `Authentication-Results:` and `Received-SPF:` (color-coded: green=pass, red=fail/reject, yellow=softfail/neutral/none); (3) **key fields** table (From, To, Subject, Date, Message-ID, Reply-To). Pure `src/lib/email-header-parser.ts` (337 LOC, exported `parseEmailHeaders()` + full types); `EmailHeaderAnalyzerPage.tsx` (333 LOC); lazy route at `/dashboard/tools/email-header-analyzer`; nav card added to `ToolsPage`. `f6-email-header-analyzer.spec.ts` 10/10 (routing chain, auth result parsing, spf=fail, empty/whitespace-safe, folded-header single-hop, Received-SPF fallback, key-header extraction, route guard); type-check + build clean; prod-deployed (HTTP 200). (`5ac5e6165`)

### v0.22.53 — CoT token-auth for headless TAK/ATAK clients (COP-16) (2026-06-30)
- ✅ **TAK/ATAK devices can now poll the CoT feed without header auth.** `GET /api/cop/:id/cot?token=<shareToken>` is now a valid auth path: the handler validates the share token against `cop_shares` (`share_token` + `cop_session_id`; row existence = valid, no additional `is_active` column needed), and on a match skips the header check and streams the CoT XML. Invalid or missing tokens on a private session still return 401. The existing `X-User-Hash` header-auth path is fully preserved as the else-branch — no behaviour change for browser and authenticated API clients. No migration needed (`cop_shares` already exists). Source-guard smoke test `cop16-cot-token-auth.spec.ts` 5/5; wrangler functions build clean; prod-deployed (HTTP 200). (`f0efe7c8b`) *Closes the secondary gap noted in COP-1: the browser button was fixed then; the headless-client path is fixed now.*

### v0.22.52 — Citation fields stored on promoted evidence (E-7a / E-12-full) (2026-06-30)
- ✅ **Auto-citation stored on every promoted evidence row.** When a survey submission is promoted to `evidence_items` via `research/submissions/process.ts`, the promote handler now calls `extractEnrichedCitation(form_data)` (new export from `functions/api/research/_lib/systema-adapter.ts`) to fish out the URL-enrichment data written by E-14's `enrichResponseUrls()` — specifically the `_enriched_<fieldname>` keys whose values carry `{url, title, excerpt, summary, fetched_at}`. The best match (the `_enriched_*` entry whose URL matches the submission's `source_url`, or the first one if none match) is stored as `metadata.citation` on the canonical `evidence_items` row at promote time, ready for client-side formatting by the existing `src/utils/citation-formatters.ts`. No new migration (the `metadata` JSON column landed in D-E8-0's migration 110). No citation *formatter* runs server-side — only citation-ready fields are persisted, avoiding a cross-boundary import of the frontend-only citation util. `e7a-citation-on-promote.spec.ts` 8/8 (null/empty inputs, no `_enriched_*` → null, valid extraction, missing optional fields → null not undefined, multi-key URL-preference, empty-url skip, source-guard confirming `process.ts` wires both the helper and the `citation:` key); prod-deployed (HTTP 200). (`1576092c9`) *Next: E-7b — promote also calls `auto-extract-entities.ts` to extract entities→actors from the enriched content (E-16). Also: COP-16 (CoT token-auth for headless TAK/ATAK clients, ~30 LOC).*

### v0.22.51 — Canonical evidence, phase 5 (FINAL): drop the legacy evidence tables — D-E8 COMPLETE (D-E8-5) (2026-06-29)
- ✅ **`evidence_items` is now the sole evidence table.** With every reader/writer repointed (D-E8-0..4) and a comprehensive grep confirming zero live SQL against them, migration `111-drop-legacy-evidence-tables.sql` drops the two empty (0-row) legacy tables `evidence` and `research_evidence`. Prod D1 was exported to an off-repo backup (9 MB) first; both tables verified 0 rows pre-DROP; applied local → remote; post-DROP verified **both tables absent and `evidence_items` intact at 99 rows**. `DROP … IF EXISTS` is idempotent (the repo re-runs all migrations each deploy). Full `@smoke` suite green afterward (**586 passed, 2 skipped, 0 failed**); prod-deployed (HTTP 200). (`45a03b373`)
  - **🎯 D-E8 (canonical evidence table) — DONE end-to-end.** The de-facto-canonical `evidence_items` is now *the* evidence store: ACH content-intelligence (D-E8-1), all read-joins (D-E8-2), and the research-evidence flow (D-E8-3) write/read it; the dead singular `/api/evidence` is retired to 410 (D-E8-4); the orphan tables are gone (D-E8-5). This was the keystone that gates deeper cross-tool evidence reuse (E-12-full auto-citation-on-evidence, E-16 entities→actors). Net: one consistent store, several previously-silently-broken read paths now functional, ~270 LOC of dead CRUD removed, two phantom tables dropped.

### v0.22.50 — Canonical evidence, phase 4: retire the singular /api/evidence endpoint (D-E8-4) (2026-06-29)
- ✅ **The last reader/writer of the bare `evidence` table is gone.** `functions/api/evidence.ts` was a full CRUD handler (GET/POST/PUT/DELETE) against the legacy 0-row `evidence` table, self-documented as deprecated with **no callers** (confirmed: no `fetch('/api/evidence')` anywhere in `src/`). Replaced the 254-line handler with a small **410 Gone** tombstone that returns a JSON pointer + `Link: </api/evidence-items>; rel="successor-version"` and touches no database (preflight still handled for CORS). Prod-verified: `GET /api/evidence` → **HTTP 410** with the canonical-endpoint body. `de8-4-evidence-endpoint-retired.spec.ts` 4/4 (source-guard: no DB access / no bare-`evidence` SQL + returns 410 + points to canonical); functions-build clean. **Comprehensive post-merge sweep: zero live SQL against the bare `evidence` *or* `research_evidence` table remains anywhere in `functions/`+`src/`** — both empty tables are now safe to DROP (D-E8-5). (`03b6f84ec`)

### v0.22.49 — Canonical evidence, phase 3: research_evidence read/write → evidence_items (D-E8-3) (2026-06-29)
- ✅ **The research-evidence flow now reads/writes the canonical store.** The three `research_evidence` handlers — `research/evidence/add` (writer), `research/evidence/list` (reader), `research/submissions/process` (promote-writer) — were repointed onto `evidence_items` through a new pure mapping module `functions/api/research/_lib/research-evidence-mapping.ts`. First-class fields mirror to real columns (so research evidence is genuinely canonical/filterable/reusable cross-tool); research-specific originals (`chain_of_custody`, `entities`, `linked_evidence`, raw `credibility_score`/`verification_status`, `collected_at`, `source_url`, …) stash losslessly in the `metadata` JSON column (migration 110) for a faithful round-trip. Exported pure helpers: `verificationStatusToItemStatus`/`itemStatusToVerificationStatus`, `admiraltyToCredibilityScore` (inverse of D-E8-1's `credibilityScoreToAdmiralty`, demonstrably band-consistent), `buildEvidenceItemsInsert`, `itemRowToResearchEvidence`. **`ResearchWorkspacePage`'s contract preserved exactly** (verified sole consumer): `id` (stringified PK), `content`, `evidence_type`, `verification_status`, `credibility_score` (0–1 float), `collected_at`, `title`. Zero data migration (0 rows). `de8-3-research-evidence-mapping.spec.ts` 22/22 (status round-trip, Admiralty band-stability, full write→read round-trip, null/edge cases); functions-build clean; prod-deployed (HTTP 200). After this merge, **no live `FROM/INTO research_evidence` SQL remains** anywhere in `functions/` or `src/`. (`a0c5a883d`)

### v0.22.48 — Canonical evidence, phase 2: repoint read-only joins → evidence_items (D-E8-2) (2026-06-29)
- ✅ **Eight read paths now read the real evidence table.** Every read-only `SELECT … FROM evidence` (the empty, 0-row legacy singular table) was repointed to canonical `evidence_items` — `events/[id]`, `settings/data/export`, `deception/aggregate`, and the six `claims/*` joins (`[id]`, `search-evidence`, `get-evidence-links/[id]`, `export-markdown/[id]`, `share/[id]`, `link-evidence`). Because the two tables have materially different columns, each query's selected/filtered columns were reconciled and **aliased back to the original response key** (e.g. `evidence_type AS type`, `credibility AS credibility_score`, `description AS content`, `when_occurred AS date`, `eve_assessment AS sats_evaluation`), so every endpoint's JSON contract is byte-identical — just now populated from the canonical store instead of silently returning nothing. Ownership scoping kept equivalent (legacy `user_id` → `evidence_items.created_by`; never widened). Surfaced that several `claims/*` handlers had been SELECTing **phantom columns** (`content_preview`, `bias_rating`, `source_type`) that existed on *neither* table — doubly broken, now NULL-aliased. Zero data migration, zero writes. `de8-2-read-joins.spec.ts` 28/28 (source-guard: asserts no bare-`evidence` FROM/JOIN + the load-bearing aliases survive); functions-build clean; prod-deployed (HTTP 200). (`fd7aeda2f`)

### v0.22.47 — Canonical evidence, phase 0+1: schema columns + ACH content-intel writer fix (D-E8-0/1) (2026-06-29)
- ✅ **`evidence_items` is becoming the single canonical evidence store.** **D-E8-0:** additive migration `110-evidence-items-canonical-cols.sql` adds nullable `metadata` / `research_question_id` / `investigation_packet_id` (+ partial index) to `evidence_items` so the collapse can *preserve* the research-question link, not degrade it — prod backed up first, applied remote, 99 rows intact, all 3 cols present (`3f7dc93c3`). **D-E8-1:** `ach/from-content-intelligence.ts` `createEvidenceFromContent()` was `INSERT`ing into the empty, broken `evidence` table; repointed to `evidence_items` (captures the new INTEGER PK via `last_row_id`, stringified for the TEXT `ach_evidence_links.evidence_id`, mirroring `ach/evidence.ts`). New pure `credibilityScoreToAdmiralty()` maps the 0–1 float → Admiralty `1`–`6`; source/date/category/relevance fold into the `metadata` JSON col. Deleted the dead `evidence`-table fallback read in `ach/public/[token].ts`. `de8-evidence-mapping.spec.ts` 12/12; functions-build clean; prod-deployed (HTTP 200). (`4cdd2c2f8`)

### v0.22.46 — "Deep scrape" a submitted URL via Content Intelligence (E-15) (2026-06-28)
- ✅ **One-click full analysis of a submitted source** — intake auto-enriches URLs in quick mode; the reviewer can now run the **full** `analyze-url` (entities/claims/full text/archive) on any submission's `source_url` via a "Deep scrape" button that reuses the Content Intelligence page's existing `pending_url_analysis` localStorage handoff (set key + navigate → auto-analyzes on mount). Pure `canDeepScrape` guard (http(s)-only) + pinned handoff constants; frontend-only (no endpoint/page change). `deep-scrape.spec.ts` 8/8; prod-deployed (HTTP 200). (`bb02ca0b8`)

### v0.22.45 — Search box on the submissions Review tab (E-9a) (2026-06-28)
- ✅ **Submissions are searchable** — the reviewer's Review tab rendered all loaded submissions with only a server-side status filter; now there's a client-side free-text search box (over the loaded ≤500, on top of the status filter) matching across `source_url`, the description/`metadata.title`, and `submitter_name`, with a graceful empty-after-filter state. Logic in a pure, unit-tested `src/lib/submission-search.ts` (`matchesSubmissionSearch`/`filterSubmissions`; null-safe). Frontend-only (no endpoint change — server-side `q` is a noted follow-up if the 500 cap is ever hit). `submission-search.spec.ts` 16/16; prod-deployed (HTTP 200). (`a512cc907`)

### v0.22.44 — COP intake gets URL auto-enrichment parity (shared helper) (E-14) (2026-06-28)
- ✅ **Scraping integration across both intake paths** — the surveys submit auto-enriched URL fields (scrape + `analyze-url` quick-mode → `_enriched_<field>` on the response) but the COP public intake submit did not. Extracted that logic verbatim into a shared `functions/api/_shared/url-enrichment.ts` (`enrichResponseUrls()` + pure `urlFieldsFromSchema`/`enrichmentRecord` helpers) and wired **both** handlers to call it inside `context.waitUntil` — surveys behavior byte-for-byte preserved (the inline block, −56 LOC, now delegates), and COP intake forms now get the same title/excerpt/summary/entities enrichment. No IP/UA added. functions-build clean; pure helpers unit-tested (`url-enrichment.spec.ts`, 14/14); prod-deployed (HTTP 200). (`9d421817c`)

### v0.22.43 — "Cite this source" connects submissions to the citation generator (E-12 lite) (2026-06-28)
- ✅ **Evidence → citation, wired** — first slice of the citation integration: the research-submissions reviewer now shows a **"Cite this source"** button on any submission with a `source_url`. It deep-links to the existing citation generator (`/dashboard/tools/citations-generator?url=<encoded>&title=<encoded>`), which already pre-fills from the params and auto-scrapes the URL into a formatted citation (APA/MLA/Chicago/Harvard). Pure, unit-tested `buildCitationGeneratorUrl` helper (`src/lib/cite-source.ts`; encodes url, includes title when present, returns null for non-http(s)/empty → button hidden). Frontend-only, reuses the existing URL→citation pipeline — no backend, no evidence-shape change, no dead write. `cite-source.spec.ts` 12/12; prod-deployed (HTTP 200). *The deeper "auto-citation persisted on the promoted evidence" is gated on D-E8 (the citation util targets a different evidence table than `research_evidence`).* (`50e59c24a`)

### v0.22.42 — Retire legacy System B; research forms fully unified on System A (E-4b-3+4) (2026-06-28)
- ✅ **System B retired — E-4b complete.** With the reviewer (E-4b-1) and promote (E-4b-2) already on System A, removed the now-dead legacy code: deleted `SubmitEvidencePage.tsx` + `CreateSubmissionFormPage.tsx` (and its misleading `collect_submitter_info` toggle) + their endpoints `research/submit/[hashId].ts` and `research/forms/create.ts`, and stripped `CopIntakeFormBuilder`'s dead `preview` stub — **−1545 LOC**. The legacy public route `/submit/:hashId` now renders a graceful `SubmissionFormMovedNotice` (legacy hash_ids don't map to System-A share tokens, so no redirect). The dormant `submission_forms`/`form_submissions` D1 tables are left in place (test data; no destructive drop). Gates: functions-build + full `tsc -b && vite build` clean (no dangling imports), `system-b-retired.spec.ts` + the privacy guard 12/12; prod-verified (legacy endpoints gone, notice serves). **The entire research-intake feature — builder, submit, upload, reviewer, promote-to-evidence — now runs on one engine (System A), which unblocks E-7 (rich promote: citation + entities) and E-9 (unified search).** (`71ef2026c`)

### v0.22.41 — Promote-to-evidence runs on System A (E-4b-2) (2026-06-28)
- ✅ **End-to-end reviewer pipeline unified on System A** — `research/submissions/process.ts` (promote a reviewed submission → `research_evidence`) was still reading legacy System B; it now reads `survey_responses` (JOIN `survey_drops`, scoped to the form owner like the E-4b-1 list), maps title/source_url/content/content_type from `form_data` via a shared, unit-tested `buildEvidenceFromResponse` adapter (never touches `submitter_ip_hash`), inserts the evidence, and marks the response processed with `status='accepted'` + `triaged_by` + **`linked_evidence_id`** (reusing an existing column — no migration). The already-processed path now returns a UI-compatible `200 {alreadyProcessed, evidenceId}`. With E-4b-1, the full chain — new builder → System A submit → reviewer list → **promote to evidence** — runs on one engine. functions-build gate clean; `promote-systema.spec.ts` 14/14; prod-deployed (HTTP 200). Remaining E-4b: `/submit/:hashId` redirect (E-4b-3) + dead-code cleanup (E-4b-4); then E-7 enriches promote with citation (E-12) + entities→actors (E-16). (`9f16671af`)

### v0.22.40 — Reviewer reads the modern engine: new-form submissions are visible (E-4b-1) (2026-06-28)
- ✅ **Keystone connection made** — the research-forms reviewer (Forms + Review tabs) was still reading the legacy "System B" tables (`submission_forms`/`form_submissions`, now only abandoned test data), so submissions to the **new** builder's forms (System A `survey_drops`/`survey_responses`) were invisible. Repointed both `research/forms/list` and `research/submissions/list` to read System A through a pure, unit-tested adapter (`functions/api/research/_lib/systema-adapter.ts`) that emits the **exact JSON shape the existing UI consumes** — so `EvidenceSubmissionsPage` needed **no change**. Mapping: `form_name←title`, `is_active←status==='active'`, `submissionUrl←/survey/<share_token>`, submission `source_url`/`description`/`submitter_name`/`status` extracted from the `form_data` JSON (slugified-label keys, with fallbacks); UI status `completed`→System-A `accepted` translated backend-side. **Privacy preserved** (never exposes `submitter_ip_hash`). No data migration (legacy System-B data is test junk, abandoned). Prod-verified: `forms/list` returns the System-A surveys. `reviewer-systema-adapter.spec.ts` 16/16. First sub-unit of the **E-4b** unification keystone (unblocks E-7/E-9 + the citation/scraping integrations once promote is repointed in E-4b-2). (`fa810696f`)

### v0.22.39 — One-click Admiralty source-credibility fields in the builder (E-10) (2026-06-28)
- ✅ **Source-credibility scaffolding** — the form builder now has an **"Add credibility fields"** button that drops in the standard NATO/Admiralty rating pair a researcher uses to grade a source: **Source Reliability** (`A — Completely reliable` … `F — Cannot be judged`) and **Information Credibility** (`1 — Confirmed` … `6 — Cannot be judged`), as two `select` fields. Defined as a pure `CREDIBILITY_FIELDS` preset in the builder helper; the button appends a deep copy, **dedupes by label** (case-insensitive) and respects the 50-field cap. Builder-only — they're plain selects the public renderer already handles. Unit-tested (`research-form-builder.spec.ts`, 62/62: well-formed A–F/1–6 presets + valid payload). Prod-deployed (HTTP 200) (`4f367c59c`). *(Maintainer also greenlit E-4b — unify the reviewer/promotion side onto System A — now the next, keystone unit.)*

### v0.22.38 — 90-day retention sweep for uploaded files (E-6d) (2026-06-28)
- ✅ **Upload retention** — uploaded files in R2 are now pruned after 90 days, completing the E-6 backend. New secret-guarded Pages endpoint `functions/api/cron/cleanup-uploads.ts` (mirrors `cleanup-content.ts`'s `X-Cron-Secret` guard) paginates the `UPLOADS` bucket's `uploads/` prefix (1000/page, 50-page defensive cap with `capped` reporting) and deletes objects whose `.uploaded` date is older than 90 days (pure `isExpiredUpload` helper — strict `>` boundary, returns false on undateable input so it never deletes what it can't date); `GET` is a non-destructive dry-run count. Wired into the standalone `researchtoolspy-cron` worker (`UPLOADS_CLEANUP_URL` var + `runUploadCleanup` called from `scheduled()`, daily 04:00 UTC) — **both the Pages app and the cron worker were redeployed** (the cron worker is independent of the Pages deploy). Endpoint verified live (401 without the secret). `upload-retention.spec.ts` 18/18; functions-build + cron dry-run gates clean. **This completes the E-6 backend (upload → R2, PDF text extraction, EXIF strip, retention); only E-6e — wiring upload into the public form + a Turnstile widget — remains, gated on creating Turnstile keys.** (`9851102fa`)

### v0.22.37 — Strip EXIF/GPS from served image uploads (E-6c) (2026-06-28)
- ✅ **EXIF/GPS scrubbed on serve** — uploaded camera photos carry EXIF metadata including **GPS coordinates**; the upload-serve endpoint now strips it so a viewer can't extract where/when a photo was taken. New pure, zero-dep `functions/api/uploads/_exif.ts` `stripImageExif()` parses JPEG marker segments and removes every **APP1** segment (where EXIF+GPS and XMP live), preserving SOI / APP0-JFIF / quantization+Huffman tables / SOS + all entropy-coded scan data / EOI; any malformed or truncated input returns the original bytes unchanged (never corrupts an image). The serve fn buffers only `image/jpeg` (other types still stream `object.body` untouched) and resets `Content-Length`. Unit-tested with crafted JPEG byte arrays (`exif-strip.spec.ts`, 8/8: APP1 removed + structure preserved + shorter, no-APP1 unchanged, non-JPEG unchanged, garbage-defensive). Aligns with the E-6 privacy decision (EXIF strip on serve). Prod-deployed (HTTP 200). *PNG/WebP EXIF stripping is a noted follow-up (JPEG is the dominant GPS carrier).* (`1a7260f84`)

### v0.22.36 — Auto-extract text from uploaded PDFs for pre-fill (E-6b) (2026-06-28)
- ✅ **PDF auto-scan** — uploaded PDFs are now text-extracted in-Worker (no vendor) so the form can pre-fill from the document. Exported a buffer-based `extractPdfTextFromBuffer` (reusing the existing `unpdf` path in `pdf-extractor.ts`); the upload endpoint reads each file's `arrayBuffer()` exactly once and feeds it to both the R2 put and the extractor, then returns a truncated `extractedText` (≤5000 chars) per PDF file. Extraction is fully guarded — PDF-only + 8MB size cap (to dodge the Workers CPU limit) + `try/catch`, so an image-only/encrypted/oversized/failing PDF simply omits `extractedText` and the upload still succeeds (`ok:true`). Pure helpers `shouldExtractPdf`/`truncateText` unit-tested (`upload.spec.ts`, 62/62). The new **`wrangler pages functions build` gate** (added after E-6a's deploy slip) passed clean. Prod-deployed (HTTP 200). (`5fd5e1019`)

### v0.22.35 — Gated file-upload-to-R2 + serve endpoint (E-6a) (2026-06-28)
- ✅ **Upload plumbing (fail-closed, backend only)** — first unit of document upload (E-6). New `POST /api/surveys/public/:token/upload`: token-scoped (mirrors submit.ts resolution + access checks), **per-IP-hash rate-limited**, enforces **10MB/file, ≤5 files/submission, MIME allowlist (pdf, png, jpeg, webp, gif)**, stores to R2 (`UPLOADS` bucket, `customMetadata.surveyId`, **no IP/UA stored**), returns `{key,name,size,type}`. New `GET /api/uploads/[[key]]` serves objects (catch-all so slashed keys work; `uploads/` prefix guard; nosniff + private cache; EXIF strip is E-6c). **Turnstile gating is fail-closed** — `verifyTurnstile` returns false on missing token/secret, so with `TURNSTILE_SECRET` unset the endpoint rejects everything (403 `turnstile_required`), keeping the anonymous surface CLOSED until E-6e wires the form + the Turnstile keys are configured. Pure helpers (`validateUpload`/`sanitizeFilename`/`uploadObjectKey`/`verifyTurnstile`) unit-tested (`upload.spec.ts`, 44/44). **Prod-verified**: upload w/o token → 403; serve-miss → 404. Decisions resolved (caps+Turnstile, EXIF-on-serve, 90-day retention, unpdf+pre-fill). **Deploy-fix in same unit:** a `functions/`-dir import was one level too high — passed `tsc` (which only covers `src/`, roadmap #14) but failed wrangler's deploy-time bundle; corrected, and `wrangler pages functions build` is now part of the gate for functions/ changes. (`203ee1180`)

### v0.22.34 — Interactive URL auto-confirm in the public intake form (E-5b) (2026-06-28)
- ✅ **Submitters see what they're submitting** — when a submitter enters a URL into a `url` field on a public intake/drop form, an inline `<UrlConfirmCard>` now calls the E-5a `preview-url` endpoint (debounced ~600ms, abort-on-change/unmount) and shows the extracted **title / author / published date / summary**, an **"Archived copy"** link, and an amber **duplicate** banner if the link was already submitted to that form. Failures are quiet and non-blocking (the user can always still submit). Built as a localized, self-contained component (a 5-line render addition to the 1358-LOC `PublicIntakeForm`, no refactor) + a pure `isPreviewableUrl` gate (http(s)-only, so previews don't fire mid-typing). Prod-deployed (HTTP 200); `url-confirm.spec.ts` 12/12. Completes **E-5** (URL auto-confirm). (`ec41469aa`)

### v0.22.33 — Public URL-preview endpoint for intake forms (E-5a) (2026-06-28)
- ✅ **Token-scoped URL preview** — new `POST /api/surveys/public/:token/preview-url` lets an unauthenticated submitter (identified only by the form's opaque share token) get extracted metadata to confirm before submitting. Reuses `submit.ts`'s token resolution + access checks + **per-IP-hash rate limit**, applies the `isPrivateUrl` SSRF guard, and calls the auth-gated `analyze-url` internally (system hash, `mode:'quick'`, 20s timeout) — returning a minimal `{ok, title, author, published_date, summary, archive_url, duplicate}` confirm-card shape (full raw result + any PII stripped). Failures degrade to `{ok:false}` at HTTP 200, never 500. Dedup is best-effort vs `survey_responses.content_hash` (URL-only basis for now). **Privacy:** the IP hash is transient (rate-limit only), never stored on a row or returned. Pure helpers (`validatePreviewUrl`/`shapePreview`) unit-tested (`preview-url.spec.ts`, 22/22). **Prod-verified end-to-end**: valid token → real title + AI summary; bad token → `{"error":"Form not found"}`. Foundation for E-5b (wiring it into the public form). (`4afd7b056`)

### v0.22.32 — Form builder: starter templates (E-4a) (2026-06-27)
- ✅ **Form templates** — the builder now offers a "Start from a template" picker with 5 researcher-oriented presets (OSINT Tip, Incident Report, Media/Image Submission, Claim to Verify, Source Document Intake) plus a Blank-form reset; selecting one loads a ready-to-edit field set (deep-copied) and prefills title/description when blank. All presets are defined in the pure helper (`FORM_TEMPLATES`) using only currently-supported field types (no `file` — that's E-6), with every `select` carrying options and the rating field bounded 1–5, so each template builds a valid payload. Prod-deployed (HTTP 200); `research-form-builder.spec.ts` 58/58 (every template asserted to build without throwing) (`72a73e01a`).

### v0.22.31 — Form builder: live preview-as-submitter (E-3c) (2026-06-27)
- ✅ **Live preview** — the research form builder now shows a live, read-only "as the submitter sees it" preview that updates as you edit, driven by a pure non-throwing `toPreviewFields(state)` helper (best-effort while fields are half-finished — empty state → none, unlabelled fields skipped, incomplete `select` → empty options, never throws; carries label/required/help/options/min/max). Every control is disabled and nothing is persisted. Built self-contained (the public `PublicIntakeForm` renderer is entangled with token-fetch/submit side effects and `CopIntakeFormBuilder`'s "preview" toggle is a dead stub). Completes **E-3** (builder a/b/c). Prod-deployed (HTTP 200); `research-form-builder.spec.ts` 46/46 (`058294c8f`). *Noted for E-4: two parallel builders (COP-intake vs research) over the same schema = consolidation candidate.*

### v0.22.30 — Form builder: field reorder + min/max validation (E-3b) (2026-06-27)
- ✅ **Builder field controls** — added per-field **Up/Down reorder** (via a pure, unit-tested `moveField<T>` helper — swap toward neighbor, no-op at the ends, no input mutation; buttons disabled at the ends + aria-labeled) and **Min/Max inputs for `number`/`rating`** fields (new `RANGE_TYPES`; `BuilderField.minRaw/maxRaw` parsed into numeric `IntakeFormField.min/max`, blank bound omitted, `min > max` throws `FormBuilderValidationError`; ignored on non-range types). Prod-deployed (HTTP 200); `research-form-builder.spec.ts` 36/36 (`824362c34`).

### v0.22.29 — New research form builder on System A (E-3a) (2026-06-27)
- ✅ **A real form builder** — `/dashboard/research/forms/new` previously rendered the weak System-B `CreateSubmissionFormPage` (fixed 9 toggles). It now renders a new `ResearchFormBuilderPage` backed by the System-A dynamic-schema engine (`survey_drops`): create a form with a name/description and **add/remove typed fields** (the 19 `IntakeFormFieldType`s; `file` deferred to E-6 with a "coming" note), per-field required toggle, options (for select-type fields), and help text; choose access level (public / password / internal). On create it POSTs to `/api/surveys` (with `status:'active'` so the form is live, not a draft that 403s at its own public URL) and surfaces the public `/survey/:share_token` link with copy-to-clipboard. **Privacy:** no IP/user-agent capture toggle (the `ip_address` field type is a submitter-input field, not auto-capture). Logic extracted to a pure, unit-tested `src/lib/research-form-builder.ts` (`buildSurveyPayload` + validation, `research-form-builder.spec.ts` 24/24). End-to-end prod-verified: created a form via the builder's path → public URL returned **HTTP 200** (not 403). (`4fb61e332`)

### v0.22.28 — Research forms-list route no longer 404s (E-2) (2026-06-27)
- ✅ **Orphaned route fixed** — `/dashboard/research/forms` was never registered, so the four "back to forms" buttons in the form creator dead-ended on a 404, and the only list page (`SubmissionFormsPage.tsx`) was imported by no router AND carried the same no-auth-headers 401 bug fixed elsewhere in COP-14. Registered `research/forms` as a redirect to the working `/dashboard/research/submissions` (which already has a Forms tab) and **deleted the dead page (−288 LOC)** rather than resurrect redundant/broken code. Prod-verified (200); regression-guarded by `tests/e2e/smoke/research-forms-route.spec.ts` (4/4: redirect registered, dead page gone) (`025a41b6b`).

### v0.22.27 — Privacy: research submit no longer captures submitter IP / user-agent (E-1) (2026-06-27)
- ✅ **Submitter IP + user-agent capture removed** (maintainer privacy directive: a normal form creator must never capture/see a submitter's IP or user-agent). The public System-B submit handler (`research/submit/[hashId].ts`) auto-captured raw `CF-Connecting-IP`/`X-Forwarded-For` + `User-Agent` (gated on a `collect_submitter_info` toggle, with no rate-limiting use — purely creator-visible). Both are now bound `NULL`; the header reads are gone. The reviewer list endpoint + UI never selected/rendered them (confirmed — no exposure existed). Migration `109-scrub-submission-pii.sql` nulls any historical values (applied; prod had 0/4 rows with PII — defensive). Prod-verified (post-deploy: 0 rows with IP/UA, HTTP 200). Source-level regression guard `tests/e2e/smoke/submission-pii.spec.ts` (6/6) asserts the handler doesn't read those headers and the reviewer endpoint doesn't select them. First unit of capability **E** (research intake form upgrade); System A (the engine we're keeping) already hashes IP for rate-limit only and strips it from outputs (`2df4b8de7`).

### v0.22.26 — Quieter COP pollers: no more "TypeError: Load failed" console spam (COP-15) (2026-06-26)
- ✅ **Transient poller noise silenced** — the COP workspace stats poller fetched every 60s **without an AbortSignal**, so a poll torn down on navigation/unmount logged `Failed to fetch workspace stats: TypeError: Load failed`; the existing `name !== 'AbortError'` guard missed it because WebKit surfaces a dropped fetch as a `TypeError`, not a DOMException. Added a pure, unit-tested `src/lib/transient-fetch-error.ts` classifier (AbortError + WebKit "Load failed" / Chrome "Failed to fetch" / "NetworkError" TypeErrors) and used it in both the stats poller and the `CopPlaybookPanel` 30s poller catches; also threaded the `AbortController` signal into `refetchStats` so polled fetches actually abort on unmount. Genuine application errors (e.g. a thrown `Error` on `!res.ok`) still log. Unit-tested (`transient-fetch-error.spec.ts`, 6/6: abort + network-drop transient, genuine errors not swallowed) (`f78ed061c`).

### v0.22.25 — No more dead screenshot archive link on analyzed URLs (COP-9) (2026-06-26)
- ✅ **Dead link removed** — `generateArchiveUrls` (in `content-intelligence/analyze-url.ts`) returned a `screenshot: /api/content-intelligence/screenshot?url=…` entry alongside wayback/archive.is, but that endpoint was never implemented (`// TODO`), so every analyzed URL's `archive_urls` carried a broken link. Extracted the builder to a pure, zero-dep, unit-testable sibling `functions/api/content-intelligence/_archive-urls.ts` and dropped the `screenshot` key (a real screenshot service remains DECISION **D-C**). Regression-guarded by `tests/e2e/smoke/archive-urls.spec.ts` (6/6: only wayback+archive_is returned, never the screenshot endpoint, target URL embedded). Prod-deployed (HTTP 200) (`3075bc336`).

### v0.22.24 — Share button gives feedback + warns on private sessions (COP-8) (2026-06-26)
- ✅ **Share-button UX** — the header "Share" button copied the private `/dashboard/cop/:id` URL silently (no toast), and that URL 403s for any non-owner/non-collaborator, so it was a silent dead-end. `handleShare` now shows a success toast on copy (and a destructive toast on clipboard failure) and tailors the message to `session.is_public`: public → "Anyone with this link can view this COP"; private → "This session is private — recipients need access; use Invite collaborators / Share link to grant access." It does NOT auto-create a public share token (deliberate data-exposure stays an explicit user action via the existing share/collaborators flow). Messaging extracted to a pure, testable `src/lib/cop-share-message.ts`. Unit-tested (`cop-share-message.spec.ts`, 6/6) (`d4ffbf7e2`).

### v0.22.23 — Personas are editable + deletable (was create-only) (COP-6) (2026-06-26)
- ✅ **Persona edit + soft-delete** — `CopPersonaPanel` could create personas but never edit or delete them. Edit reuses the panel's inline add form in an edit mode (prefill → the personas endpoint's existing action-routed `UPDATE` path; no new handler needed). Delete is a **soft delete** that sets `status='deleted'` (the enum already had it; reversible) and is now filtered out of the GET list (`AND status != 'deleted'` — verified NULL-safe: both insert paths bind a non-NULL status, prod has 0/47 NULL). Calls centralized in a new framework-free injectable `src/lib/cop-persona-api.ts` (`updateCopPersona`/`deleteCopPersona`, headers passed in); delete is confirm-gated. Unit-tested (`cop-persona-api.spec.ts`, 12/12: path/method, header forwarding, soft-delete body, error surfacing). Prod-deployed (HTTP 200) (`10c559688`).

### v0.22.22 — REDSIGHT/Apify upstream failures are visible in event_logs (COP-7) (2026-06-26)
- ✅ **No more silent upstream failures** — the REDSIGHT incident fetch (`alerts.ts`) and the Apify dataset-items fetch (`scrape.ts`) each returned a bare `[]` on a non-OK response or a thrown error, so a real outage was indistinguishable from "no data" and invisible in prod (Pages Functions `console.error` isn't visible there). Threaded `env` into both fetch helpers (smallest change to reach the D1 binding) and now emit a low-volume `logEvent('warn', source:'cop/alerts'|'cop/scrape', {status|reason})` at all 4 failure sites (each helper's `!ok` branch + `catch`), via a pure zero-dep `functions/api/cop/[id]/_upstream-failure-log.ts` builder; each `logEvent` is guarded with `.catch(() => {})` so logging can never break the request. The `return []` contract is preserved (route response + frontend untouched) — failures are now visible via `GET /api/cron/event-logs`. Mirrors the v0.22.15 extraction-failure pattern. Unit-tested (`cop-upstream-failure-log.spec.ts`, 8/8: status capture, error-reason coercion + 500-char cap, LogEventInput shape) (`0c83d52cb`).

### v0.22.21 — COP entity Edit button works (was a no-op) (COP-3) (2026-06-26)
- ✅ **Entity editing wired** — the COP entity drawer's "Edit" button (`CopEntityDrawer.tsx`) was a literal no-op placeholder, so entities could be created/deleted but never edited. Reused the existing `EntityCreateForm` in a new **edit mode** (optional `editId` prop: when set the form PUTs via the new injectable `src/lib/cop-entity-update.ts` helper and the button reads "Save" instead of POSTing); `handleEdit` maps the entity into the form's prefill shape, opens it prefilled, and on save reuses the same cache-invalidate + refetch as create/delete. Editable fields = everything the create form already supports per type (actor/source/event/place/behavior name, description, + type-specific fields), matching each PUT handler's accepted body. Unit-tested (`cop-entity-update.spec.ts`, 20/20: per-type path mapping, `PUT` method, header forwarding, body, 400/401/non-JSON error surfacing). Prod-deployed (HTTP 200). Lint: both new files clean, `CopEntityDrawer` 11→10 errors (improved), `EntityCreateForm` 9→9 (no net-new) (`6f90c267e`).

### v0.22.20 — Research submissions page loads: auth headers on list reads (COP-14) (2026-06-26)
- ✅ **`/dashboard/research/submissions` un-401'd** — `EvidenceSubmissionsPage`'s two READ fetches (`loadForms`, `loadSubmissions`) called `fetch(url, {signal})` with **no auth headers**, while `/api/research/forms/list` + `/api/research/submissions/list` hard-401 via `getUserFromRequest` → the page showed "Authentication required" on every load (the page's own mutations already sent `getCopHeaders()` — pure omission). Centralized both reads in a new framework-free, injectable `src/lib/research-forms-api.ts` (`listResearchForms`/`listResearchSubmissions`, headers passed in) so the omission can't drift back; both call sites now forward `getCopHeaders()`. Behavior otherwise preserved (workspaceId resolution, status param, `data.forms`/`data.submissions` parsing, AbortError guard). Prod-verified (`forms/list` → 401 no-auth, 200 with `X-User-Hash`). Unit-tested (`research-forms-api.spec.ts`, 10/10: paths, header-forwarding, 401-throws). Surfaced by a real user browser-console report (`bf999c17a`).

### v0.22.19 — COP map renders again: CSP allows maplibre workers + CartoCDN (COP-13) (2026-06-26)
- ✅ **COP map un-broken in prod** — the CSP added in `d1866dae8` ("resolve 14 security findings") had **no `worker-src`** (so maplibre-gl's `blob:` render workers fell back to `default-src 'self'` → blocked) and `connect-src` **omitted the CartoCDN basemap host** actually used by `CopMap` (`src/.../CopMap.tsx:112`) plus the nominatim place-search host — so the dark-matter basemap, tiles, sprite, glyphs, render worker, and geocoder were all refused and the map was 100% non-functional. Added `worker-src 'self' blob:` and `https://*.cartocdn.com https://nominatim.openstreetmap.org` to `connect-src` in `public/_headers` (the single authoritative CSP, copied to `dist/_headers` at build) — no `unsafe-*`/`*` broadening. **Live-prod-verified** by curling the served `Content-Security-Policy` header (all three directives present). Regression-guarded by `tests/e2e/smoke/csp-map.spec.ts` (4/4: worker-src has `blob:`; connect-src has `*.cartocdn.com` + nominatim + `'self'`). Surfaced by a real user browser-console report (`66f7f2b7e`).

### v0.22.18 — Datasets feature reachable: `/api/dataset` → `/api/datasets` (COP-2) (2026-06-26)
- ✅ **Dataset feature un-404'd** — the frontend called the **singular** `/api/dataset`, but only the **plural** `functions/api/datasets.ts` exists (Pages Functions use exact file-based routing), so every Datasets call 404'd silently. Re-pointed **all 5** call sites (the audit found PUT at `DatasetPage.tsx:65` and DELETE at `:93` beyond the 3 originally named GET/POST) to `/api/datasets` after verifying it implements compatible GET (`{dataset:[...]}`) / POST / PUT / DELETE handlers + matching auth (`getCopHeaders` → `X-User-Hash`/Bearer). Centralized the path in a new framework-free, injectable `src/lib/datasets-api.ts` (`DATASETS_API_PATH` + `listDatasets`/`createDataset`) so it can't drift again. Unit-tested (`tests/e2e/smoke/datasets-api.spec.ts`, 10/10; full smoke suite 174 passed/0 failed this run). URL was the only behavior change. Prod-verified (HTTP 200, bundled assets) (`b5365b491`).

### v0.22.17 — COP CoT/ATAK export now authenticates (COP-1) (2026-06-26)
- ✅ **CoT export fixed** — the COP "Export CoT/ATAK" button launched the feed via `window.open('/api/cop/${id}/cot')`, a browser navigation that **cannot attach the `X-User-Hash` header** the endpoint requires → **401 for the owner, 403 for everyone else** (the endpoint + serializer + `cop_markers` schema were always correct; this was pure auth-transport). It now exports via an authenticated `fetch(..., { headers: getCopHeaders() })` + `Blob` download through a new injectable, framework-free helper `src/lib/cop-cot-export.ts` (`cotExportFilename` + `downloadCotExport`), with success/error toasts (`useToast`) wired into both `CopWorkspacePage.tsx` and `CopPage.tsx`. Helper unit-tested (`tests/e2e/smoke/cot-export.spec.ts`: filename derivation incl. the `cop-` double-prefix guard, success-download click+revoke, 401-rejects-so-UI-can-toast). Prod-verified (deploy HTTP 200, bundled assets) (`e213e8485`). *Secondary gap still open: a headless TAK/ATAK client polling this URL still can't send the header — needs a `?token=<shareToken>` path (tracked in the plan doc).*
- ✅ **Apify scraper rate-limited** — the COP scraper (`functions/api/cop/[id]/scrape.ts`, a POST that triggers Apify actors billed per run) was the one **unthrottled paid op**. Added a per-user KV limit (`scrape:<id>`, **10/60s**) in `_middleware.ts` via an exported `isApifyScraperPath()` matcher that catches only `/api/cop/<id>/scrape` (not `/tools/scrape-metadata`, `/ai/scrape-url`, `/web-scraper`). Unit-tested (`tests/e2e/smoke/scraper-rate-limit.spec.ts`) (`c9b421bf3`).
- ✅ **Now#1 (burst-accurate rate limiting) RESOLVED by maintainer decision** — low-traffic community service → KV limiters accepted as good enough; the "strongly consistent" bar is downgraded (no Durable Object / `[[ratelimit]]` binding). Revisit only if traffic grows.

### v0.22.15 — URL-extraction failures visible in event_logs (2026-06-20)
- ✅ **Extraction-failure observability** — `analyze-url.ts` logged extraction hard-failures only via `console.error` (invisible in Pages Functions prod). The 422 failure branch now also emits a low-volume `logEvent('warn', source:'content-intelligence/analyze-url', {url, reason})` to `event_logs`, so failure reasons are visible via `GET /api/cron/event-logs`. Pure context-builder in a zero-dep sibling (`_extraction-log.ts`), unit-tested (`tests/e2e/smoke/extraction-failure-log.spec.ts`) (`28c8515f8`).

### v0.22.14 — Consent-gate COG/CARVER vulnerability generation (2026-06-20)
- ✅ **Sensitive-use gate extended** — `functions/api/ai/cog-analysis.ts` (COG/CARVER vulnerability generation) now sits behind the established Tier-1 `requireConsent` gate (shared `sensitive_ai` consent type, placed after `requireAuth`, before the AI call), matching `ai/generate.ts`. Returns the standard 403 `consent_required` so the existing `fetchWithConsent` dialog handles it — no client change. Prod-verified (no-consent → 403). Unit-tested (`tests/e2e/smoke/cog-consent-gate.spec.ts`) (`0ca2b5f26`).

### v0.22.13 — Remove dead duplicate ACH scoring engine (2026-06-20)
- ✅ **One ACH scoring definition** — `ach-scoring.ts` kept a ~300-line parallel ranking engine (`analyzeHypotheses` et al.) that still ranked by **net sum** (the inversion bug fixed in v0.22.4) with **zero callers** — a foot-gun if ever wired in. Removed it (−261 LOC), keeping only the still-used scale constants (`LOGARITHMIC_SCORES` / `LINEAR_SCORES` / `ScoreOption`, consumed by `ACHMatrix`). Constants guarded by a new `@smoke` test (`2ec03af21`).

### v0.22.12 — ACH evidence-quality weighting uses real source ratings (2026-06-20)
- ✅ **Real evidence-quality weighting** — ACH's `calculateEvidenceQuality` weighted every piece of evidence with hardcoded constants (reliability 0.75, source 1.0, confidence 0.75) and fell back to parsing the **source name** as a credibility grade. It now uses the real `reliability` (A–F), `confidence_level` (high/medium/low), and `credibility` (1–6) values via the previously-dead `getReliabilityScore`/`getConfidenceScore`/`getCredibilityScore` helpers; the ACH GET now selects `reliability` + `confidence_level`. Completes the credibility residual of the v0.22.4 Heuer-ranking fix. Unit-tested (`tests/e2e/smoke/ach-evidence-quality.spec.ts`) (`f785cddd5`).

### v0.22.11 — Deception PDF category bars use the correct 0–5 scale (2026-06-20)
- ✅ **PDF bars no longer render at ⅓** — the Deception/SATS PDF export drew each category bar (and its `x / N` label) against `max: 15` (MOSES `10`) while `categoryScores` are **0–5 averages**, so bars filled ~⅓ (MOSES ~½) of true value; the on-screen view was already correct. All four now use a single exported `DECEPTION_CATEGORY_MAX = 5` (source of truth in `deception-scoring.ts`). Scale invariant unit-tested (`tests/e2e/smoke/deception-pdf-scale.spec.ts`) (`07c635c5e`).

### v0.22.10 — Deception confidence reflects coverage, not score magnitude (2026-06-20)
- ✅ **Confidence by coverage** — `calculateDeceptionLikelihood` confidence was the count of criteria scored **> 0**, so a thorough low-risk assessment (legitimate zeros) reported **VERY_LOW** confidence. It now counts criteria **assessed** (key present in the input — a genuine `0` counts) via a new exported `DECEPTION_CRITERIA_KEYS`; thresholds (14/10/7/4) unchanged. Unit-tested (`tests/e2e/smoke/deception-confidence.spec.ts`) (`5d986f223`).

### v0.22.9 — COG no longer crashes on unscored vulnerabilities (2026-06-20)
- ✅ **COG vulnerability-scoring robustness** — guarded every `vuln.scoring.*` read in the COG view, vulnerability matrix, and exports (`?? 0`), so custom-scored and AI-generated vulns (which legitimately have no `scoring` object) no longer throw and make the whole analysis unviewable; the matrix sort no longer NaN-corrupts. Fixed the AI vuln handler (`COGForm`) to emit a valid `scoring{}` object via a new testable `aiVulnScoring()` helper (impact→impact_on_cog, feasibility→attainability, follow-up→neutral 3, clamped 1-5) with `composite_score` from `calculateVulnerabilityCompositeScore`. Unit-tested (`tests/e2e/smoke/cog-vuln-scoring.spec.ts`) (`0517fac1f`).

### v0.22.8 — Agentic Research: callback verification (backward-compatible) (2026-06-20)
- 🔄 **Per-job callback token + status guard** — the collection callback was unauthenticated (any POST for a known `jobId` could write results / complete a job). Added a nullable `callback_secret` (migration 108) that `start.ts` generates, stores, and forwards to the OSINT agent; `callback.ts` verifies it (`evaluateCallbackAuth`: match → accept, mismatch → **403**) and now **409s late/duplicate callbacks** that target an already-terminal job. **Rollout:** callbacks with no token are still accepted (logged `warn` "unsigned callback accepted (rollout)") so the live agent doesn't break — flip to strict reject once the external agent echoes the token. Unit-tested (`tests/e2e/smoke/collection-callback-auth.spec.ts`) (`3e6b5b02c`).

### v0.22.7 — Agentic Research hardening: rate-limit + stuck-job timeout (2026-06-19)
- ✅ **`/api/collection/start` is rate-limited** — added to the per-user AI limiter (`AI_RATE_LIMITED_PATHS` in `_middleware.ts`) so spawning an OSINT-agent run counts against the 40/min cap (cost/abuse). Used the exact path, not the broad `/api/collection/` prefix (which would have throttled the IP-keyed callback).
- ✅ **Stuck collection jobs now time out** — a job left `running`/`pending` past 15 min (agent accepted but never called back) is lazily transitioned to a terminal `error` on status read via a **race-safe** UPDATE (WHERE re-checks status+age, so a late callback still wins), so the polling UI stops. Exported pure helper `isCollectionJobStale()` + `@smoke` test (`2e4e75ff6`).

### v0.22.6 — AI endpoints return a clean "declined" on model refusal (2026-06-19)
- ✅ **No more opaque 500 on content-policy refusal** — `swot-auto-populate` and `pmesii-pt/import-url` now check the gateway's `_refusal` marker and return the standard `REFUSAL_BODY` (`{declined:true}`, 200) instead of `JSON.parse`-ing refusal prose into an opaque 500, matching the established `ai/generate.ts` / `ai/cog-analysis.ts` idiom. Contract pinned by `tests/e2e/smoke/ai-refusal-contract.spec.ts` (`detectRefusal` flags refusals + ignores analytical content; `REFUSAL_BODY` shape) (`a19142904`).

### v0.22.5 — Explicit public ACH API contract (2026-06-19)
- ✅ **Public ACH endpoints return an allowlist, not the full row** — the no-auth ACH detail (`public/[token].ts`) and list (`public/index.ts`) endpoints stopped spreading `SELECT *` rows into responses; a dependency-free `serializePublicAnalysis()` (`functions/api/ach/public/_public-fields.ts`) returns only display fields, so the internal `user_id` and any future column are excluded. Prod-verified (public list response carries no `user_id`); unit-tested (`tests/e2e/smoke/ach-public-fields.spec.ts`). `clone.ts` already returned only the new clone's own ids — left as-is (`f08e7f4e3`).

### v0.22.4 — ACH ranks by Heuer disconfirmation (2026-06-19)
- ✅ **Flagship ACH correctness fix** — ACH now ranks hypotheses by **weighted inconsistency** (sum of contradicting scores only; least-disconfirmed = most likely), matching Heuer's method, instead of by **net support** (which rewarded confirmation and produced an inverted "Most Likely"). Fixed on all live paths — `ach-diagnosticity.ts` `calculateHypothesisLikelihoods` (consumed by all 4 ACH export components) and the `ACHMatrix` ranking badge + "Key Principle" text; net/raw sums kept as clearly-labeled secondary signals. Canon pinned by a regression test (`tests/e2e/smoke/ach-ranking.spec.ts`, net-sum-vs-disconfirmation disagreement scenario) (`27f9b502b`).

### v0.22.3 — One canonical audit trail (2026-06-19)
- ✅ **Consolidated to a single written audit/observability trail (`event_logs`)** — dropped the two dead "audit" tables (`auth_logs`, `settings_audit_log`; both **0 rows** in prod, migration 107) that implied coverage that didn't exist, and rerouted the one real security event (`hash_backup_generated` in `settings/hash/backup.ts`) to `event_logs` via `logEvent()` with a new `'audit'` level (the `/api/cron/event-logs` reader now surfaces it). The reroute also **stopped persisting the backup recovery code and the full user hash** (only an 8-char prefix is logged for correlation). Unit-tested via `tests/e2e/smoke/audit-log-sink.spec.ts` (`d10ea56c8`).

### v0.22.2 — Auth resilience: retryable 503 on D1 error (2026-06-19)
- ✅ **Retryable 503 instead of spurious 401** — `resolveHashUser` no longer swallows D1 errors to `null` (which callers mapped to **401**, telling authenticated users to re-auth on a mere DB hiccup under burst load). It now resolves a valid user id or throws `AuthDbError`; `requireAuth` maps that to a **retryable 503** (`Retry-After: 2`), and `functions/api/_middleware.ts` duck-types the same marker as a global chokepoint for the many endpoints that call `getUserFromRequest`/`getUserIdOrDefault` directly (those become 503, not an opaque 500). Genuine no/invalid-hash still → 401 (prod-verified). Unit-tested via `tests/e2e/smoke/auth-resilience.spec.ts` (`3a016c141`).

### v0.22.1 — Content-intel & claims reliability (2026-06-15)
- ✅ **Batched deception analysis** — claim deception scoring is now chunked on both the content-intel and claims live paths (shared engine extracted), fixing JSON-truncation failures on large claim sets (`574e2587d`, `b747388bb`).
- ✅ **Thin/paywalled extraction warnings** — URL extraction now warns up front when content is thin or paywalled, gated on word count to avoid false positives (`108e9698f`, `2b6eab787`).

### v0.22.0 — AI safety, observability & cost control (2026-06-13→15)
- ✅ **AI content-policy refusal handling** — `detectRefusal()` in the gateway; callers return a clean `{declined}` instead of opaque 5xx; defensive analyst framing on intel prompts.
- ✅ **De-biased claim credibility scoring** — removed hardcoded political-affiliation priors in `extract-claim-entities`; now stake/independence/proximity-based.
- ✅ **Single AI egress point** — all ~40 callers routed through `callOpenAIViaGateway`; only the gateway calls OpenAI directly (caching, fallback, org-header forwarding, refusal detection inherited everywhere).
- ✅ **Production observability** — `event_logs` D1 sink (migration 105) + `logEvent()`; backend errors, AI refusals, and **client crashes** (ErrorBoundary → `/api/client-error`) are visible via secret-guarded `GET /api/cron/event-logs`; pruned >30 days by the daily cron.
- ✅ **Tier-1 sensitive-use consent gate** — `user_consents` table (migration 106) + `requireConsent()` 403 + `/api/user/consent` + frontend `fetchWithConsent` dialog. Gates `ai/generate`, `summarize-entity`, `relationships/infer-type`.
- ✅ **Dependency hygiene** — vulns **17 → 3** (within-range updates + `uuid` override for the exceljs transitive). Remaining 3 are build-tooling-only.
- 🔄 **AI rate limiting** — KV-backed limiters (middleware per-user + gateway global) replace an isolate-local in-memory Map. Protects against *sustained* abuse; **not burst-accurate** (KV eventual consistency) — see Now/#1.
- ✅ **Auth resilience** — `getUserFromRequest` hardened against guest-hash collisions (8→32-char prefix) and one transient-D1 retry. **Residual (resolved 2026-06-19):** the transient 401 under burst D1 throttling is now a retryable 503 — see v0.22.2 above.

### v0.21.0 — Content-analysis retention (2026-06-13)
- ✅ **Bounded D1 growth** — `expires_at` on insert, cascade-safe global cleanup endpoint, daily cron worker (`researchtoolspy-cron`), legacy backfill. **D1 225 MB → ~18 MB.**

---

## Now (highest priority)

**0E. Research intake form upgrade** `E-*` `Decisions resolved 2026-06-27` — capability **E** is now loop-eligible (D-E0=System A engine + retire System B; D-E1/2/3 as recommended; **privacy hard-rule: never capture/expose submitter IP or user-agent**). Full design + unit breakdown: [`plans/2026-06-27-research-intake-form-upgrade.md`](plans/2026-06-27-research-intake-form-upgrade.md). AUTO units in order:
   - ✅ **E-1** `P1` 🔒privacy — **Stop capturing submitter IP + user-agent** — DONE (v0.22.27, `2df4b8de7`). `research/submit/[hashId].ts` no longer reads CF-Connecting-IP/X-Forwarded-For/User-Agent; both columns bound NULL. Reviewer list/UI never exposed them (confirmed). Migration `109-scrub-submission-pii.sql` nulls historical rows (applied; prod had 0 of 4 with PII — defensive). Prod-verified (0 PII, HTTP 200); guarded by `submission-pii.spec.ts` (6/6). *Discovered: the `collect_submitter_info` create-toggle now only gated IP/UA → dead/misleading; remove/relabel in E-3/E-4.*
   - ✅ **E-2** `P2` — **Fix orphaned `/dashboard/research/forms` list route** — DONE (v0.22.28, `025a41b6b`). The route was unregistered (404); CreateSubmissionFormPage's 4 "back to forms" buttons dead-ended. Redirected `research/forms` → the working `/dashboard/research/submissions` (which has a Forms tab) and **deleted the dead, auth-broken `SubmissionFormsPage.tsx`** (−288 LOC). Prod-verified (200); guarded by `research-forms-route.spec.ts` (4/4).
   - ✅ **E-3a** `P1` — **New builder on System A (basic)** — DONE (v0.22.29, `4fb61e332`). `/dashboard/research/forms/new` now renders `ResearchFormBuilderPage` (456 LOC) + `src/lib/research-form-builder.ts` helper: name/description, add/remove typed fields (19 types; `file` deferred to E-6), required toggle, options (for select types), help text, access-level/password → POST `/api/surveys` (status `active`) → shows the public `/survey/:token` link + copy. **No IP/UA toggle** (privacy). End-to-end prod-verified (create → public URL HTTP 200, not 403). Unit-tested (`research-form-builder.spec.ts`, 24/24). *(Recovered from a prior crashed iteration that left it merged-but-unpushed with a destructive uncommitted revert — discarded the revert, verified, pushed+deployed.)*
   - ✅ **E-3b** `P2` — **Builder field controls** — DONE (v0.22.30, `824362c34`). Up/Down reorder per field (pure `moveField` helper, disabled at ends, aria-labeled) + Min/Max inputs for number/rating (parsed into `IntakeFormField.min/max`; `min>max` throws). Prod-deployed (200); unit-tested (`research-form-builder.spec.ts`, 36/36).
   - ✅ **E-3c** `P2` — **Live preview-as-submitter** — DONE (v0.22.31, `058294c8f`). Self-contained read-only preview in the builder, live from a non-throwing `toPreviewFields(state)` helper (best-effort while editing; never throws), every control disabled, persists nothing. (PublicIntakeForm wasn't reusable — token-fetch-entangled; `CopIntakeFormBuilder`'s "preview" is a dead stub.) Prod-deployed (200); `research-form-builder.spec.ts` 46/46. *Discovered for E-4: `CopIntakeFormBuilder` (COP-scoped, POSTs `/api/cop/:id/intake-forms`) and `ResearchFormBuilderPage` are parallel builders over the same `IntakeFormField` schema — consolidation candidate; and CopIntakeFormBuilder's dead `preview` toggle should be wired or removed.*
   - ✅ **E-4a** `P2` — **Form templates** — DONE (v0.22.32, `72a73e01a`). `FORM_TEMPLATES` (OSINT Tip, Incident Report, Media/Image Submission, Claim to Verify, Source Document Intake) + a "Start from a template" picker that loads a preset into the builder (Blank reset too). All templates use supported field types (no `file`/E-6), every select has options, rating has min/max. Prod-deployed (200); `research-form-builder.spec.ts` 58/58.
   - 🔑 **E-4b** `P1` **IN PROGRESS — unify on System A** (keystone; unblocks E-7/E-9/E-12/E-15/E-16). **No data migration needed** — the legacy System-B data is abandoned test junk (verified: 4 submissions, 2 are `example.com/test`). Sub-units:
     - ✅ **E-4b-1** — **reviewer lists → System A** — DONE (v0.22.40, `fa810696f`). `research/forms/list` + `research/submissions/list` now read `survey_drops`/`survey_responses` via a pure adapter (`research/_lib/systema-adapter.ts`) emitting the existing UI shape (no frontend change); PII-safe (no `submitter_ip_hash`); status `completed`→`accepted` translation. **Prod-verified: forms/list returns System A surveys.** New-form submissions are now visible to the reviewer. `reviewer-systema-adapter.spec.ts` 16/16.
     - ✅ **E-4b-2** — **promote → System A** — DONE (v0.22.41, `9f16671af`). `research/submissions/process.ts` now reads `survey_responses` (JOIN `survey_drops`, owner-scoped), maps fields via the shared `buildEvidenceFromResponse` adapter, creates `research_evidence`, and marks processed via `status='accepted'` + `triaged_by` + **`linked_evidence_id`** (no migration). Already-processed guard returns 200 `{alreadyProcessed,evidenceId}` (UI-compatible). PII-safe. functions-build clean; `promote-systema.spec.ts` 14/14. **End-to-end now runs on System A: new form → submission → reviewer → promote-to-evidence.** *(E-7/E-12/E-16 will enrich this promote with citation + entities→actors.)*
     - ✅ **E-4b-3+4** — **retire System B** — DONE (v0.22.42, `71ef2026c`). `/submit/:hashId` now renders a graceful `SubmissionFormMovedNotice` (legacy hash_ids don't map to System-A tokens). **Deleted −1545 LOC**: `SubmitEvidencePage.tsx`, `CreateSubmissionFormPage.tsx` (+ its `collect_submitter_info` toggle), `research/submit/[hashId].ts`, `research/forms/create.ts`; removed `CopIntakeFormBuilder`'s dead `preview` stub. (`submission_forms`/`form_submissions` D1 tables left in place — dormant test data, no drop.) functions-build + full build clean (no dangling imports); `system-b-retired.spec.ts` + PII guard 12/12; prod-verified (endpoints gone). **E-4b COMPLETE — the research-forms feature is fully unified on System A; E-7/E-9 + the citation/scraping integrations are now unblocked.**
   - ✅ **E-5a** `P1` — **Public URL-preview endpoint** — DONE (v0.22.33, `4afd7b056`). New `POST /api/surveys/public/:token/preview-url` (token-scoped, reuses submit.ts token-resolution + per-IP-hash rate-limit + `isPrivateUrl` SSRF guard) calls `analyze-url` internally (system hash, `mode:'quick'`) and returns a minimal confirm-card payload `{title, author, published_date, summary, archive_url, duplicate}`; graceful `{ok:false}` on failure; **IP hash transient, no PII stored/returned** (privacy). Dedup vs `survey_responses.content_hash` (best-effort — URL-only hash; tighten in E-5b). Prod-verified end-to-end (valid token → real title+summary; bad token → 404). `preview-url.spec.ts` 22/22.
   - ✅ **E-5b** `P1` — **URL auto-confirm in the public form** — DONE (v0.22.34, `ec41469aa`). New self-contained `<UrlConfirmCard>` + pure `isPreviewableUrl` helper rendered below each `url` field in `PublicIntakeForm` (5-line localized diff): debounced (~600ms) call to the E-5a `preview-url` endpoint, abortable, shows title/author/date/summary + "Archived copy" link + an amber **duplicate** banner; quiet non-blocking failure (never blocks submit). Prod-deployed (200); `url-confirm.spec.ts` 12/12. **E-5 complete.** *(Dedup still URL-only basis — tightening via full form_data is a later refinement.)*
   - **E-6 · Document upload + auto-scan** `P1` `decisions resolved 2026-06-28` — conservative caps + **Turnstile** gating; PDF text via `unpdf` + pre-fill; **EXIF strip on serve**; **90-day retention**; image OCR deferred. R2 bucket confirmed working (account-level put/get/delete). Sub-units:
     - ✅ **E-6a** — **Upload backend + serve (fail-closed)** — DONE (v0.22.35, `203ee1180`). `POST /api/surveys/public/:token/upload` (token-scoped, per-IP-hash rate-limit, 10MB/file, ≤5 files, MIME allowlist pdf+png/jpeg/webp/gif, R2 store, **no IP/UA**) + `GET /api/uploads/[[key]]` serve (catch-all for slashed keys, `uploads/` prefix guard). **Turnstile fail-closed** (`verifyTurnstile`): rejects all uploads (403 `turnstile_required`) until `TURNSTILE_SECRET` is set — so the surface stays CLOSED. Prod-verified (upload→403, serve-miss→404). `upload.spec.ts` 44/44. *Deploy-fix: a `functions/` import was one level off (`../../_shared`→`../_shared`) — passed tsc (src-only) but broke wrangler's bundle; now I run `wrangler pages functions build` for any functions/ change.*
     - ✅ **E-6b** — **unpdf text extraction** — DONE (v0.22.36, `5fd5e1019`). Exported a buffer-based `extractPdfTextFromBuffer` (in-Worker unpdf); upload.ts reuses the already-read arrayBuffer (read once) to extract PDF text after the R2 put, guarded (PDF-only + 8MB + try/catch so extraction failure never fails the upload), and returns a truncated `extractedText` (≤5000 chars) per file for pre-fill. Pure helpers `shouldExtractPdf`/`truncateText` tested (`upload.spec.ts` 62/62); functions-build gate clean. *Follow-up: full-text storage for reviewers (beyond the 5000-char pre-fill cap) is a later refinement.*
     - ✅ **E-6c** — **EXIF strip on serve** — DONE (v0.22.37, `1a7260f84`). Pure `functions/api/uploads/_exif.ts` `stripImageExif` walks JPEG markers and drops every **APP1** segment (EXIF incl. **GPS** + XMP), preserving SOI/APP0/tables/SOS/scan/EOI; malformed input → original bytes unchanged (never corrupts). Serve fn buffers only `image/jpeg`, strips, resets Content-Length; other types still stream. `exif-strip.spec.ts` 8/8; functions-build gate clean. *Follow-up: PNG `eXIf`/WebP EXIF (JPEG-only for now — the dominant camera-GPS carrier).*
     - ✅ **E-6d** — **90-day retention cron** — DONE (v0.22.38, `9851102fa`). New secret-guarded `functions/api/cron/cleanup-uploads.ts` (mirrors `cleanup-content`): paginates the R2 `uploads/` prefix (limit 1000, MAX_PAGES=50 cap), deletes objects older than 90 days (`isExpiredUpload`, strict `>` boundary, fails-safe on undateable input); GET = dry-run count. Wired into the `researchtoolspy-cron` worker (`UPLOADS_CLEANUP_URL` + `runUploadCleanup`, daily 04:00). Both deployed (Pages + the standalone cron worker redeployed). Endpoint live (401 w/o secret). `upload-retention.spec.ts` 18/18. **E-6 backend (a–d) complete.**
     - 🔒 **E-6e** — **Wire upload into the public form** (un-defer the `file` field type) + **Turnstile widget** — *opens the anonymous surface; PREREQUISITE: create a Cloudflare Turnstile widget (site key) + set the `TURNSTILE_SECRET` secret. This is a dashboard/ops action — the loop will pause for it.*
   - **E-7** `P2` **rich promote** — extract→store at promote (`functions/api/research/submissions/process.ts`). Split into:
     - ✅ **E-7a** — **Citation fields on promoted evidence** — DONE (v0.22.52, `1576092c9`). `extractEnrichedCitation(form_data)` fishes the best `_enriched_<field>` URL-enrichment record from the form data and stores it as `metadata.citation` on the `evidence_items` row at promote time. No server-side formatter — citation-ready fields only (url, title, excerpt, summary, fetched_at) for client-side rendering by the existing `citation-formatters.ts`. `e7a-citation-on-promote.spec.ts` 8/8; prod-deployed (HTTP 200).
     - ⬜ **E-7b / E-16** — **Promote enriched entities → actors** — NEXT AUTO: call `auto-extract-entities.ts` at promote time to extract actors from the enriched content and create `cop_actors` records in the active workspace. Confidence threshold + dedup policy decisions needed before implementing. See plan §3d.
   - **Evidence ↔ tools integration** `E-12..16` `Requested 2026-06-28` — connect the evidence-collection form to citation/scraping/etc (plan §3d). Mostly ride on E-4b's unified promote:
     - 🟡 **E-12** `P2` — **citation integration** — ✅ **lite shipped** (v0.22.43, `50e59c24a`): a **"Cite this source"** button on each submission (with a `source_url`) opens the existing citation generator pre-filled (`/dashboard/tools/citations-generator?url=…&title=…`), which auto-scrapes → citation. Pure `buildCitationGeneratorUrl` helper; `cite-source.spec.ts` 12/12; prod 200. **Remaining (full):** auto-citation *stored on the promoted evidence* — gated on **D-E8** (the citation util targets a different evidence shape than `research_evidence`).
     - 🔄 **E-13** `P2` **DEFERRED — larger refactor than a loop unit** (2026-06-28): `src/utils/citation-library.ts` is a **synchronous** localStorage API consumed by **8+ components** (`CitationLibrary`, `CitationPicker`, `CitationEditForm`, `CitationToEvidenceModal`, `EvidencePage`, `GenericFrameworkView`, `URLProcessingPage`, `BatchProcessingPage`). A D1 move converts all of them sync→async — a big, regression-prone refactor for a peripheral "shared/cross-session citations" gain. **Do as a deliberate, reviewed effort** (or a lighter hybrid: keep localStorage as the read source + background dual-write to `/api/citations` so consumers stay synchronous). Not an unattended loop unit.
     - ✅ **E-14** `P2` — **COP-intake enrichment parity** — DONE (v0.22.44, `9d421817c`). Extracted the surveys-submit URL-enrichment into a shared `functions/api/_shared/url-enrichment.ts` (`enrichResponseUrls` + pure `urlFieldsFromSchema`/`enrichmentRecord`); **both** the surveys submit (rewired, behavior preserved, −56 LOC inline) **and** the COP intake submit now run the same `analyze-url` quick-mode enrichment in `waitUntil`. functions-build clean; `url-enrichment.spec.ts` 14/14; prod 200.
     - ✅ **E-15** `P3` — **deep-scrape action** — DONE (v0.22.46, `bb02ca0b8`). "Deep scrape" button on a submission (with `source_url`) hands the URL to the Content Intelligence page via its existing `pending_url_analysis` localStorage handoff → full `analyze-url`. Pure `canDeepScrape` helper; `deep-scrape.spec.ts` 8/8; prod 200.
     - 🔄 **E-16** `P2` **(needs E-4b)** — **promote enriched entities → actors** via `auto-extract-entities.ts`. Folds into E-7.
     - 🔑 **D-E8** — **canonical evidence table** — ✅ **DECIDED + IN PROGRESS (2026-06-29): `evidence_items` canonical**. Scope mapped (plan §3e): **no data migration** (`evidence`/`research_evidence` = 0 rows); link-table FKs already target `evidence_items` (048/050/003/005); the `evidence`/`research_evidence` handlers are **already broken** (write/select non-existent cols) so repoint = bug fix. Decision: **preserve** the research-question link by adding columns (not degrade). Phases: **D-E8-0** ✅ additive migration (`metadata`,`research_question_id`,`investigation_packet_id` on evidence_items — `3f7dc93c3`) · **D-E8-1** ✅ fix the `evidence`-writer (`ach/from-content-intelligence`→items + `credibilityScoreToAdmiralty`; `4cdd2c2f8`) · **D-E8-2** ✅ repoint 8 read-joins `evidence`→items (aliased col-drift, contracts preserved; `fd7aeda2f`) · **D-E8-3** ✅ repoint `research_evidence`→items via shared mapping module (`ResearchWorkspacePage` contract preserved; `a0c5a883d`) · **D-E8-4** ✅ retire singular `/api/evidence` → 410 Gone (last bare-`evidence` owner; `03b6f84ec`) · **D-E8-5** ✅ DROP the empty `evidence` + `research_evidence` tables (migration 111; backed up; verified absent + 99 evidence_items intact; `45a03b373`). **🎯 D-E8 COMPLETE — `evidence_items` is the sole canonical evidence store.** Each backed-up + functions-build-gated + verified.
   - 🔄 **E-8** `P2` **GATED on E-6e** — EXIF/GPS→`geopoint` needs uploads actually wired into the form (E-6e), which needs your Turnstile keys.
   - ✅ **E-9** `P2` — **E-9a search/filters DONE** (v0.22.45, `a512cc907`) + **E-9b map DONE** (v0.22.55, `43f9a3564`): Map tab on the submissions reviewer with MapLibre GL clustering; pure `submission-geo.ts` extracts lat/lng/location from `form_data` with WGS-84 range validation; `SubmissionsMap.tsx` clusters pins, popup on click, graceful empty state. `e9b-submissions-map.spec.ts` 16/16. **E-9 complete.**
   - ✅ **E-10** `P3` — **Credibility scaffolding** — DONE (v0.22.39, `4f367c59c`). `CREDIBILITY_FIELDS` preset (NATO/Admiralty **Source Reliability A–F** + **Information Credibility 1–6**, two `select` fields) + an **"Add credibility fields"** button in the builder that appends them (dedup by label, respects MAX_FIELDS). Builder-only (plain selects the renderer already handles). Prod-deployed (200); `research-form-builder.spec.ts` 62/62.
   - ✅ **E-11** `P1` — **Journalist "drop spot" mode** — DONE (v0.22.57, `08c88862e`). Full anonymity (D-E4 decided): no IP hash, no user-agent. Separate `drop-submit.ts` endpoint (clean audit surface); builder Survey/Drop toggle; `/drop/:slugOrToken` public page; `PublicDropFormPage` with `isDropMode` framing; migration 112 (`intent` column on `survey_drops`, applied remote). Turnstile fail-open (blocked tip worse than spam). `e11-drop-spot.spec.ts` 31/31 incl. privacy source-guards; prod-deployed (HTTP 200). *Remaining depth: D-E5 (return codename + replies), D-E6 (dedup policy), D-E7 (drop retention) — DECISION-gated.*
   - 🛑 **DECISION (loop must NOT auto-do):** D-E2 retention-window value · D-E3 exact upload caps · **D-E5** SecureDrop-style return codename + replies (in/out v1) · **D-E6** drop-mode dedup policy · **D-E7** drop retention/access · **#19** `forms/list` workspace authz (fix while here) · OCR model if Workers AI lacks a fit.
   - *Done when:* each AUTO unit ships behind the verify gate with the privacy rule upheld and new tables carry a retention cron.

**0. COP workspace fix batch** `COP-*` `Discovered 2026-06-26` — verified loop-eligible bugs/stubs from a `/team-investigate` pass (the user-reported COT + dashboard issues). Full evidence + fixes + verify gates in [`plans/2026-06-26-cop-investigation-findings.md`](plans/2026-06-26-cop-investigation-findings.md). **Take these first** — small, reversible, each finishes in one turn. AUTO units in dependency/impact order:
   - ✅ **COP-1** `P1` — **Fix COT export** — DONE (v0.22.17, `e213e8485`). `window.open` (couldn't send `X-User-Hash` → 401/403) replaced with authenticated fetch + Blob download via a testable `src/lib/cop-cot-export.ts` helper; both pages toast on success/failure. Prod-verified (HTTP 200 bundle); helper unit-tested (`tests/e2e/smoke/cot-export.spec.ts`).
   - ✅ **COP-2** `P1` — **`/api/dataset` route mismatch** — DONE (v0.22.18, `b5365b491`). All **5** singular callers (GET/POST/PUT/DELETE — the audit found PUT+DELETE beyond the 3 named) re-pointed to plural `/api/datasets` (verified GET/POST/PUT/DELETE-compatible) via new testable `src/lib/datasets-api.ts` (`DATASETS_API_PATH` + injectable helpers). Prod-verified; unit-tested (`tests/e2e/smoke/datasets-api.spec.ts`, 10/10).
   - ✅ **COP-13** `P1` — **CSP breaks the COP map** — DONE (v0.22.19, `66f7f2b7e`). Added `worker-src 'self' blob:` + `https://*.cartocdn.com https://nominatim.openstreetmap.org` to `connect-src` in `public/_headers`. **Live-prod-verified** (curled the served CSP header — all three directives present). Regression-guarded (`tests/e2e/smoke/csp-map.spec.ts`, 4/4).
   - ✅ **COP-14** `P1` — **`/research/submissions` 401** — DONE (v0.22.20, `bf999c17a`). Both read fetches now go through new injectable `src/lib/research-forms-api.ts` (`listResearchForms`/`listResearchSubmissions`) forwarding `getCopHeaders()`. Prod-verified (`forms/list`: no-auth→401, with `X-User-Hash`→200). Unit-tested (`tests/e2e/smoke/research-forms-api.spec.ts`, 10/10). *Follow-up captured: `forms/list` trusts client `workspaceId` without filtering by user — authorization concern, see #19.*
   - ✅ **COP-3** `P1` — **Wire entity Edit button** — DONE (v0.22.21, `6f90c267e`). Reused `EntityCreateForm` in an edit mode (`editId` prop → PUT not POST) via new injectable `src/lib/cop-entity-update.ts`; `handleEdit` prefills + saves to `/api/<type>/[id]` for all 5 types, reusing the create/delete refresh. Prod-deployed (HTTP 200); unit-tested (`tests/e2e/smoke/cop-entity-update.spec.ts`, 20/20). *Follow-ups #20 (type-selector shown in edit mode but actors/sources PUT ignore `type`) + per-type fields not yet in the form.*
   - 🔄 **COP-4** `P1` **BLOCKED — needs a data-model decision** (2026-06-26): persisting the evidence↔persona link has **no existing path** — `cop_persona_links` is persona↔persona (both FKs → `cop_personas`; its `evidence_id` is just supporting-evidence for a persona-pair link), and there's no evidence↔persona table/endpoint. Implementing it requires choosing a model (new `cop_persona_evidence` join table vs. `linked_evidence_ids` JSON on `cop_personas` vs. FK) **and** a UI path to display the links — otherwise it's a dead write (cf. the `content_chunks` anti-pattern). **Decide:** the model + whether to surface linked evidence on the persona, then it's a normal multi-part build. Loop skipped it (won't guess a schema). Dialog today (`CopEvidencePersonaLinkDialog.tsx:106`) correctly creates/identifies the persona; only the association is unpersisted.
   - ✅ **COP-7** `P1` — **Surface error-swallowing** — DONE (v0.22.22, `0c83d52cb`). All 4 failure sites in `alerts.ts` (REDSIGHT) + `scrape.ts` (Apify) now `logEvent('warn', …)` to `event_logs` (via pure `_upstream-failure-log.ts` helper, guarded `.catch`); array contract preserved (no frontend ripple). Prod-deployed (HTTP 200); unit-tested (`cop-upstream-failure-log.spec.ts`, 8/8). *Follow-up #21: scrape run-start `apifyError` parsed then discarded.*
   - 🔄 **COP-5** `P2` — ✅ **COP-5a view-log DONE** (v0.22.56, `7916483d2`): "View Log" button on playbooks now opens a modal via `CopPlaybookLog` component; `fetchPlaybookLog` injectable helper. Remaining: **COP-5b** promote-persona-to-actor (POST `/api/actors` from persona fields + `linked_actor_id`; data op) and **COP-5c** playbook-editor (UX-ambiguous rule fields; needs a form design) — both need new UI flows, not auto-doable.
   - ✅ **COP-6** `P2` — **Persona edit/delete** — DONE (v0.22.23, `10c559688`). Edit reuses the panel's inline form (prefill → existing action-routed update path); delete is a **soft delete** (`status='deleted'`, reversible) now filtered from the GET list (NULL-safe: status is never NULL — both insert paths default `'active'`, prod confirms 0 NULL of 47). New injectable `src/lib/cop-persona-api.ts` + confirm-on-delete. Prod-deployed (HTTP 200); unit-tested (`cop-persona-api.spec.ts`, 12/12). *Edit form covers the create form's fields; `profile_url`/`status` not yet exposed (minor enhancement).*
   - ✅ **COP-8** `P2` — **Share-button UX on private sessions** — DONE (v0.22.24, `d4ffbf7e2`). `handleShare` now toasts on copy success/failure and tailors the message by `session.is_public` (public → "anyone with this link can view"; private → "private — recipients need access; use Invite collaborators / Share link"). Messaging in pure `src/lib/cop-share-message.ts`; does NOT auto-create a public token (kept an explicit user action). Prod-deployed (HTTP 200); unit-tested (`cop-share-message.spec.ts`, 6/6). *(Full share-dialog / token-link copy = part of the deferred share UX, not this.)*
   - ✅ **COP-9** `P2` — **Stop emitting the dead screenshot URL** — DONE (v0.22.25, `3075bc336`). `generateArchiveUrls` advertised `/api/content-intelligence/screenshot?url=…` to a never-implemented handler on every analyzed URL. Extracted to pure `functions/api/content-intelligence/_archive-urls.ts` and dropped the `screenshot` key (real provider = D-C). Prod-deployed (HTTP 200); regression-guarded (`archive-urls.spec.ts`, 6/6).
   - ✅ **COP-11** `P3` **CLOSED — not a bug (works via fallback)** (2026-06-26): on inspection the `BehaviorSearchDialog` is **functional today** — it fetches `/api/frameworks?type=behavior&limit=50` (verified live: returns `{sessions:[…]}`) and filters client-side. The TODO at `:48` is aspirational, not a defect. A dedicated `/api/behaviors/search` is a **marginal** optimization (server-side search only matters at >50 behavior sessions, otherwise the `limit` bump suffices) — deprioritized rather than manufacture a low-value endpoint + prod deploy. *Minor known limitation: client filter only sees the first 50; raise the limit if a workspace exceeds it.*
   - 🔄 **COP-12** `P2` **DEFERRED — larger than estimated; needs chunking** (2026-06-26): the actual count is **130 `console.error` across 58 COP handler files** (not the "~50" estimated). A 58-file mechanical sweep in one unattended prod-deploying iteration violates the loop's "one small, spot-verifiable unit" rule. Best done as a **deliberate, chunked** effort (e.g. ~10 highest-traffic handlers per PR: sessions/evidence/markers/hypotheses/tasks/rfis/timeline/claims/personas/stats), reviewed. The COP-7 pattern (`logEvent` + guarded `.catch`, env already in handler scope) is the template. *Ask to run it in chunks and the loop can take a defined slice per iteration.*
   - ✅ **COP-10** `P3` — **Doc fix** — DONE (docs-only, no deploy; `39c66520e`). Confirmed via `wrangler d1 list` that only `researchtoolspy-prod` (bound) + `researchtoolspy-dev` exist — there is **no** `researchtoolspy-db`. Fixed the active references: `CLAUDE.md` Database section + the 4 inconsistent examples in `docs/operations/CLOUDFLARE_LESSONS_LEARNED.md` (its lesson #8 already stated the correct name; its examples didn't). `scripts/cop-api.sh` had **no** DB-name reference (uses the HTTP API, not wrangler — the original report was wrong there). Dated `plans/2026-03-*` docs left as historical record.
   - ✅ **COP-15** `P3` — **COP poller `Load failed` noise** — DONE (v0.22.26, `f78ed061c`). New pure `src/lib/transient-fetch-error.ts` classifier (AbortError + WebKit/Chrome network-drop TypeErrors) used in both poller catches; threaded the AbortController signal into `refetchStats` so the 60s stats poll aborts on unmount. Genuine errors still log. Prod-deployed (HTTP 200); unit-tested (`transient-fetch-error.spec.ts`, 6/6).
   - ✅ **COP-16** `P2` — **CoT token-auth for headless TAK/ATAK clients** — DONE (v0.22.53, `f0efe7c8b`). `GET /api/cop/:id/cot?token=<shareToken>` validates against `cop_shares` (`share_token` + `cop_session_id`); valid → CoT XML served; invalid/missing → 401. Existing header-auth path unchanged. Source-guard `cop16-cot-token-auth.spec.ts` 5/5; prod-deployed (HTTP 200).
   - ✅ **D-A** empty-session triage **RESOLVED 2026-06-26** — owner confirms `cop-cdebda63-a33` is fresh/empty (expected, not data loss). 🛑 **DECISION (loop must NOT auto-do):** **D-B** data-import stubs · **D-C** real screenshot service · **D-D** full console→event_logs sweep (beyond COP-12). Details in the plan doc.
   - *Done when:* each AUTO unit has its fix + a regression/smoke test + prod verification per its "Verify" line.

1. ✅ **Burst-accurate rate limiting — RESOLVED by maintainer decision** (v0.22.16, 2026-06-20). Low-traffic community service → the **KV limiters are accepted as good enough**; the "strongly consistent" bar is **downgraded** (no Durable Object / `[[ratelimit]]` binding — KV's sub-minute burst staleness is an accepted, documented tradeoff). Limits in place: auth **5/min·IP**, register/pwd **10/min·IP**, **AI 40/min·user** (+ gateway **100/min·user** & **3000/hr global** cost backstop), and **Apify COP scraper 10/min·user** (`c9b421bf3` — the previously-unthrottled paid op). *Revisit a DO/binding only if traffic grows.*

2. **SAT correctness & safety fixes** `D0` — a 2026-06-19 audit found **bugs in shipped analytic techniques that change the answers analysts get** (theme #1). Full list + file:line + fixes in [`plans/2026-06-19-analytic-capability-expansion.md`](plans/2026-06-19-analytic-capability-expansion.md) → "Workstream D0." Headline items:
   - ✅ **ACH fully resolved:** ranking inversion FIXED (v0.22.4, Heuer weighted-inconsistency) · evidence-credibility weighting FIXED (v0.22.12, real reliability/confidence/credibility) · dead net-sum engine removed (v0.22.13, `2ec03af21` — one scoring definition).
   - **Deception AI** — ✅ **confidence-counts-magnitude FIXED** (v0.22.10) · ✅ **PDF bars ⅓-scale FIXED** (v0.22.11, `07c635c5e`). 🔄 **server-side move DEFERRED (2026-06-20): needs maintainer go-ahead on approach.** It's a **large multi-file migration** — `src/lib/ai-deception-analysis.ts` is ~517 LOC wrapping a browser OpenAI client (`VITE_OPENAI_API_KEY`, *verified unset → already returns the fabricated stub today*, so low regression risk), consumed by **5 components**; the move needs a new gateway endpoint + rewiring all consumers + removing the client key & fabricated fallback + refusal handling. Too big/security-sensitive to do autonomously on an ambiguous signal. **Decide:** greenlight the full server-side move (I'll run it as a focused multi-step effort) or keep it parked.
   - ✅ **COG crash on custom-scored vulns — FIXED** (v0.22.9, `0517fac1f`): all `vuln.scoring.*` reads guarded (`?? 0`) + AI handler emits a valid `scoring{}` object. *(Follow-up: the AI COG prompt could return all 3 Eikmeier dims directly instead of the lossy feasibility/impact→3-dim mapping.)*
   - 🔄 **COM-B canon — blocked (2026-06-20): needs a maintainer canon decision.** Investigation (2026-06-20): the `COM_B_INTERVENTION_MAP` in `src/utils/behaviour-change-wheel.ts` and `src/lib/bcw-canon.ts` are **byte-identical** (no divergence there) — but **both** contain the cells the audit flagged as Table-3.3 deviations (`physical_capability`→`environmental_restructuring`, `reflective_motivation`→`modelling`/`enablement`, `automatic_motivation`→`training`). The genuine divergence is in **`INTERVENTION_POLICY_MAP`** (Table 2.9): the UI map gives e.g. `education`→3 policies while `bcw-canon` gives 5 (incl. `regulation`/`legislation`), so the wheel UI and `/recommend` API recommend different **policies**. Unifying onto `bcw-canon` (the plan's instruction) is straightforward DRY but **changes the UI's policy recommendations** and still leaves the COM-B→intervention cells (which both share) unverified against the authoritative BCW table — and BCW Table 3.3/2.9 cells are **version-dependent in the literature**. **Decide:** confirm the authoritative table version + cells (so I unify onto a verified canon), or accept `bcw-canon` as-is and I'll DRY-unify both files onto it. Either changes analyst-facing recommendations, so it shouldn't be guessed.
   - ✅ **AI-endpoint refusal crashes — FIXED** (v0.22.6, `a19142904`): `swot-auto-populate` + `pmesii-pt/import-url` now check `_refusal` and return a clean `REFUSAL_BODY` (200) instead of an opaque 500. ✅ **ACH public `SELECT *` + spread — FIXED** (v0.22.5, `f08e7f4e3`): public ACH detail+list return an explicit field allowlist (`serializePublicAnalysis`), excluding `user_id`/future columns.
   - *Done when:* each headline bug has a regression test and the technique output matches canon (Heuer disconfirmation, SATS, Eikmeier, BCW Table 3.3).

3. **Agentic Research — review & hardening** `Agentic` — the AI source-collection feature ("Agentic Research", `/dashboard/tools/collection`) works end-to-end (job → external OSINT agent → callback → triage → approve → batch-analyze), but a 2026-06-19 code review found security / reliability / cost gaps. *Files:* `functions/api/collection/*` (`start.ts`, `callback.ts`, `[jobId]/{status,results,approve}.ts`), `src/pages/tools/CollectionPage.tsx`, `functions/api/_middleware.ts`, `schema/` (`collection_jobs` / `collection_results` / `collection_queries`).
   - 🔄 **[HIGH] Unauthenticated callback — rollout SHIPPED** (v0.22.8, `3e6b5b02c`): per-job `callback_secret` (migration 108) verified by `callback.ts` (mismatch → 403) + 409 status-guard on terminal jobs. **Still open (cross-system):** unsigned callbacks are accepted during rollout (logged `warn`); **flip to strict 403 once the external OSINT agent (`OSINT_AGENT_URL`) echoes the token** via the `X-Collection-Secret` header or `callbackSecret` body field — requires updating the agent service (separate repo).
   - ✅ **[HIGH] Stuck jobs never time out — FIXED** (v0.22.7, `2e4e75ff6`): jobs `running`/`pending` past 15 min are lazily transitioned to a terminal `error` on status read via a race-safe UPDATE.
   - ✅ **[MED] Collection endpoints not rate-limited — FIXED** (v0.22.7, `2e4e75ff6`): `/api/collection/start` added to the per-user AI limiter.
   - **[MED] No retention** — `collection_jobs` / `collection_results` / `collection_queries` have no cleanup (violates the project "new tables ship with a retention cron" convention). → `expires_at` + cron window (needs a retention-window decision).
   - **[LOW] Hygiene** — `workspace_id` accepted null (isolation); missing-score fallback `relevanceScore || 0.5`; hardcoded 70% select threshold + no score distribution; approve → batch-process cross-origin call forwards no auth.
   - *Done when:* each HIGH item has a fix + regression test, prod-verified; MED/LOW fixed or explicitly captured.

## Next

4. **Per-user limiting everywhere** `TD-05` — thread `metadata.user_id` through more gateway callers so per-user (not just global) limits apply. ✅ *Apify scrapers now throttled separately (v0.22.16, 10/min·user).* *Note (2026-06-20): the gateway limiter (`enforceRateLimit`) already keys per-user when `metadata.user_id` is supplied — the remaining work is a **broad sweep** threading `user_id` through the ~40 callers that omit it. Open-ended acceptance; best done as a defined sweep (or highest-traffic callers first) — not a single crisp change.*
5. 🔄 **Content-extraction quality** — ✅ **extraction failures now logged to `event_logs`** (v0.22.15, `28c8515f8` — `analyze-url.ts` 422 branch). **Remaining:** decide the fate of the **dead `content_chunks` full-text path** (`analyze-url.ts` writes it, nothing reads it — fix or remove).
6. ✅ **Extend consent gating — DONE** (v0.22.14, `0ca2b5f26`): the Tier-1 `requireConsent` (`sensitive_ai`) gate now also covers **COG/CARVER vulnerability generation** (`functions/api/ai/cog-analysis.ts`), prod-verified (no-consent → 403). *PimEyes/face-match has no server endpoint (frontend-only tool) — nothing to gate server-side.*
7. ✅ **Schema sprawl cleanup** `TD-06` — DONE (v0.22.63, `8aa6fc9a1`). 161 → 136 tables. 25 dead dropped (migration 113); 58 zero-row referenced tables documented for future decision. *Follow-up: the 58 referenced-empty tables represent incomplete features — worth a separate sweep to either activate or remove the dead endpoints.*

## Later

8. **vite 8 / rolldown migration** — clears the 3 remaining build-tooling vulns (esbuild/vite/wrangler). Real migration: `manualChunks` → `advancedChunks` + config types + **browser QA** (maplibre worker risk). Branch-only, when rolldown-vite matures.
9. **Index hygiene** `TD-04` — drop redundant `content_analysis` single-column indexes covered by composites (write amplification: ~14 index updates per insert). Cross-check D1 insights first.
10. **Logger consolidation** `TD-07` — merge the two `activity-logger` modules into one canonical module; migrate the 7 callers.
11. **R2 uploads verification** `TD-08` — confirm `env.UPLOADS` writes land (0 objects despite active write code in `feedback/submit.ts` + `twitter-image-proxy.ts` = likely silent failures; check the Dashboard R2 binding on the Pages project).
12. **JSON column trimming** `TD-02` — one-time GATED trim of legacy oversized `word_frequency` / `links_analysis` rows; consider moving `extracted_text` to R2 keyed by `content_hash`.
13. **CI-on-merge** — deploy `main` automatically so prod and `main` don't drift (TD-09 lagged ~1 month once).
14. **Lint / type-check gate hardening** `Discovered 2026-06-19` — `npm run lint` is red repo-wide (~6.5k pre-existing `no-explicit-any` / `no-unused-vars`, *including* the `tests/e2e/smoke/*.spec.ts` themselves — `eslint.config.js` ignores only `dist` and has no `tests/`/`functions/` overrides), so the lint gate is effectively **non-enforcing**. Separately, `npm run type-check` (`tsc -b`) covers only `src/` + `vite.config.ts` — it does **not** type-check `functions/` or `tests/`, so Pages-Function TS errors surface only at `wrangler` build/deploy time. Add scoped eslint overrides + a `functions/` typecheck step (feeds CI-on-merge #13).
15. **Public-endpoint field-allowlist sweep** `Discovered 2026-06-19` — the ACH public endpoints were returning the full DB row (fixed v0.22.5). Audit the other no-auth/public endpoints for the same `SELECT *` + spread pattern and apply explicit allowlists — notably the COP public share endpoints under `functions/api/cop/public/`. Prevents internal columns (and future columns) from being returned to unauthenticated clients.
16. **Refusal-check sweep across AI gateway callers** `Discovered 2026-06-19` — v0.22.6 fixed `swot-auto-populate` + `pmesii-pt/import-url`, but other `callOpenAIViaGateway` callers that `JSON.parse` the response may still lack a `data?._refusal` guard (→ opaque 500 on refusal). Grep for `callOpenAIViaGateway` + `JSON.parse` without a nearby `_refusal` check and apply the standard `REFUSAL_BODY` guard.
17. **`window.open('/api/…')` audit** `Discovered 2026-06-26 (COP-1)` — COP-1 fixed the CoT export, which failed because a browser navigation can't send `X-User-Hash`. Sweep the app for any other `window.open`/`<a href>` that points at an **auth-required** `/api/*` endpoint (export/download links especially) — each silently 401/403s the same way and needs the fetch-with-auth + Blob-download pattern (`src/lib/cop-cot-export.ts`).
18. ✅ **COP error-path toasts** `Discovered 2026-06-26 (COP-1)` — DONE (v0.22.61, `89d7bfd0a`). 7 silent error paths now surface destructive toasts; 2 completely silent catches (handleToggleLayer, handleSessionUpdate) also fixed.
21. ✅ **scrape run-start `apifyError` discarded** `Discovered 2026-06-26 (COP-7)` — DONE (v0.22.59, `90ac13adb`). `apifyError` now logged via `logEvent`/`buildUpstreamFailureLog`; all catch-block `console.error` calls in scrape.ts upgraded to same pattern. *Follow-up: 9 pre-existing lint errors in scrape.ts (no-explicit-any + unused opts).*
20. ✅ **Entity-edit `type` selector is a no-op** `Discovered 2026-06-26 (COP-3)` — DONE (v0.22.58, `3858ae71b`). Type field made read-only in edit mode (disabled + aria-disabled + helper hint). *Follow-up: per-type fields not exposed in the form (separate enhancement).*
19. ✅ **`research/forms/list` workspace authorization** `Discovered 2026-06-26 (COP-14)` — DONE (v0.22.60, `f8231037d`). `userOwnsWorkspace()` guard added (owner + collaborator check); returns 403 on mismatch. `submissions/list.ts` audited — not vulnerable. *Note: existing `created_by = ?` already blocked data reads; this closed a workspace-existence enumeration via timing.*
22. **Route-level ErrorBoundary / `errorElement` sweep** `Discovered 2026-06-30` — React Router's "Hey developer" message surfaced in prod when `SocialMediaPage` crashed (`null is not an object — formatNumber` fixed in `37e32f71e`). No route in `src/routes/index.tsx` has an `errorElement` or `errorBoundary` prop, so any uncaught render error reaches the bare React Router fallback instead of a branded error UI. Fix: (1) add a shared `<RouteErrorBoundary>` component that catches, logs to `/api/client-error`, and renders a "something went wrong / refresh or go back" card; (2) wire it as `errorElement` on every route in `src/routes/index.tsx` (or as the root element prop). AUTO — no decision needed.
23. **SocialMediaPage design polish** `Discovered 2026-06-30` — impeccable hook flagged 5 pre-existing issues when the null-guard fix was applied: `border-l-4` side-tab accent borders on 4 tool sections (L895/915/937/957 — hard-banned) and the Instagram `bg-gradient-to-r from-purple-500 to-pink-500` color in `platformColors` (L99 — AI-palette tell). Replace the border-left accents with an icon-led or background approach; pick a deliberate non-gradient Instagram color.

---

## Capability expansion (research-backed — rides on the hardened base)

Net-new analytic capability, scoped against the Heuer & Pherson SAT catalog, game-theory computation feasibility, and social-science stats tooling (research 2026-06-19). **Full coding context — data models, algorithms, file-level patterns, do/don't — is in [`plans/2026-06-19-analytic-capability-expansion.md`](plans/2026-06-19-analytic-capability-expansion.md).** These are the *feature* direction; they ride on top of the hardening track above, not around it.

The codebase has three extension tiers — Tier 1 config-only framework (`framework-configs.ts`, ~30 min), Tier 2 bespoke Form/View (COG/Deception pattern), Tier 3 standalone tool page + own table (`src/pages/tools/` — `CrossTablePage`, `EquilibriumAnalysisPage`, `HamiltonRulePage`). Each item below is tagged with its tier.

- **A · SAT coverage** `Tier 1 (+ small Tier 2)` — the biggest remaining gaps are scenarios/indicators, challenge analysis, and decision support, almost all **config-only** reusing the generic engine + AI auto-population. Ship batch: What-If, Premortem, Red Hat, Pros-Cons-Faults-and-Fixes, Devil's Advocacy, Force Field, Structured Self-Critique. Then **Indicators/Signposts (+Validator)** — highest-value, small item-schema extension. Then matrix-widget techniques (Multiple Scenarios 2×2, Cross-Impact N×N) on one shared `<AnalyticMatrix>`. *Note:* `kac` (Key Assumptions Check) already exists — **verify scope, don't rebuild**. Avoid: Delphi, Argument Mapping, full Morphological, standalone Bayesian (low value / high cost here).
- **B · Game Theory / Strategic Interaction** `Tier 3` — net-new and complements the existing strategic/evolutionary tools (Hamilton's Rule = kin-selection cooperation; Equilibrium Analysis = behavioral equilibria) — neither covers classical Nash / mixed strategies / **ESS / replicator dynamics**. No viable npm solver exists → **~200-LOC dependency-free TS solver** (`src/lib/game-theory/solver.ts`), all compute **client-side** (Workers CPU limit irrelevant). MVP: 2-player normal-form payoff editor + pure/mixed NE + best-response highlight + named templates (Prisoner's Dilemma, Chicken, Stag Hunt, Deterrence, Battle of the Sexes; cap 6×6). Stretch: AI payoff estimation → ESS/replicator simulator → iterated PD → game tree → sensitivity.
- **C · Quantitative & Reliability Analysis** `Tier 3` — **inter-rater reliability is the differentiator** (the platform already does multi-analyst claim/evidence coding; no OSINT tool offers κ / Krippendorff's α natively). In-browser TS stats (`simple-statistics` + mljs + `krippendorff`/`label-score` + jStat) — **do not bundle WebR/Pyodide in a Worker**; WebR-in-browser is a later feature-flagged escape hatch; keep R-export for power users. New D1 tables (`quantitative_datasets`, `analysis_runs`) **ship with a retention cron** (project convention). Phase 1 stats MVP (descriptives, correlation, χ², t-test/ANOVA, OLS); **Phase 2 Reliability Engine** (Cohen's/Fleiss' κ, Krippendorff's α, Cronbach's α — consider doing first); Phase 3 multivariate; Phase 4 WebR.
- **D · Improve / fix / enhance the EXISTING SATs** `mixed` — the audit behind Now/#2. Beyond the D0 correctness/safety bugs, two **high-leverage architecture fixes lift every framework at once**: (1) wire `promptQuestions` + section config into the AI prompt builders (today they're only a frontend accordion; AI endpoints hard-code duplicate maps; only 2 of 18 configs have any promptQuestions) — and (2) unify the **three incompatible evidence-linking systems** (`framework_evidence` vs ACH's `ach_evidence_links` vs claims' `claim_evidence_links` vs COG/SWOT inline JSON) so evidence vetted once is reusable across techniques. Plus: **five configured frameworks are unreachable** (carver, ooda, abcde, kac, 5whys — no route/page); add a generic **scored-item / enum-item type** to fix CARVER (unscored) and KAC (no importance/support); delete dead files (`COGForm.tsx.bak`, unimported `frameworks/AICOGAssistant.tsx`); register the authored-but-unwired i18n `frameworks` namespace. Enhancements: ACH sensitivity + tentative-rejection (Heuer steps 5–6, absent), consolidate ~1.5k LOC of bespoke exporters onto `report-generator.ts`, real COG centrality, DOTMLPF→DOTMLPF-P, PESTLE/DIMEFIL variants.

- **F · OSINT tooling gaps** `mixed (Tier 2/3)` `Researched 2026-06-30` — gap analysis vs. the leading open-source OSINT platforms (SpiderFoot, theHarvester, IntelOwl, Maigret/Sherlock, MISP/OpenCTI, Recon-ng, Harpoon, Bellingcat tools). ResearchTools already ships structured analytic frameworks, COP workspace, evidence/entity management, network-analysis exports, and Apify scraping. The gaps that are **feasible on a CF Pages + D1 stack** and would add the most OSINT analyst value:
  - **F-1 · Username/handle lookup** `Tier 3` `P1` — Sherlock/Maigret-style: given a handle, check 20–30 platforms (Twitter/X, Reddit, GitHub, Mastodon, Telegram, Instagram, TikTok, LinkedIn, Keybase, Pastebin, Snapchat, YouTube, etc.) in parallel via Worker `fetch`, check 200/404/301 with per-site patterns. Store hits as COP personas or COP actors. **Zero external API needed for the check itself** (site-specific URL patterns); implementation is ~200 LOC pure TS + a platform registry JSON. New tool page `/dashboard/tools/username-lookup`. *Done when:* a handle lookup returns platform hits in ≤10s; results can be added as personas to the active COP session.
  - **F-2 · IOC enrichment (URL/IP/hash reputation)** `Tier 2` `P2` — Inline VirusTotal + AlienVault OTX lookups for URLs, IPs, or file hashes found in evidence/entities. Show a reputation badge (clean/suspicious/malicious) + expandable detail. Proxy via a new `functions/api/tools/ioc-enrich.ts` (user supplies their own API key stored as a per-user secret, or the platform key). Mirrors IntelOwl/OpenCTI's enrichment pattern. *Decision needed: API key strategy (user-supplied vs. platform-held).*
  - **F-3 · IP/domain OSINT** `Tier 3` `P2` — Given an IP or domain (from entity or evidence), pull: Shodan host info, ASN/BGP, WHOIS, passive DNS (SecurityTrails/CIRCL). New panel in entity drawer. Shodan API is free-tier friendly (`api.shodan.io/shodan/host/{ip}`). SpiderFoot's core use case — the most-requested OSINT primitive missing from ResearchTools. *Decision: Shodan/SecurityTrails key strategy.*
  - **F-4 · Breach / credential exposure check** `Tier 2` `P3` — HaveIBeenPwned v3 API: given an email from an actor/persona, check if it appears in known breaches. Free for 1 req/1.5s; Pwned Passwords endpoint is free (k-anonymity SHA1 prefix). Inline badge in actor/persona form. Zero schema change needed. **~50 LOC.**
  - **F-5 · Screenshot service** `Tier 2` `P2` — Replace the dead `screenshot` key in `_archive-urls.ts` (D-C). Cloudflare Browser Rendering API (`/browser/screenshot`) is available on Workers Paid; alternatively proxy Screenshotone/Microlink (both have free tiers). Would complete the archive_urls object with a real screenshot. *Decision: CF Browser Rendering vs. external provider.*
  - ✅ **F-6 · Email header analyzer** `Tier 2` `P3` — DONE (v0.22.54, `5ac5e6165`). Pure TS `src/lib/email-header-parser.ts` (337 LOC, `parseEmailHeaders()`, full types) + `EmailHeaderAnalyzerPage.tsx` at `/dashboard/tools/email-header-analyzer`. Parses `Received:` hop chain (from/by/IP/timestamp/per-hop delay, RFC 5322 multi-line unfolding), SPF/DKIM/DMARC from `Authentication-Results:` + `Received-SPF:` fallback, key fields table. Zero external API. Nav card added to ToolsPage. `f6-email-header-analyzer.spec.ts` 10/10; type-check + build clean.
  - **F-7 · STIX 2.x import** `Tier 3` `P3` — Accept a STIX 2.1 bundle JSON (drag-drop or URL fetch), map indicators/threat-actors/observables → COP entities/evidence/relationships. OpenCTI, MISP, and most national CTI feeds export STIX. Complements the existing STIX export. New endpoint `POST /api/cop/:id/import-stix`.
  - **F-8 · In-app network graph** `Tier 3` `P2` — Today RT exports to Gephi/Neo4j/Maltego but has no in-browser graph. A Sigma.js or vis-network force-directed graph using the same entity+relationship data the export uses would give analysts a quick link-chart without leaving the page. Maltego's core draw; Palantir's core draw. New COP panel `CopNetworkGraphPanel`. *Feasibility: Sigma.js is ~70 kB gzipped; vis-network ~220 kB — both acceptable.*
  - **F-9 · RSS/news feed monitor** `Tier 3` `P3` — Add RSS/Atom feed URLs to a COP session; a cron job (or polling endpoint) fetches new items, filters by keyword/entity match, and inserts them as evidence. Harpoon and TheHarvester's news-monitoring modules. New D1 table `cop_rss_feeds` with retention cron. *New table must ship with a retention cron (project convention).*
  - **F-10 · Bulk entity enrichment** `Tier 2` `P3` — Run F-1/F-2/F-3 lookups across all actors/personas in a COP session in one action ("Enrich all entities"). Fan-out with `waitUntil`, results stored per-entity in the metadata JSON column. Needs F-1 or F-2 first.
  - 🛑 **DECISIONS (loop must NOT auto-do):** **D-F1** API key strategy for Shodan/VirusTotal/HIBP (user-supplied per-user vs. platform-held secrets) · **D-F2** which platforms to include in the F-1 username registry (some check pages have terms-of-service issues) · **D-F3** CF Browser Rendering API vs. external screenshot provider.

- **E · Research intake form upgrade** `mixed (Tier 2/3)` `Requested 2026-06-27` · **✅ decisions resolved 2026-06-27 — now loop-eligible (see Now item 0E)** — make `/dashboard/research/forms/new` (+ the submitter flow) a researcher-grade intake tool: **document upload with automatic scanning/extraction, interactive URL auto-confirm, rich typed fields, auto entity/claim/geo extraction, dedup, and search** — "paste a URL or drop a PDF and the form pre-fills metadata for you to confirm." **Full design, phasing, AUTO/DECISION split, and the reusable-building-block inventory: [`plans/2026-06-27-research-intake-form-upgrade.md`](plans/2026-06-27-research-intake-form-upgrade.md).** Key facts: there are **two form systems** — the named route sits on the weaker fixed-9-field `submission_forms`, while a richer dynamic-schema engine (`survey_drops.form_schema`, 19 field types incl. `file`, URL enrichment, dedup, geo) already exists **with no builder UI**. Almost every primitive is already built (PDF text via `unpdf` `pdf-extractor.ts`, `analyze-url` extraction, R2 write pattern, entity/claim extraction, Nominatim geocoding, archive-snapshot creation); only **EXIF/GPS-from-image** is missing. **Blocking DECISIONs:** **D-E0** converge on one engine (recommend System A dynamic schema, retire/redirect System B) — *blocks everything*; **D-E1** image-OCR provider; **D-E2** upload retention/PII-EXIF policy; **D-E3** anonymous-upload abuse limits. Also verify the R2 `UPLOADS` binding is dashboard-enabled (**TD-08**) before Phase 3, fix the orphaned `/dashboard/research/forms` list route, and address **#19** (`research/forms/list` workspace authz) while on this surface. **Suggested order:** Phase 0 (decide engine + R2 verify) → Phase 1 builder UI → Phase 2 URL auto-confirm → Phase 3 upload+auto-scan → Phase 4 intelligence + search.

**Suggested entry point:** **D0 SAT correctness fixes first** (live-feature bugs) → D1 architecture levers (promptQuestions→AI, evidence unification) + route the 5 dead frameworks → A1 SAT quick-wins → A2 Indicators → C-Phase-2 Reliability Engine → B Game Theory MVP. Full rationale + sequencing in the plan doc. **Item E (research intake form)** rides alongside these — its Phase-0 engine decision (D-E0) is the gate.

---

## Known limitations (accept, or fix above)
- **ACH ranking is now Heuer-correct** (disconfirmation / weighted-inconsistency, v0.22.4) **and evidence-quality-weighted** (real reliability/confidence/credibility, v0.22.12). Deception AI still falls back to a fabricated stub when the (unset) client key is absent (Now/#2).
- **Rate limiting is sustained-abuse only** (KV eventual consistency) — burst-accurate fix is Now/#1.
- **AI endpoints are open to any 16+ char hash** (`getUserFromRequest` auto-provisions) — mitigated by rate limiting + Tier-1 consent; deeper fix is real accounts.
- **Tier-1 consent gate's runtime fire is unverified** — the intermittent 401 that blocked this was resolved by the v0.22.2 503 fix (2026-06-19); the runtime fire (403 → consent dialog → success) is now **unblocked but still needs an actual verification pass**.
- **3 build-tooling vulns** persist (not shipped to the runtime Worker) pending the vite 8 migration (Later/#8).

---

## Working with Claude on this roadmap

- **Pick the top unblocked item in "Now."** Each carries file pointers and a "Done when" — treat that as the acceptance test.
- **Verify against prod the way prod actually behaves:** Pages Function `console.*` is NOT visible in `wrangler tail` — use the `event_logs` sink (`GET /api/cron/event-logs` with `X-Cron-Secret`).
- **Never run untested SQL against prod.** Apply + verify on local D1 first, then `--remote`. Back up before any migration: `wrangler d1 export researchtoolspy-prod --remote --output backups/...`.
- **D1 conventions:** lowercase snake_case tables/columns; entity types UPPERCASE (CHECK constraints); entity tables use `created_by`+`workspace_id`, framework tables use `user_id`.
- **Update this file as part of the work** — move shipped items to "Recently shipped" with a date, and keep the header date/release current. Mirror to both remotes: `git push origin main && git push gitlab main`.

## Operational reference
- **Deploy:** `./deploy.sh` (full, runs migrations) or `./deploy.sh --skip-migrate`. Don't deploy from repo root — the script copies `functions/` into `dist/` first.
- **Check prod logs:** `GET /api/cron/event-logs` with `X-Cron-Secret` (errors/refusals/rate-limits + 24h summary).
- **D1 health:** `wrangler d1 info researchtoolspy-prod` (size bounded by the daily cron).
- **Cron:** `researchtoolspy-cron` Worker, daily 04:00 UTC — content retention + `event_logs` pruning. Setup: [`CRON_CLEANUP_SETUP.md`](operations/CRON_CLEANUP_SETUP.md).
