# Saved-revision presentation

Baseline 12bf2d737d19245689341118c92a0b3bedae63a4 freshly equals clean integration HEAD, origin/main and gitlab/main. Worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; primary untouched. User explicitly authorized non-force pushes to origin/main and gitlab/main and reviewed prebuilt deployment to researchtoolspy/researchtools.net. Previous runtime ae238e63ed03b103553491a6f10b8fd4a672cdf8; deployment 9d1d77c6-7225-4980-81f0-c24d7c8df0c7.

Outcome: signed-in private workspace users can preview, present and export the last successfully saved/opened revision independently of current unsaved edits. Starting surface /dashboard/tools/timeline > Workspace saving > Preview saved revision. Saved revision number and full revision ID identify its provenance. The saved version is retained in memory only, may be older than the current server head, and is cleared on workspace/principal/deactivation changes. No new API, database schema, persistence, public sharing, revision-history browser, or publication claim. TL-17 remains partial.

Contract: WorkspaceSaving retains a cloned {snapshot, artifact} pair only after successful save/open. Saves retain attempt.snapshot, including lost-response retries, never current mutable edits. Failed/lost/conflicted saves and malformed reads do not replace this pair. Scope remount clears it and any open presentation. TimelineJSExport gains optional savedRevision {revisionId:string, sequence:number}; its trigger becomes Preview saved revision, title becomes Saved revision preview, captured export preserves the original saved exportedAt and saved revision is explicit. Existing open-workspace export default remains unchanged. Saved snapshot export uses the same bounded projection and complete companion; metadata is UI provenance only and never added to exported schemas. No new network requests to preview and no mutations merely opening/exporting/presenting.

Topology: shared cooperative native worktree; owner alone commits. tl00_recon owns only src/components/timeline/TimelineDurablePanel.tsx; owner owns TimelineJSExport.tsx and docs; validator_review owns only added/adjusted saved-presentation cases in tests/e2e/smoke/timeline-durable-browser.spec.ts. dependency_review performs independent read-only source/runtime/artifact acceptance; visual/receipt review separate after provider completion. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/. Authenticated external lanes unqualified.

Verification: candidate execution only pinned credential-free network-none Docker with read-only source/deps/root and no credentials/socket. Exact source snapshot. Focused tsc -b --noEmit and new saved-presentation browser cases both Chromium/mobile-safari. Regression partitions planned upfront: all timeline-durable-browser cases both projects; all timeline-timelinejs-browser and timeline-renderer-browser cases both projects plus timeline-renderer-contract and timeline-timelinejs-contract Chromium. Five existing typecheck commands, Vite build, pinned Wrangler pages functions build and scripts/verify-timeline-release.mjs against freshly exported read-only actual production schema. No full unrelated API suite claim. Browser acceptance covers unsaved edits versus saved bytes, revision advancement, lost-response retry, failed save/read retention, account/workspace clearing, renderer loaded and same snapshot downloads, screenshots desktop/mobile. Existing renderer protocol/header/assets unchanged.

Publication acceptance: independent exact source/runtime and staged file comparison; fresh both main refs; non-force push then --no-bundle prebuilt deploy; existing 30 read-only live checks per URL, unchanged schema/migrations/secret-name inventories. Renderer bytes unchanged so existing edge behavior plus exact live shell checks apply; new saved UI exercised in guest browser against real D1 handlers, not authenticated live user data. Durable receipts state exact counts and limitations. Stop for divergence, unsafe validator, outside ownership or unresolved privacy seam. No real user timeline publication.
