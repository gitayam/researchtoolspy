# Shared presentation link previews

Baseline: caf12140e84924633415a48f879d3e0bb3133c70, freshly fetched origin/main and gitlab/main equal; integration clean. User authorizes improving shared URL previews and continuing clean main pushes/deployment. Primary checkout remains untouched. Delivery: user-visible link cards and prepublication preview.

Outcome: /present/<token> serves per-presentation Open Graph/Twitter metadata in initial HTML for both browsers and crawlers; a dedicated 1200x630 PNG timeline card replaces the generic site image. Sharing review shows the same title/description/image before publication. Existing frozen snapshot is the only content source. Chat apps may retain cached previews after revocation; disclose this accurately.

Contract: shared browser-independent src/lib/timeline-link-preview.ts exports timelineLinkPreview(timeline: TimelinePresentation['timeline']) returning {title,description,eventCount,imagePath,imageAlt}. imagePath=/timeline-share-card.png. title is bounded plain text <=150 Unicode codepoints, fallback Timeline presentation. description <=240 codepoints, starts with event count then bounded title framing, fallback invitation to explore the timeline. Plain text decoded once; HTML escaped at output. No date inference, hidden workspace fields or event assessments in metadata. PNG has no user content.

Server route: functions/present/[[catchall]].ts, GET/HEAD only, exact 64lowerhex token path, no queries, read through existing readTimelinePresentation + presentationRoute (active human owner, hash/schema validation, revocation). No new SQL/API/schema/auth/dependency. Use ASSETS /index.html and HTMLRewriter to replace generic title/description/canonical/OG/Twitter tags for all user agents. Preserve SPA asset scripts and strict self-only CSP, no-store/no-referrer/noindex/nofollow, nosniff; no CORS. Invalid/revoked/corrupt/inactive links return generic shell404 with no payload metadata; DB/asset failure503. HEAD carries no body. Canonical/image URLs from actual request origin only, never forwarded headers; do not expose query/fragment.

Ownership: server provider owns new helper, new route, new contract spec. UI provider owns TimelinePresentationShare.tsx, new link-preview browser spec and public/timeline-share-card.svg artwork. Owner owns PNG generated in isolated validator, public/_routes.json include, compiled verifier integration, docs and private release helpers. Native workers are cooperative shared editors, no commits. Independent crossreviews exclude own files. Owner commits all.

Verification: candidate execution only pinned credential-free Docker via existing validation/container-run-renderer.py, read-only exact git archive, no network/auth. Cwd /workspace. Focused Playwright contract/browser chromium+mobile-safari, existing sharing contract/D1/browser regressions; five TypeScript checks, Vite/worker builds, scripts/verify-timeline-release.mjs real compiled GET/HEAD metadata lifecycle with synthetic D1 and local ASSETS. PNG generated via isolated Playwright from reviewed SVG, inspected before final source commit. Exact-source reviews, package comparison, live readonly probes and bothmain sync receipts. No production research publication or migration; route live unavailable state exercised without creating content. Actual third-party chat posting is outside scope.

Stop conditions: undecided contract, outside finite ownership, dependencies/auth/config/schema changes, unavailable validator. No host candidate scripts; no sourcing cop-api.sh; no production actions by providers. Existing authorization permits owner-only reviewed pushes/deployment separately from plan-only controller publication.
