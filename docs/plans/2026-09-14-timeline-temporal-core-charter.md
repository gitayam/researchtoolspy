# Timeline calendar temporal core tranche

Baseline: 2eba1ed4a0508a176b54d4bb444e899739f1c204, freshly fetched identical HEAD, origin/main and gitlab/main; clean shared integration worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911. Primary checkout is preserved. Integration-enabling TL-06 slice: reusable calendar claim validation and conservative extents consumed by existing timing review. No new UI or persisted workspace fields. Next visible gate is versioned interval authoring. TL-06 remains in progress.

Contract: new src/lib/timeline-temporal.ts exports CalendarLabel { date: string; precision?: 'year'|'month'|'day'; time?: string }, CalendarTemporalClaim discriminated union with schema:'timeline-calendar-claim.v1', displayText:string, and kind:'date' with value:CalendarLabel; kind:'interval' with start/end:CalendarLabel; kind:'relative' with relation:'before'|'after', anchorEventId:string; or kind:'unknown'. This is an internal integration contract, not extraction/workspace v2 or a public service capability. No timezone/instant/circa inference. Strict parseCalendarTemporalClaim(value:unknown) returns {ok:true,claim:CalendarTemporalClaim} or {ok:false,reason:'invalid-claim'|'invalid-date'|'invalid-interval'}. Reject extra keys, wrong types, empty anchor IDs, displayText over 4096 chars. Preserve supplied displayText and label precision exactly. Calendar labels allow Gregorian years 0001..9999 and HH:mm[:ss] only with full day; existing authoring minimum 1000 unchanged.

calendarLabelBounds(label:CalendarLabel) returns {start:number,end:number} or {reason:'missing-date'|'invalid-date'}, retaining shipped timing review behavior exactly. Bounds are half-open numeric calendar coordinates, not UTC instants. Precision spans entire recorded year/month/day/minute/second. temporalClaimBounds(claim:CalendarTemporalClaim) returns bounds or {reason:'relative'|'unknown'|'invalid-date'|'invalid-interval'|'invalid-claim'}. It validates runtime inputs. Interval endpoints are inclusive recorded units: extent begins at start's lower bound and ends at end's exclusive upper bound. Reject definitely reversed endpoints (start.start >= end.end); overlapping endpoint ranges are allowed as a conservative possible extent, never a certain duration or definite endpoint ordering. Use min(start.start,end.start) and max(start.end,end.end) to avoid discarding uncertainty for overlapping mixed precision. Relative/unknown claims never resolve from workspace ordering. No mutation, sorting, clock mapping, source assertion promotion, schema/API/discovery/persistence changes or dependency additions.

Ownership: tl00_recon native cooperative provider owns src/lib/timeline-temporal.ts only; validator_review native cooperative test author owns tests/e2e/smoke/timeline-temporal-contract.spec.ts only; owner owns src/lib/timeline-timing.ts integration and docs/private release helpers. dependency_review independently reviews exact committed source and artifacts. No worker commits, candidate execution or production actions. Owner commits after workers quiesce. Shared editor topology, no isolation claim; external model dispatch disabled. Read CLAUDE.md without sourcing auth helper, .team/project.json and existing tranche charter. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/.

Verification in credential-free pinned network-none Docker only: trusted snapshot.py and container-run-renderer.py, cwd /workspace; node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium tests/e2e/smoke/timeline-temporal-contract.spec.ts plus all previous 36 timing/ordering/renderer/TimelineJS/history contract cases. Existing 34 browser checks in Chromium/mobile-safari preserve timing review/date entry/export/presentation seams. Five existing typechecks, Vite and compiled worker build, fresh read-only production schema verification. Independent exact-source review; unchanged public renderer tree receives existing exemption from edge replay with live byte checks. Existing presentation browser captures remain verified. Runtime receipt lists exact counts and commands, never source-review-only acceptance.

Authority: user previously authorized clean non-force pushes to origin/main and gitlab/main and prebuilt ResearchTools Pages production deployment after accepted stages, separate from pilot controller plan-only publication. No production data writes. Preserve failed receipts; stop on divergent refs, unsafe validator, unsettled contract or scope expansion. Postflight read-only checks on deployment and production, unchanged schema/migrations/secret names, then durable receipt/roadmap/register and docs-only sync. One non-visible integration stage after two visible stages; prioritize interval authoring next.
