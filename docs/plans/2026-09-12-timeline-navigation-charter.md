# Timeline presentation search and jump

Baseline d297d5ab78ac70f8e85cd2a832ebefcd290e931d freshly equals clean integration HEAD, origin/main and gitlab/main. Integration worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; primary preserved. User authorization continues to cover clean non-force pushes to both main refs and reviewed prebuilt deployment to researchtoolspy/researchtools.net. Previous accepted runtime a11ef636a5fbde09d17cb58b19afb4f9047092ee, deployment971c8e13-acd6-4f1c-bc55-5829ac10c2af.

User-visible scope: add native search by title, recorded date and displayed event text to the existing TimelineJS accessible event list; clear/reset and no-results feedback; each matching event has a keyboard-operable Show in presentation action. Search changes only list visibility, never narrative selection, exported bytes, chronology or renderer slide membership. Query and disclosure state survive theme/start/retry frame replacement while jump commands never replay into replacement frames. Native list remains usable when renderer fails. Jump disabled until successful load. No persistence, API, auth, schema, dependency, header or public-publication changes; TL-17 remains partial.

Authoritative seam: new parent-to-child message {type:'timelinejs:navigate',nonce,eventId}. Child accepts only exact parent source and frame nonce, exact fields, after successfully loaded current projection, and eventId exactly in its frozen allowed event IDs. No title/URL/data-object navigation. Invalid, stale, pre-load and unknown-ID commands are ignored without changing state or exposing content. Calls pinned public TimelineJS goToId(id). Existing render validation remains one-shot and unchanged; sandbox, CSP exact hashes, Cloudflare data-cfasync=false, zero external requests and projection-only transfer remain. Native controls and render failures retain their existing behavior.

Topology: cooperative native provider owns ONLY public/timelinejs/preview.js and tests/e2e/smoke/timeline-renderer-contract.spec.ts; no commit or candidate execution. Owner owns React TimelineJSPreview.tsx, renderer browser acceptance, generated preview.html and docs. Provider may read pinned vendor source and skill instructions. Owner alone assembles exact shell with trusted data-only helper and commits. Independent dependency_review checks source/protocol/runtime/artifact; validator_review checks representative screenshots and durable receipts. External authenticated lanes remain unqualified. .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/ prohibited.

Acceptance: candidate execution ONLY pinned credential-free network-none Docker validator. Focused main typing + renderer contracts + renderer/export browser tests in Chromium/mobile Safari: duplicate titles and partial dates, search/clear/no-match, native keyboard jump, retained query/disclosure with no old jump on remount, loading/failure controls, unknown/stale/wrong-source/extra-field/pre-load child messages, unchanged snapshot/export and slide count, zero external requests; light/dark mobile/desktop screenshots. Then full scripts/verify-timeline.mjs (five type checks/all API/browser tests/build), pinned Wrangler worker build and scripts/verify-timeline-release.mjs against current read-only production schema inputs. Compare all staged files independently, preserve failed attempts and source hashes. Reviewed non-force pushes, --no-bundle prebuilt deploy, 30 live checks eachURL including exact owned shell bytes, and network-none browser replay of captured production HTML/headers including jump. No full accessibility or public publication claim.

Stop for outside ownership, unsafe validator, divergence or unresolved data/security seam. Read official method contract at https://timeline.knightlab.com/docs/json-format.html (stable unique_id supports goToId). Original provider prompt/instruction hashes are frozen before dispatch; record amendments separately.
