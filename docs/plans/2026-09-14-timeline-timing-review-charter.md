# Timeline timing review tranche

Baseline b2269e46462e3a5d9840a70091f35b11ee7626d1 freshly fetched equal clean integration HEAD/origin/main/gitlab/main. Shared integration /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; primary untouched. Native cooperative editors; integration owner alone commits after quiescence. Candidate execution only pinned credential-free network-none Docker validator; no host candidate scripts, auth helper or external model dispatch.

Outcome: user-visible initial TL-06 timing diagnostic at /dashboard/tools/timeline > Analyst view > Timing review. Compare each recorded relative before/after placement with its anchor's recorded calendar bounds. Show agreement, disagreement or unresolved without changing any event, selection, original extraction, backup or saved revision. This is analyst placement consistency, not independent evidence validation or inferred actual timing. TL-06 remains in progress; structured intervals/circa/clock mappings/extraction v2 remain pending.

Contract: exported pure reviewTimelineTiming(events) returns one row per relative event in supplied order: {eventId, anchorEventId, relation:'before'|'after', status:'consistent'|'conflict'|'unresolved', reason:'ordered'|'reversed'|'overlap'|'missing-date'|'invalid-date'|'missing-anchor'}. Unknown/missing/self/ambiguous duplicate anchor fails unresolved. Bounds are valid proleptic Gregorian recorded calendar ranges, years0001..9999, year/month/day strings retaining precision. No time => entire date range. Day+HH:mm covers that minute; day+HH:mm:ss covers that second. Partial date+time unresolved (no invented date). Validate calendar/precision mismatches and malformed time. Use half-open ranges: before definite if A.end<=B.start, conflict if A.start>=B.end, else unresolved overlap; invert for after. Compare calendar labels in the common workspace context only, not UTC instants or daylight-saving resolution. No parsing with Date.parse, local timezone arithmetic or locale-dependent sorting. Do not infer positions/narrative order as temporal relations. No evidence transitivity, cycle solver or clock conversions. Stable IDs; no mutation/persistence/network.

UI: accessible details summary Timing review, compact counts for placement/date disagreements and unresolved checks; render only when relative rows exist. Clear neutral copy and distinct textual status badges; both event and anchor titles/dates with direct event links. Whole check list can scroll/wrap on mobile; no giant new toolbar. Named links Review event TITLE and Review anchor TITLE focus the event in existing sequence. Unknown results never presented as agreement. All rows remain inspectable; diagnostic transient and updates after edits/import. Existing event/section labels retained.

Ownership: tl00_recon owns src/lib/timeline-timing.ts and tests/e2e/smoke/timeline-timing-contract.spec.ts. Owner owns src/components/timeline/TimelineTimingReview.tsx and TimelineResults.tsx plus docs/private helpers. validator_review owns tests/e2e/smoke/timeline-timing-browser.spec.ts. dependency_review read-only independent source/runtime/visual/artifact audit. All providers read .team/project.json, CLAUDE.md and this charter; no commit or candidate execution. Prohibited .team/ .env* deploy.sh wrangler.toml workers/ containers/; no API/schema/vendor/public renderer/security changes.

Verification: exact committed snapshot via trusted snapshot.py/container-run-renderer.py, guest cwd/workspace. node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium tests/e2e/smoke/timeline-timing-contract.spec.ts; new browser suite both chromium/mobile-safari. Existing ordering/TimelineJS/renderer/history contract suites plus real renderer/TimelineJS browser suites both projects. Five type checks, Vite/worker build and compiled actual production-schema gate via reviewed build-command.json and fresh read-only schema. Independent final-source source/visual/runtime/all-artifact review. No lowered text/backup/navigation oracles; public renderer tree byte unchanged, same edge-script replay exemption with live owned-byte checks. Distinct final-source inventory/count captured, no borrowed prior counts.

Authority persists: clean non-force origin and gitlab HEAD:refs/heads/main; trusted prebuilt researchtoolspy Pages production deploy --no-bundle. Separate from pilot controller plan-only publishing. Prior runtime0bdb3590887d054cd2f5e2737c42cc34e5e63122 deployment11685fa7-7411-4ba2-895b-1dd4e8d6e3d3. Stop on diverged refs, ownership expansion, unsafe validator, source/data/security API contract expansion. Postflight30read-only checks each URL, unchanged14tables386rows13migrations/secretnames; later docs-only receipt/roadmap/register sync.
