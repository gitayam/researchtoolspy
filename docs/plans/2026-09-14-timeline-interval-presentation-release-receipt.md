# Recorded intervals in TimelineJS presentations

Recorded intervals now appear in **Open presentation** and TimelineJS JSON, with both endpoints shown in slides and the accessible event list. Recorded calendar and clock precision is preserved. Year/month geometry is a renderer convention, not an exact duration. An overlapping-precision interval whose end would plot before its start remains a labeled slide without a span and includes an explanation.

Presentation scheduling keeps recorded intervals fixed and displays their range. An explicitly timed complete recorded end anchors following automatic steps; untimed or partial ends show a warning and retain the previous usable anchor/default. Complete ResearchTools backups remain unchanged. Private interval saving remains unavailable; TL-06 and TL-17 remain in progress.

Runtime `7064a61aff38524c604f0c1bc14801379b788f9e` was synced nonforce to both main refs and deployed as `869c6b6b-08a9-433f-8337-63f7bdc8d63a` at [the accepted build](https://869c6b6b.researchtoolspy.pages.dev). 73 distinct checks passed (39 contracts, 34 browser-project checks), plus five type checks and frontend/worker/schema builds. Independent review verified 155 package files and representative light/dark desktop/mobile screenshots. Thirty read-only live checks passed per URL. Fresh production-served renderer HTML was replayed with blocked network in Chromium and mobile Safari; navigation, endpoint labels, sandbox isolation and blocked edge scripts passed.

No production data/schema/migration changes occurred: 14 tables, 386 catalog rows, 13 applied migrations and secret names remain unchanged. The prior deployment preserves local v2 intervals but cannot present them. [Detailed receipt](./2026-09-14-timeline-interval-presentation-release-receipt.json) records exact source, evidence hashes, preserved failed attempts and recovery. Full unrelated API testing and exhaustive accessibility certification are not claimed.

Recovery: Initial read-only schema export returned Cloudflare authentication error 10000. The failed receipt/log were retained privately and one bounded retry passed all inventories; no production data writes occurred. Review found two obsolete help messages; commit 129f8308a corrected them. Preliminary source 9adfdc513 passed contracts, focused browsers and build; the driver stopped at its source-change guard before broad regressions. Source 129f8308a then passed contracts and focused browsers. Screenshot inspection found the range explanation displaced the event description, so commit 7064a61af moved the explanation after the description and assessment and strengthened the existing visibility assertion. The source guard again stopped older-source jobs. All 73 accepted checks and the build ran on final source 7064a61af; preliminary runs remain preserved and excluded from final counts.
