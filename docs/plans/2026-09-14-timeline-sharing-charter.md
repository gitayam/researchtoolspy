# Direct presentation sharing

Baseline: 8b04019534d6406c16cc127386ebe349838eb831 on freshly fetched origin/main and gitlab/main; clean shared integration worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911, branch work/timeline-foundation-20260911. No divergence. Primary checkout untouched.

Outcome: user-visible Share presentation action in the current export/presentation dialog. Explicit Publish creates a frozen selected presentation and Copy link opens a public, read-only /present/:token page without login. Creator can list and revoke their links. Any holder can read until revocation; copies already downloaded cannot be recalled. User asked for direct links; unanswered optional audience preference defaults to anyone-with-link with revocation, not workspace-wide authorization. No data is uploaded merely by opening preview. Owner must not publish user research during implementation/testing.

Contracts: store only strict timeline-presentation.v1 payload {schemaVersion,timeline} from current buildTimelineJSExport(...).timeline, not workspace/evidence/source/backup. Preserve intervals, escaped HTML whitelist, scheduling text and 1..100 events. Limit JSON to512KiB. Shared codec in src/lib/timeline-presentation-contract.ts exposes TimelinePresentation, decodeTimelinePresentation(value):TimelinePresentation throwing on invalid, presentationPlainText(string):string (only safe escaped formatting, render via React text, never HTML insertion). Reuse date and projection security rules in preview.js, including optional end_date and default-component nonreversal. Public response excludes owner/user IDs, request keys and private metadata. No arbitrary URLs, media, input code, private backup, source fetch or model call.

API: POST /api/timeline-presentations body {schemaVersion:'timeline-presentation.v1',timeline} plus Idempotency-Key UUID; GET same path lists creator links; GET /api/timeline-presentations/:token is anonymous read; DELETE same path revokes only creator. Token is cryptographically random64lowercasehex. Human create/list/revoke uses requireTimelineHuman (no guest provisioning or service tokens). Create response201 (replay200) {schemaVersion:'timeline-presentation-link.v1',token,createdAt,revoked:false}; list200 {schemaVersion:'timeline-presentation-links.v1',links:[{token,createdAt,title}]} active links only, max20. Public GET200 {schemaVersion:'timeline-presentation.v1',timeline}, no envelope metadata; nonexistent/revoked/malformed all404 with generic unavailable. DELETE204 idempotent for owner, unknown/other-owner404. Standard errors {schemaVersion:'timeline-presentation-error.v1',error:{code}} with401/403/409/413/503 as applicable. Same-origin API, no wildcard CORS. All responses Cache-Control:no-store, Referrer-Policy:no-referrer, X-Robots-Tag:noindex,nofollow and nosniff; JSON content type except204. Never log tokens or payloads. Public reads require creator still active/non-guest, revocation takes effect on future fetch; public viewer uses cache:no-store and explicit Refresh.

Storage: new forward-only schema/managed-migrations/0014_timeline_presentations.sql; table timeline_presentations token primary key, owner_id foreign key users, request_key, payload_hash, payload TEXT nullable after revoke, created_at, revoked_at. Unique(owner_id,request_key), index(owner_id,revoked_at,created_at). Immutable payload/token/hash/request/owner/creation except one-way revoke simultaneously sets payload=NULL; no resurrection/replace. Enforce quota max20active and active human predicate in INSERT SQL atomic statement, owner+activehuman predicate in revoke. Replay same owner/key/hash returns same active link; same key/differenthash or revoked returns409, never republishes. Server validates before write. New table only; no existing schema/table/data/migration edits. Fresh-install and upgrade-from-production-prefix must both pass actual isolated D1/Miniflare; existing catalog preservation distinguished from new table. Owner adapts compiled release gate to exact0014 and verifies deployment prerequisite, not broad pending migration relaxation.

UI: Share button available in export details and presentation header, opens explicit review panel showing title/count and public scope (everything visible in selected presentation including framing, assessments and schedule; full workspace excluded). Publish current frozen preview, disable stale/invalid/empty/over100/oversize, maintain same idempotency key on ambiguous retry. Auth change must invalidate pending results; no auto upload or cached private snapshot in browser storage. Copy link fallback selectable URL, status and revoke; management list only authenticated creator, fetched explicitly when requested. Unsaved workspace preserved. Public /present/:token route standalone read-only with title, renderer, accessible plain-text event list/search/jump, loading/unavailable/retry, no workspace download/edit/login prerequisite. No dangerous HTML insertion. Renderer sandbox/nonce/CSP/vendor unchanged.

Ownership: provider tl00_recon owns shared codec, server routes functions/api/timeline-presentations/index.ts and [token].ts, functions/api/_shared/timeline-presentation-store.ts and0014migration only. provider validator_review owns src/components/timeline/TimelinePresentationShare.tsx, src/pages/PublicTimelinePresentationPage.tsx, src/lib/timeline-presentation-client.ts and new tests/e2e/smoke/timeline-sharing-browser.spec.ts only; consumes frozen codec/API. Owner owns TimelineJSExport.tsx and TimelineJSPreview.tsx seams, routes registration, server integration/contract tests, release schema script/private helpers/docs. dependency_review independent read-only source/security/migration/runtime acceptance. Native cooperative editors shared filesystem no commits or candidate execution; owner commits after quiescence. One backend provider and one frontend provider plus owner, then independent reviewer; no external model lane qualified.

Execution: read .team/project.json, CLAUDE.md and foundation charter; never source scripts/cop-api.sh. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/, dependencies and unrelated changes. Candidate installs/scripts/builds/tests only pinned network-none credential-free Docker /workspace via established supervisor. Exact Playwright commands node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium [--project=mobile-safari] tests/e2e/smoke/NAME.spec.ts, selected sharing/renderer/timelinejs/interval contracts/browser plus real compiled new API/D1 gate and existing five types/Vite/worker/schema gates. Freeze exact inventory/plan at committed source. Current-snapshot copy/interval/browser data preserved. Synthetic fixtures only; no real data publishes.

Publication: prior explicit user authorization covers clean nonforce origin HEAD:refs/heads/main, gitlab HEAD:refs/heads/main and prebuilt researchtoolspy Pages main deployment after exact-source acceptance. Additive0014 is necessary for requested sharing; prepare concrete reviewed migration with fresh production preflight and isolated full/upgrade rehearsal before applying. No edits to old migrations, no user research publications, no unrelated production writes/config/secret changes. Controller pilot publication remains plan-only; owner performs authorized release. Postflight catalogs/migrations/secretnames, anon denied mutation and public-not-found/live byte gates. Rollback old runtime leaves additive table; revoked content cannot be restored by rollback. Persist source/ref/deployment/migration states separately. Stop for divergence, unsafe validator, failed migration/authorization boundary or unsettled contract; owner resolves scope without exposing data.

Owner seam clarification: API malformed create requests use400 invalid_request; malformed public token remains404. Management list titles are safe plain text truncated to200 Unicode characters including ellipsis, rendered directly without another decode; immutable full titles remain intact. Owner additionally owns functions/api/_middleware.ts sharing-only dispatch (no broad CORS/telemetry, mismatched Origin denied) and public/_headers /present/* restrictions (no-store/no-referrer/noindex, self-only scripts/connect to exclude third-party analytics). Existing renderer shell/script/CSP and all other route behavior remain unchanged. Exact header/compiled middleware tests are required. Existing managed-chain migration test is extended through0014 and its prior0010 upgrade lane actually applies its declared suffix; no historical migration bytes change.

Verification ownership amendment: after interface implementation became quiescent, tl00_recon owns only new timeline-sharing-d1.spec.ts, validator_review owns only new timeline-sharing-contract.spec.ts, and dependency_review owns only scripts/verify-timeline-release.mjs to implement its reviewed migration rehearsal. Owner independently reviews those additions; backend/frontend providers cross-review code they did not author at the final committed SHA. All providers remain cooperative editors, no execution or commits. Private prompt amendments are recorded with checksums before candidate validation.

Owner reader-shell seam: src/App.tsx uses router location subscription to render public presentation routes outside authentication checks, analytics, consent, guest conversion and workspace providers. Other routes retain their existing providers. Public browser tests must prove no workspace/auth/analytics/guest API requests occur. This avoids side effects when a recipient opens a link.
