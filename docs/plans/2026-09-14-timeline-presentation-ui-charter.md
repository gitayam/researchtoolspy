# Timeline presentation readability

Baseline0ecf8d552f48c0edfb51ce185f196c87f4414bde freshly fetched equal clean integration HEAD/origin/main/gitlab/main. Shared integration /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; preserve primary and unrelated cwd. User-visible outcome: /dashboard/tools/timeline > Present has readable story typography, compact navigation without squeezed adjacent headlines, legible marker/axis labels, and a bounded reader layout on desktop/mobile. Source evidence and all presentation scheduling behavior remain unchanged.

Contract: CSS and parent presentation layout only. Provider tl00_recon owns public/timelinejs/preview.css; owner owns src/components/timeline/TimelineJSPreview.tsx, TimelineJSExport.tsx, assembled public/timelinejs/preview.html and docs/private helpers. validator_review owns tests/e2e/smoke/timeline-renderer-browser.spec.ts only. dependency_review independent source/runtime/visual/artifact reviewer. No preview.js/vendor/script/font/CSP/header/API/schema/dependency changes. Reader content starts near top at all widths, max readable measure, clear date/title/body hierarchy, unitless line heights, deliberate paragraph spacing. Hide narrow adjacent slide titles/descriptions; retain desktop arrows and native Previous/Next navigation with44px targets. On phones retain upstream hidden side arrows and use the named native buttons, preserving story width. Marker labels are compact clamped/ellipsized without overlapping lines; date-axis labels must not wrap into overlapping rows. Keep upstream marker positions, zoom/pan and chronological geometry. Mobile and desktop layout use same semantic content, safe scrolling for long provenance; parent modal height capped to avoid huge empty story areas on tall viewports. Preserve accessible list/search/jump/retry semantics and names, keyboard and sandbox. Do not solve visual crowding by removing content from projection or altering data.

Native cooperative editors with exact disjoint ownership, no commits/candidate execution; owner commits after final quiescence. Read .team/project.json, CLAUDE.md (never source auth helper), foundation charter and this charter. Prohibited .team/ .env* deploy.sh wrangler.toml workers/ containers/. Credential-free pinned network-none Docker validator only for all candidate tests/builds/scripts; external authenticated model lanes unqualified. Host trusted reads/edits/Git/data-only shell assembly/publisher only.

Verification: trusted snapshot.py then container-run-renderer.py RUN node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium [--project=mobile-safari] tests/e2e/smoke/FILE; guest cwd/workspace, onejob/oneworker. Focused new dense20event long-title browser fixture light/dark desktop/mobile full-bounds text, navigation and marker typography first; full renderer-browser and TimelineJS-browser both projects, renderer/TimelineJS/history contracts Chromium. Deduplicate focus from final partitions by exact case-key inventory. Five typechecks and Vite/worker/compiled schema build via prior reviewed build-command.json using fresh read-only production-schema inputs. Deterministic shell assembly; independently prove HTML outside style blocks, vendor/bridge and security bytes unchanged. Existing full heading/date/provenance reachability and backup tests retained. Representative screenshots reviewed before publication. No fresh edge-script replay for CSS-only shell with identical executable/security bytes; actual renderer and live owned-byte checks required.

Authority: user explicitly authorized clean non-force origin HEAD:refs/heads/main and gitlab HEAD:refs/heads/main and production researchtoolspy Pages prebuilt --no-bundle deployment. Same established publisher separate from pilot controller plan-only output. Release only exact-source reviewed/validated artifact, fresh equal refs; postflight30readonlychecks eachdeployment/production, unchanged schema/migrations/secretnames, durable receipt/roadmap/register then docs-only sync. Stop on divergent refs, unsafevalidator, ownership/API/securityscope expansion. Previousruntime89c33f1e9b00b1072d0253657b29a3add0195d77 deployment0c6011ec-377f-40be-ac09-0de0b7e195a8. This is presentation UI repair, not TL13 planning or completion of TL17 publication.
