# Timeline visual hierarchy and navigation

Baseline `7fdcac02909a62e1603b8bf6a83e69cd7bc3cd1c`, freshly equal origin/main and gitlab/main. Shared integration `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911` is clean; primary checkout remains untouched. User requests clearer timeline UX/UI, then continued implementation; existing explicit authorization covers non-force pushes to both main refs and reviewed deployment. Current project Git policy and team-orchestrator rules apply; candidate execution only in qualified credential-free network-none Docker.

User-visible outcome: a recognizable event-led workspace with strong section hierarchy, distinct event/evidence/judgment/question/narrative treatments, readable date anchors, restrained surfaces and clear primary/secondary actions. Meaning always has text or icon plus label, not color alone. Improve navigation as the implementation continuation: a compact workspace contents bar leads directly to sequence, evidence/analysis and narrative; preserve stable event/chapter IDs and native fragments. Scope is presentation/navigation only, with no data/auth/API/source/integrity/storage semantics change. Do not mark later roadmap stages complete.

Owner owns TimelineResults.tsx, pages/tools/TimelineAnalysisPage.tsx, timeline-workspace.css, navigation seams, browser fixtures, docs and exact-file commits. Cooperative native provider tl00_recon owns only TimelineEvidence.tsx, TimelineJudgments.tsx, TimelineNarrative.tsx, TimelineDurablePanel.tsx under src/components/timeline. Keep all labels, selectors, handlers, forms, default disclosure behavior and state lifecycle compatible. Provider may add semantic classes and Lucide icons but no packages, candidate execution, commits, network, credential or deployment actions. Reviewer dependency_review independently checks exact source and runtime; validator_review can review visual evidence. Private immutable prompts and hashes are recorded before dispatch.

Visual contract: restrained blue event cards on a timeline rail; teal evidence panels whose styling does not certify truth; violet judgments with separated confidence/likelihood and dissent; amber research questions/gaps; indigo narrative controls; slate private-saving panel. Use meaningful headings/section eyebrow labels, border accents, spacing and icon shapes. Support light/dark, 390px mobile and desktop without horizontal overflow. Buttons and form labels wrap. Visible focus, native details keyboard behavior and legible contrast must remain. Main event sequence should be easy to reach before long advanced editors; do not silently hide existing contents or remove editing affordances.

Acceptance: representative multi-event/mixed-evidence/unknown-date/question/judgment browser fixture screenshots for desktop/mobile, light/dark; real navigation and keyboard checks, horizontal containment, existing import/export/private save/reopen regressions. No implementation-mirroring style unit tests. Full five type checks, API/browser verification and compiled production-schema rehearsal on exact committed snapshot, independently compared prebuilt artifact. No migrations or credentials; read-only live checks and schema inventory comparison. Record honest visual limitations, failed/recovery attempts, source/runtime/doc SHAs separately. After visual changes, finish navigation improvements before the final release gate; broader TL-04 remains partial.
