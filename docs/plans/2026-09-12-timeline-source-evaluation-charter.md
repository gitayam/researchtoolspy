# Timeline assertion source evaluation

Baseline `66f5a049a5a28363d8e3ffdde9d7a425d36b6108`, freshly equal origin/main and gitlab/main; clean shared integration `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911`. Primary checkout untouched. Prior visual/navigation stage deployed; no divergent commits or changed-on-both seams. User continuation and prior explicit authorization cover implementation, non-force main pushes to both remotes and reviewed prebuilt production deployment.

User-visible TL-04 slice: in the existing Timeline evidence disclosure, analysts record separate source-quality factors with rationale and inspect changed-input notices. Available to guest/local and private saved timeline users at `/dashboard/tools/timeline`, no flags. Synthetic source/assertion fixture is the demo; marker is “Source evaluation”. Existing snapshots and JSON exports preserve evaluation; previous revisions retain prior content. No automatic truth, confidence, independence or corroboration score. TL-04 remains partial; no new source store, authenticated peer-review service, route or migration.

Frozen contract: optional `evaluation: TimelineSourceEvaluation` on each assertion; legacy absence remains absent. Exact schemaVersion `timeline-source-evaluation.v1`; fields `access`, `reliability`, `credibility`, `currency`, `completeness`, `bias`, `deception`, `reviewedAt`, `basis`. Each factor is exact `{value,rationale}`; rationale max1000, nonblank for every assessed value (unassessed may be blank). Access values unassessed/direct/indirect; reliability and credibility unassessed/low/medium/high; currency unassessed/current/outdated/unclear; completeness unassessed/complete/partial; bias and deception unassessed/no_indication/possible/indicated. No numeric score. reviewedAt is valid ISO timestamp. Basis is bounded canonical JSON max262144, schema tagged and bound to assertion ID plus full selected assertion ancestry and corresponding source metadata, with evaluations excluded everywhere to avoid recursion. Current basis computed by `timelineSourceEvaluationBasis(evidence, assertionId): string`; `timelineSourceEvaluationNeedsReview(evidence, assertionId): boolean` treats missing/invalid bases as needing review. Export `validateTimelineSourceEvaluation(value)` strict validator and `emptyTimelineSourceEvaluation(basis, reviewedAt)` factory. Old bases preserved as stale. Existing evidence/judgment bases continue including complete assertion evaluations, so evaluation changes stale previous corroboration/judgment review; legacy hashes unchanged. Evaluation never confers corroboration automatically.

Editor freezes input basis when opened; saving after a concurrent input change retains that opening basis and shows needs-review rather than silently attesting to new inputs. Assertion edits preserve existing evaluation. A replaced evaluation survives immutable snapshot history; current content is client editable and not an authenticated reviewer record. Existing 60KiB canonical snapshot /64KiB request limits remain authoritative and failures are visible.

Ownership: cooperative native tl00_recon owns only src/types/timeline-workspace.ts, src/lib/timeline-evidence.ts, new src/lib/timeline-source-evaluation.ts and new tests/e2e/smoke/timeline-source-evaluation-contract.spec.ts. Owner owns UI component/seam, browser and durable D1 tests, verifier registration, API docs, release docs and all commits. Native reviewers are read-only; no candidate execution, commits, network/credential/production changes by workers. Disjoint shared-worktree topology. Provider library precedes consumer integration; owner reserved. External authenticated worker lanes remain unqualified.

Verification: owner runs candidate code only in established pinned network-none credential-free Docker validator from /workspace. Focused: node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium timeline-source-evaluation-contract.spec.ts timeline-workspace-snapshot-d1.spec.ts; browser: new evaluation flow in timeline-durable-browser.spec.ts on chromium/mobile-safari. Full: node scripts/verify-timeline.mjs (five type checks, all API/browser checks, Vite build); compiled worker via pinned Wrangler pages functions build, then node scripts/verify-timeline-release.mjs against readonly actual production schema and migration inventory. Prove malformed fields rejection, legacy absence/hashes, ordering-independent complete lineage, stale ancestor/source edits, nonrecursive evaluation basis, changed evaluations stale downstream review, private save/reopen/history, export/import, frozen edit basis, keyboard/mobile/dark representation and size failures. Independent exact-SHA source/runtime/byte-identical artifact review; read-only live checks, unchanged schema/migrations/config/secret-name inventory. No candidate scripts on authenticated host. Stop on scope crossing, undecided schema, remote divergence, missing isolation or review blocker; retain failed attempts. Record redacted receipts and actual partial completion separately from publication/deployment.
