# Timeline presentation scheduling

Baseline: 5fb33532b13c0de03eacdb7e5a4317a3a6e2aa3f, freshly fetched equal origin/main, gitlab/main and clean integration HEAD. Shared integration /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911. Primary and unrelated cwd untouched. Prior runtime 1fccfb3051a6b0144c7ed24ae1327bb385a21352, deployment ab6d7fc7-aaa8-4e7c-9b87-28533dccf7a7. Delivery user-visible: timeline Export TimelineJS adds an explicit presentation schedule for selected undated or dated events, defaults date to local today, optional per-event date/time overrides and Start / Do the action / Arrive semantics. Dated relative/position events export their recorded date without needing a schedule.

Contract: buildTimelineJSExport(snapshot, schedule?: TimelinePresentationSchedule), exported types TimelinePresentationSchedule {defaultDate: string; events: Record<string, {date?:string; time?:string; meaning?: 'start'|'action'|'arrive'}>}. Absence means recorded-only behavior. Schedule date resolution: explicit nonempty override, otherwise recorded date, otherwise defaultDate. Time: explicit nonempty override, otherwise recorded time. A missing date filled, explicit date/time, or non-default meaning marks a presentation assumption; action is default. Explicit date/time values are presentation-only even if numerically equal to recorded values. No automatic durations, anchor date copying or before/after time arithmetic. Full valid Gregorian date required for schedule defaults/overrides; valid HH:mm[:ss] time; recorded partial dates remain partial unless overridden. Time override on partial date requires explicit complete presentation date. Invalid active schedule produces errors and zero projection events; ResearchTools backup remains available. Result adds errors:string[], scheduledCount:number, eventDetails:Record<unique_id,{dateLabel:string; placementLabel?:string; scheduled:boolean}> for plain-text accessible rendering. Existing event fields unchanged. HTML export escapes schedule/placement text. Relation/position described including anchor title, never silently enforced as chronology. Equal timestamps retain selected array order, verified against actual renderer. No invented minute spacing. Explicit clear disclosure warns chronology can differ and dates are assumptions. Controls opt-in (Use presentation schedule), default date local today on capture, per-event inputs accessible fieldset names and labels. Settings exist only while dialog open; reset on close/refresh, retain when returning from presentation. Stored revisions may be projected with temporary settings but saved snapshot is never rewritten. Both exports use same frozen original snapshot; backup explicitly excludes temporary presentation settings. No schema/API/persistence/public/vendor changes.

Ownership: tl00_recon native cooperative provider only src/lib/timeline-timelinejs.ts; owner TimelineJSExport.tsx, new TimelinePresentationSchedule.tsx and TimelineJSPreview.tsx, charter/roadmap/register/receipts. validator_review native cooperative tests only existing timeline-timelinejs-contract.spec.ts and timeline-renderer-browser.spec.ts (other test edits require owner). dependency_review independent source/runtime/artifact/visual read-only. Native editors do not commit or execute candidate code; owner commits after final quiescence. Qualified credential-free network-none pinned Docker validator only; no host package scripts/tests/install/dev server. External authenticated model lanes unqualified. Prohibited .team/ .env* deploy.sh wrangler.toml workers/ containers/.

Verification: trusted snapshot.py then container-run-renderer.py RUN node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium [--project=mobile-safari] tests/e2e/smoke/FILE. Guest cwd /candidate established by supervisor; focused schedule browser both projects first, then adapter/renderer/history contracts Chromium; complete renderer and TimelineJS browser + durable browser both projects, narrative/tool UI proportionate regression. One job at a time, one worker. Five typechecks + Vite/worker build + compiled release-schema gates via private build-command.json copied from prior accepted stage; read-only current production schema inputs. Independent exact-SHA review, representative desktop/mobile light/dark screenshot inspection and actual equal-date order, export immutability, dated relative eligibility, invalid inputs, retiming semantics, no unexpected network. Preserve failed runs.

Authority: user explicitly authorized clean non-force origin HEAD:refs/heads/main and gitlab HEAD:refs/heads/main and production researchtoolspy Pages deploy as stages finish. After exact-source acceptance, fresh refs unchanged, publish reviewed prebuilt artifact with trusted Wrangler --no-bundle, production read-only smoke and unchanged schema/migration/secret inventories. No production data mutation, migration or config changes. Separate source/publication/deployment/docs receipt states. Stop for divergent refs, unsafe validator, new API/schema scope or ownership conflicts.

Primary documentation: https://timeline.knightlab.com/docs/json-format.html requires start_date.year even with display_date; https://timeline.knightlab.com/docs/using-spreadsheets.html documents automatic chronological order and equal-date row order. Verify JSON ties in pinned renderer. No use of cosmological dates for ordinal sequences.
