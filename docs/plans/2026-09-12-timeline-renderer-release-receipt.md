# Self-hosted TimelineJS presentation release

[Timeline](https://researchtools.net/dashboard/tools/timeline) now provides **Open presentation** inside **Export TimelineJS**. The actual pinned TimelineJS 3.9.13 renderer displays the captured selected narrative, with beginning/latest start choices, keyboard navigation and a native accessible event list. Back, retry, close and changed-workspace handling dispose of the frame. Existing matching JSON downloads remain available.

The renderer loads only on explicit request and supports up to 100 dated selected events. Unknown/relative/position-only events remain listed as omissions in the export preview. Larger exports remain downloadable. Only the existing presentation projection enters the frame; the complete backup and evidence graph stay outside it. Chronological rendering does not rewrite narrative order, recorded date precision or assessments.

Pinned unmodified upstream JS/CSS and license notices are retained locally. The self-contained shell embeds the exact scripts under CSP hashes, removes the external font-face rule from its embedded CSS, and trims trailing CSS whitespace; standalone vendor files remain unchanged. Text uses system fonts and icons use an embedded font. The dependency manifest records verified npm integrity, source commit, MPL-2.0 license and per-file hashes, including the bundled DOMPurify notice. The iframe permits scripts without same-origin privileges; strict child policy blocks network connections, remote media and nested frames. Root policy changes only same-origin frame permission and embedded data fonts. No spreadsheet, CDN or third-party data transfer is required by the renderer. Cloudflare-added analytics/challenge scripts are excluded by the child CSP; this is verified against captured production HTML in isolated browsers. See [Knight Lab instantiation guidance](https://timeline.knightlab.com/docs/instantiate-a-timeline.html).

Accepted/deployed source `a11ef636a5fbde09d17cb58b19afb4f9047092ee` landed on both main refs without force. Deployment `971c8e13-acd6-4f1c-bc55-5829ac10c2af` serves [the accepted build](https://971c8e13.researchtoolspy.pages.dev); receipt/roadmap updates are a documentation-only follow-up.

The full suite at `2c65f486b718f68f55ec0a70941bd9d23f230bb7` passed five type checks, 161 API/contract checks and 104 desktop/mobile browser checks, builds and compiled schema rehearsal. The final source repairs only Rocket Loader script-tag handling and its generator/contract assertion; it separately passed six renderer contracts, twelve desktop/mobile checks, fresh frontend/worker builds and compiled schema rehearsal. Full-suite/type results are inherited from the unchanged baseline code, not claimed as rerun at the repair source. Captured production HTML and headers were also replayed in isolated Chromium and WebKit: rendering/navigation passed and injected edge scripts remained blocked. Focused tests use the real pinned renderer under shipped CSP and SAMEORIGIN headers, check isolated storage/parent access, local-only requests, chronological display, source preservation, start modes, failure/retry, stale disposal and selection bounds. Six contract cases verify provenance, bridge validation, bounds and options. Representative light/dark desktop/mobile captures passed independent review; limitations are recorded in the JSON receipt.

All 155 staged files were independently compared; inventory SHA256 `4552f19e7e8e53af1caa8d4eed95e3184ac133ae7066ff810ea32cbd5da39e2c`. Prebuilt publish used `--no-bundle`. Thirty read-only checks passed on each deployment and production URL, including eight raw renderer asset hashes, exact owned HTML bytes and root frame/font policy. HTML verification allows only identified CSP-blocked Cloudflare tail additions; a whole-response HTML hash is recorded for replay binding, not compared to local HTML. All 13 migration names and 386 catalog entries across 14 tables remain unchanged.

The [JSON receipt](./2026-09-12-timeline-renderer-release-receipt.json) records fingerprints, instruction/supervisor amendments and recovery. The full-run supervisor retains the same isolation and cleanup with a 1200-second bound; the preceding accepted suite had taken 879.53 seconds before renderer coverage. Previous deployment `d1a610f0-3876-4381-82ac-685730dfd32b` reads identical saved snapshots and retains JSON export but lacks the embedded presentation. Preserve data and immutable revisions. Primary checkout untouched; existing build chunk-size warning remains.

**TL-17 remains partial:** immutable authorized publication/revocation and richer revision/composite/plan/comparison snapshots remain pending. **TL-04 remains partial:** RSS coverage and broader source stores remain pending.

## Subsequent deployment

The [presentation navigation release](./2026-09-12-timeline-navigation-release-receipt.md) adds native list search and stable-ID jumping with the same snapshot format. This receipt retains historical renderer and edge-repair evidence; the newer receipt records current deployment.
