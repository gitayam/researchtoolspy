# Recorded intervals in presentations

Baseline: f70aef08c27454e44ad44dc6b928d8942a13bc13, freshly fetched origin/main and gitlab/main, clean integration worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911. Both refs equal; zero divergence. Primary checkout untouched.

Outcome: user-visible interval slides and spans in TimelineJS from Analyst Add/Edit and Export at /dashboard/tools/timeline, no flags or server account needed for local imports. Recorded interval events previously omitted now show both endpoints, accessible list labels and export JSON. TL-06/TL-17 remain partial; private v2 saving is next.

Contract: reuse recordedEnd and workspace v2 unchanged. TimelineJS optional end_date preserves only recorded calendar components, clock precision and escaped display_date. For overlapping precision where TimelineJS default-component comparison would put end before start, retain the slide and complete recorded range label but omit its geometric end_date with explicit notice; do not invent endpoint components or reverse them. Validate core claims. Equal dates supported. No duration or timezone inference. Interval events retain recorded endpoints under presentation scheduling; nonempty date/time or nondefault meaning overrides for them are rejected clearly. Schedule UI shows a read-only recorded range instead of ineffective controls. In automatic scheduling, a complete recorded interval end with an explicit clock is the next usable anchor; otherwise it does not anchor, and an explicit warning explains that subsequent undated steps still use previous usable timed anchor/default. Single-event legacy behavior unchanged. Complete backups and source snapshot stay identical. Private save rejection remains.

Renderer boundary: permit only optional end_date using existing strict date validation; reject malformed or geometrically reversed endpoints. Parent identity/nonce, sandbox, formatting whitelist, IDs and no-network policy unchanged. Regenerate inline shell from exact owned bridge and unchanged vendor/styles, update only corresponding CSP hash. Do not weaken security. Fresh edge-injected shell replay after publication required for script change.

Ownership: native cooperative provider tl00_recon owns src/lib/timeline-timelinejs.ts and src/components/timeline/TimelinePresentationSchedule.tsx only. validator_review owns tests/e2e/smoke/timeline-interval-contract.spec.ts and new tests/e2e/smoke/timeline-interval-presentation-browser.spec.ts only. Owner owns public/timelinejs/preview.js, assembled preview.html, renderer contract tests, documentation and private release helpers. dependency_review read-only independent source/runtime/artifact/visual acceptance. Shared worktree, no worker commits/execution; owner commits exact paths after quiescence. Providers are native because this is a bounded frontend/adapter feature. External model lanes remain unqualified.

Execution: all candidate tests/builds/scripts only pinned network-none credential-free Docker via established container-run-renderer.py, guest /workspace. No candidate host execution or auth helper sourcing. Read .team/project.json, CLAUDE.md, foundation charter. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/, dependencies and unrelated edits.

Acceptance: node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium tests/e2e/smoke/{timeline-timelinejs-contract,timeline-renderer-contract,timeline-interval-contract,timeline-temporal-contract}.spec.ts; browser both chromium/mobile-safari for timeline-interval-presentation-browser, timeline-interval-browser, timeline-renderer-browser, timeline-timelinejs-browser. Exact commands expanded in private plan before execution, deduplicated by case identity. Existing five tsc checks, Vite/worker build and compiled schema gate with fresh read-only production inventories. Exact-source independent review, representative light/dark mobile/desktop interval screenshots and 155-file baseline-independent package inventory (recompute actual count). Failed attempts preserved. Full unrelated backend suites not claimed.

Publication: prior explicit user authorization covers clean nonforce origin HEAD:refs/heads/main and gitlab HEAD:refs/heads/main, and trusted prebuilt --no-bundle researchtoolspy Pages main deployment after exact-source acceptance. Controller pilot publication remains plan-only; this is owner publication under user authority. No production content, schema, migration, secret, infrastructure or dependency changes. Read-only postflight schema/migrations/secretnames, 30 checks each URL and network-none replay of freshly captured edge shell. Record distinct source/ref/deployment states and limitations in receipt and update both roadmap documents. Stop on contract ambiguity, outside ownership, unsafe validator, divergence or failed acceptance; resolve within authorized scope.
