# Timeline evaluation input comparison

Baseline `f8455577d7725849c963da75a2c04855f5763549`, freshly equal origin/main and gitlab/main, clean shared integration `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911`. Prior evaluation runtime `639f2cab66b81b48f3069dfa0348021e6855b7ce` deployed as `5eaf67bd-31a6-440f-80d1-cb54e366b8ac`. No divergence or changed-on-both seams. Primary checkout untouched. User continuation and prior authorization cover non-force pushes to both main refs and reviewed prebuilt deployment.

User-visible TL-04 slice: explain changed-input evaluation notices with an optional read-only comparison of recorded versus current source inputs at `/dashboard/tools/timeline`, in both Analyst and Narrative evidence panels. Audience is existing guest/local or signed-in private timeline analysts, no flags. Synthetic evaluated assertion fixture is the demo. “Compare evaluation inputs” opens the comparison; record labels and dates always qualify the snapshots as analyst-entered. No new schema, API, database, auth, evaluations or automatic review decisions. TL-04 remains partial.

Frozen seam: new `TimelineSourceEvaluationInputs({evidence,assertion})` React component uses existing strict evaluation validator and `timelineSourceEvaluationBasis` before parsing either basis. Recorded input is assertion.evaluation.basis, current input is freshly computed; never replace one with the other. Display assertion ID/wording, temporal wording, quote, locator, status, source ID, observed/reported clocks and derivesFrom IDs for the full union of selected/ancestor assertions; display full union of associated source ID/URL/title/publisher/published/retrieved fields. Stable ID matching; do not align distinct records by title or URL. Mark added/removed/changed/unchanged with text as well as color. Unrecorded optional fields remain visibly missing, not invented dates. Sort by ID, compare derivesFrom as sets. Recorded and current panels labeled “Recorded inputs” and “Current inputs”; comparisons do not imply objective truth, authenticated provenance or chronological reconstruction. Missing current inputs or invalid stored inputs show an explicit unavailable message without substituting data. React text rendering only; links optional and only valid HTTP(S), no fetch. Default-collapsed native details; accessible keyboard navigation, wrapped text/mobile/dark styles. Opening/closing never calls onSave or changes JSON, private state or draft bytes.

Ownership: cooperative native tl00_recon owns ONLY new src/components/timeline/TimelineSourceEvaluationInputs.tsx. Owner owns integration in TimelineSourceEvaluation.tsx, browser fixture assertions/captures, docs/API description and exact-file commits. Independent dependency_review checks cumulative exact source/runtime/artifact; validator_review checks visual/docs. Shared disjoint editing, owner reserved; no worker commits or candidate execution, network, credentials or production actions. Immutable private prompt/checksums recorded before dispatch. External authenticated worker lanes remain unqualified.

Verification: all candidate execution only in pinned credential-free network-none Docker from /workspace. Focused command `node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium --project=mobile-safari timeline-durable-browser.spec.ts --grep 'source evaluations retain'` extends real-D1 save/reopen flow to compare old/new publisher and claim, unchanged stored evaluation/export bytes, keyboard disclosure and Narrative behavior. Add ancestry/added/removed/missing-date fixture coverage as needed. Screenshot-only hide fixed chrome and descendants for tall component captures; retain honest visual limits, no global app UI changes. Full `node scripts/verify-timeline.mjs` (five types,134 API/contract and browser checks, Vite), pinned Wrangler compiled worker and `node scripts/verify-timeline-release.mjs` actual production-schema rehearsal. Independent exact-file artifact hashes, non-force refs, readonly live checks and unchanged catalog/migrations/config/secret-name inventory. Preserve failed attempts, distinct source/doc/deploy states and rollback compatibility: previous evaluation runtime remains data-compatible. Stop on contract/ownership crossing, remote divergence or missing isolation/review gates.
