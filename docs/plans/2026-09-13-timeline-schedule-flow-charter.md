# Timeline automatic presentation schedule

Baseline: 36dad545ac581c04063ba43ae492ce814e543f9c, freshly fetched equal clean integration HEAD, origin/main and gitlab/main. Shared integration /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; preserve primary and unrelated cwd. Prior runtime 53eefe8a516df8702b5bfb9639ba05b54b82b3eb, deployment bbe5b500-a461-4e93-b230-5dfe6b9f94d2. User-visible outcome at /dashboard/tools/timeline > Export TimelineJS > Use presentation schedule: enter one time and following automatic events adjust, with compact effective-time controls and Today/Tomorrow shortcuts.

Contract: optional automatic:{startTime:string,intervalMinutes:number} extends TimelinePresentationSchedule; absence preserves prior independent override behavior. UI initializes automatic 09:00, 15 minutes, local today; presentation remains opt-in. Pure resolveTimelinePresentationSchedule(snapshot,schedule) returns {events:Record<eventId,{date?:string,time?:string,source:'automatic'|'override'|'recorded'|'default',scheduled:boolean}>,errors:string[],warnings:string[]}. Resolve selected narrative order only. Undated events without overrides start at defaultDate/startTime, then follow the preceding usable full date/time plus interval. A date or time override replaces that part of the automatic candidate; explicit overrides seed following automatic events. Recorded dates retain recorded precision/time unless explicitly overridden; full recorded date/time is an anchor. Recorded dates without a clock do not acquire invented precision or anchor a clock. A prior usable timed anchor continues past an untimed recorded event; disclose this. Undated recorded clocks are retained as anchors on the candidate presentation date. Date-only overrides use candidate clock for undated events, recorded clock for dated events. Manual/recorded anchors may precede a previous timestamp: retain them and warn clearly that TimelineJS reorders; never move recorded facts silently. Before/after relationships remain descriptive; automatic spacing follows narrative selection order, not inferred travel duration or constraint solving. Global interval integer 1..1440 minutes; valid HH:mm[:ss], Gregorian years 0001..9999. Calendar wall-clock arithmetic independent of host timezone/DST (no timezone conversion), midnight/leap/year rollover and overflow fail closed. Invalid active schedule returns no projection; backup remains original. Retain current optional per-event date/time/meaning and reset behavior. Explicit clearing/reset removes override and reconnects automatic flow. UI shows actual effective values and Automatic/Custom/Recorded distinction; minute precision native time picker, date editing behind Change date, Today/Tomorrow shortcuts. No new dependency, persistence, API, schema, public renderer or security changes.

Ownership and execution: native cooperative shared editors, no commits or candidate execution. tl00_recon owns only src/lib/timeline-timelinejs.ts (resolver/adapter); validator_review owns only tests/e2e/smoke/timeline-timelinejs-contract.spec.ts and timeline-renderer-browser.spec.ts (contract/device proof). Owner owns TimelinePresentationSchedule.tsx, TimelineJSExport.tsx, docs and private trusted release helpers. dependency_review independent read-only source/runtime/artifact/visual review. Provider before consumer, owner commits exact paths only after final quiescence. Prohibited .team/ .env* deploy.sh wrangler.toml workers/ containers/. External model lanes unqualified. All candidate execution pinned credential-free network-none Docker validator; host only trusted read/edit/Git and reviewed helper/data processing/pinned prebuilt publisher.

Verification commands: trusted snapshot.py then container-run-renderer.py RUN node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium [--project=mobile-safari] tests/e2e/smoke/FILE, guest cwd /workspace. Focus new schedule browser both devices first, then full renderer and TimelineJS browser both devices; renderer, TimelineJS adapter and history contracts Chromium. Five existing typechecks, Vite/worker build and compiled release-schema gates using private build-command.json and fresh read-only production schema. One job at a time. Test propagation after edits/reset, manual anchors, mixed recorded dates, midnight/leap rollover, invalid inputs, frozen backup, actual chronological renderer, keyboard labels and mobile/light/dark full control bounds. No full unchanged backend suite required beyond compiled schema gates. Independent exact-source review and artifact inventory; public tree must equal baseline. Preserve failed runs.

Authority: enduring explicit user authorization for non-force origin HEAD:refs/heads/main and gitlab HEAD:refs/heads/main and production researchtoolspy Pages deployment. Fresh equal refs and clean reviewed source before push; trusted prebuilt --no-bundle publish only after exact-source runtime/artifact/visual acceptance. Postflight read-only live smoke both deployment and custom domain; unchanged schema/migration/secret inventories. Separate runtime, publication, deployment and docs-only receipt states. Stop for divergent refs, unsafe validator, API/schema scope expansion or ownership conflict. Controller pilot publication remains plan-only; this uses separately user-authorized established publisher.
