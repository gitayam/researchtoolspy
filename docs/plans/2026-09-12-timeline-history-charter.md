# Saved revision history

Baseline 0687285d25fd1dafad97e6a29707ad74c10fa2a3 freshly equals clean integration HEAD, origin/main and gitlab/main. Worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; primary untouched. User authorization covers non-force origin/main and gitlab/main pushes and reviewed prebuilt production deployment. Previous runtime13c2f284bb0e96d6a3f5a90114a2df715a0f2f0b deployment928c14a5-d9e7-4621-aa2f-76b7ac04fee7.

Outcome: /dashboard/tools/timeline private workspace saving offers saved revision history after successful save/open. Explicit Load revision history, newest-first revision number/time/ID, Load older revisions (20 per page, maximum100 loaded), Refresh history, and Inspect revision. Pinned history pagination never silently incorporates newer saves; refresh deliberately starts again. Selected verified historical snapshot has Preview selected revision with existing renderer/exports; provenance says selected historical revision, unsaved edits excluded. No open-workspace replacement, save-head change, auto-network, public sharing, server changes or migration. Memory-only and cleared on artifact/workspace/account/eligibility change. TL-17 remains partial.

Contracts: add exported TimelineRevisionSummary {revisionId,sequence,parentRevisionIds,objectCount,changeCount,contentHash,createdBy,createdAt}, TimelineRevisionPage {headRevisionId,revisions,nextCursor}; loadTimelineRevisions(artifactId,identity,signal,previous?:TimelineRevisionPage) uses existing GET /revisions?limit=20&cursor=...; previous last sequence bounds nextpage and head must match. Validate strict wire keys, IDs, safe counts, canonical UTC timestamps, SHA256, descending sequence, uniqueness, ETag head, opaque bounded cursor and its decoded artifact/head/before binding. Firstpage first revision is head; subsequentpage everysequence below previouslast. Reject malformed, repeated, mismatched and oversized replies. openTimelineRevision(artifactId,revision,identity,signal) returns validated TimelineWorkspaceExport from existing revision-pinned /objects, using shared existing object/payload/manifest hash checks and single browser-workspace object invariant. No current-head read or mutation needed.

Topology: cooperative native provider tl00_recon owns ONLY src/lib/timeline-durable.ts. Owner owns new TimelineRevisionHistory.tsx, TimelineDurablePanel.tsx seam and optional TimelineJSExport history label. validator_review owns only tests/e2e/smoke/timeline-durable-browser.spec.ts additions/bridge and new tests/e2e/smoke/timeline-history-contract.spec.ts; owner registers contract suite. dependency_review independent read-only source/runtime/artifact and visual review. Owner alone commits after final provider quiescence acknowledgment; all candidate scripts/tests/builds only pinned credential-free network-none Docker validator. No prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/ edits. External authenticated lanes unqualified.

Verification: focused history contract and actual-D1 history browser cases both Chromium/mobile Safari; all durable browser cases bothprojects; existing renderer/export contract+browser regression; five typechecks and Vite/worker build + scripts/verify-timeline-release.mjs against fresh read-only actual production schema. Browser tests preserve unsaved edits/save If-Match after historical inspection, page pinning/refresh, zero writes, malformed/hash mismatches, delayed response after scope/signout and failure clears selected history. Screenshot card centered, trial-click and honest viewport mobile/desktop, dialogs. No full unrelated API-suite claim. Independent staged155-or-current file comparison, reviewed source main pushes, prebuilt --no-bundle deploy,30live checks perURL, unchanged schema/migration/secret-name inventories, durable receipt/docs audit then docs sync.

Stop for divergence, unsafe execution, ownership/schema/API widening or undecided privacy seam. Existing renderer public bytes and headers unchanged; no fresh edge replay required if verified unchanged. No production user-data operations in tests.
