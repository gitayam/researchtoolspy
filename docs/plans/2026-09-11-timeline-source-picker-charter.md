# Recent stored source picker

Baseline: `52b96353586888d4bc1b3ed08f6986e38d2b119e`, freshly fetched equal origin/main and gitlab/main. Clean shared integration worktree `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911`; preserve primary checkout untouched. User authorized continued implementation, non-force pushes to both main refs and deployment of accepted stages. Project Git policy and CLAUDE apply. Candidate execution only in the established credential-free network-none validator; authenticated external workers remain unqualified.

Outcome/delivery: user-visible partial TL-04 improvement. Active human analysts in an opened writable private timeline can explicitly load up to twenty recent owned complete Content Research records and select an analysis ID by displayed title. Existing exact-quote preview and recheck-before-import remain authoritative; listing is not extraction integrity verification or source truth. Manual ID entry remains available. No source-text preview, pagination, search, new source store, evidence wire change, migration, model/source fetch, credentials or cross-app work.

Contract: `GET /api/timeline-source-candidates?workspaceId=<id>` accepts exactly one workspaceId and no other query parameters; optional X-Workspace-ID must match. Shared existing-human auth rejects guests/services without provisioning. A single final database SELECT rechecks active human/private nondefault workspace/write access and restricts records to that user's ownership in that workspace. Inaccessible workspace returns uniform 404; accessible empty workspace returns 200 with empty items. Only positive safe integer IDs, processing_status=complete and canonical retained (saved=1/null expiry) or future unsaved (saved=0/null plus parseable future expiry) records qualify. Order by ID descending, LIMIT 20 before returning rows. This means recent stored IDs, not a claimed publication or ingestion clock.

Return exact `{schemaVersion:'timeline-source-candidates.v1',workspaceId,items:[{analysisId,title}]}` with no source text, URL, hash, count or timing metadata. SQL projects only a title prefix of at most 200 SQLite codepoints; sanitize control characters to spaces and reject malformed Unicode, use `Stored analysis <id>` for missing/blank title. Titles max400 UTF-16 units, <=20 unique descending items, whole JSON <=65536 bytes. No content_chunks reads and no content mutations. Standard no-store artifact JSON/error handling and GET/OPTIONS CORS. Invalid query400, inaccessible404, existing auth401/403. Database failure503.

UI: add separate controlled picker inside existing import form. Explicit Load recent stored sources button, title/ID selector buttons, empty/error/status states and truthful up-to20/newest-ID wording. Selection invalidates old passage preview and clears quote/assertion text; listing never imports content. No local/session storage persistence. Bound streaming JSON<=65536 and exact response schema/items/order/workspace. Capture identity/credentials/context, abort or ignore superseded/unmounted/sign-out/workspace/event changes and clear results on storage events. Disable selection during passage resolution; avoid late picker overwriting edited ID. UI passes a selected ID into existing form, retaining manual entry.

Ownership: backend provider tl00_recon owns new functions/api/timeline-source-candidates.ts and tests/e2e/smoke/timeline-source-candidates-d1.spec.ts only. No execution or commits. Owner owns picker component/import integration, browser/compiled fixtures, verifier, docs and exact-file commits. dependency_review independently reviews contracts, exact source and staged artifacts. Native workers are cooperative shared-filesystem editors, not isolated branches. Immutable prompt packets/checksums recorded privately before dispatch.

Acceptance: actual-D1 tests for ownership/auth/private/write/empty/order/limit/retention/title bounds and final-query revocation race, unchanged data and zero source text disclosure. Browser desktop/mobile selects a recent title into the existing exact-quote flow and saves/reopens; delayed list after sign-out cannot restore private metadata. Malformed/oversized/stale replies fail closed. Existing chunk/short import tests stay valid. Full `node scripts/verify-timeline.mjs`, Pages Functions build with pinned Wrangler, and `node scripts/verify-timeline-release.mjs` run in qualified Docker on explicit refreshed committed snapshot. Compiled worker exercises actual production schema with synthetic candidates; compare same14table catalogs/13migrations. Publish only exact-SHA reviewed identical staged build using --no-bundle, readonly live checks, docs-only follow-up, both clean main pushes. Preserve failed receipts and distinguish runtime/source/doc SHA. TL-04 stays partial.
