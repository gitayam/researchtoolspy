# TimelineJS selected-narrative export

Baseline b54417446ca642b55351b82261e0720b1b11f10e, freshly equal clean integration HEAD, origin/main and gitlab/main. Worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; primary checkout preserved. No divergent commits or changed-on-both seams. User authorization covers clean non-force pushes to both main refs and reviewed prebuilt production deployment. Prior production runtime4ba13bdeedbff7686f8b021604a5be85e4019837 deployment58710a88-8e09-4c96-9aed-e7233a6d2d32.

User-visible TL-17 partial slice: Export TimelineJS on /dashboard/tools/timeline in guest/local and private workspaces, both views, no flag. Preview a frozen selected narrative, show excluded events and presentation losses, download TimelineJS JSON and matching complete ResearchTools companion. No renderer, public publishing endpoint, new revision store, spreadsheet, third-party requests, dependencies, schema/API/auth/migration changes. TL-17 remains partial.

Frozen provider contract: NEW src/lib/timeline-timelinejs.ts exports buildTimelineJSExport(snapshot: TimelineWorkspaceExport): {timeline: {title: {text: {headline: string; text: string}; unique_id: string; autolink: false}; events: Array<{start_date: {year:number; month?:number; day?:number; hour?:number; minute?:number; second?:number}; text: {headline:string; text:string}; unique_id:string; display_date:string; group?:string; autolink:false}>; scale:'human'}; selectedCount:number; omitted:Array<{eventId:string; title:string; reason:string}>; notices:string[]}. Pure, validated snapshot input, never mutate it. Select with existing narrativeTimelineEvents. Only recorded absolute dates eligible (absent placement or absolute); omit unknown/relative/position events even if a date exists and explicitly explain. Preserve year/month/day precision without invented date parts; recorded times only with full day precision, otherwise omit time with notice. No timezone conversion; wallclock is accompanied by explicit narrative timezone in display label and notice when times present. Stable safe unique_id event- plus encodeURIComponent(event.id), title unique_id narrative-title. Keep narrative input order in JSON, unconditional notice TimelineJS sorts chronologically and narrative sequence/transitions may change. Chapter group labels escaped title plus stable chapter ID to distinguish duplicate titles. No fabricated eras/media. Escape all human text for HTML including group/displaydate; autolink false. Include description, why-it-matters and qualified timelineAssessmentLabel in event text; omit transition because chronology may differ. Title includes narrative title/framing only. Notices explain companion retains complete workspace/unselected events/evidence/review history/questions and chapter claims/narrative metadata absent from presentation. Adapter is deliberately not round-trip.

Cooperative native tl00_recon owns ONLY new adapter and NEW tests/e2e/smoke/timeline-timelinejs-contract.spec.ts; target six meaningful cases across mapping, precision/time, omitted placements, selection/stable IDs/duplicate chapters, injection, immutability/qualified assessments. Integration owner owns NEW TimelineJSExport.tsx, TimelineResults integration, NEW browser suite, verifier registration and docs. Shared filesystem, no worker commits, candidate execution, network, credentials or other edits. Immutable prompt/hash and instruction hashes before dispatch. Independent native source/runtime/artifact and visual/receipt reviews. External authenticated worker lanes remain unqualified.

Preview snapshot captures current source and complete workspace at open; both downloads consume that same snapshot. Modal blocks background editing; a changed input signature disables both downloads and requests fresh preview. Opaque light/dark dialog, readable counts and omission reasons, keyboard/mobile support. Existing Export JSON remains unchanged in meaning. No persistence mutation on preview/download; snapshot may include unsaved local edits, explicitly labeled. ResearchTools companion includes all records, explicitly labeled.

Credential-free pinned network-none Docker /workspace: focused adapter contracts, desktop/mobile browser download-byte checks (matching companion, omitted unknown event, escaping, source unchanged, disabled empty export, stale snapshot guard), light/dark screenshots. Full node scripts/verify-timeline.mjs, pinned Wrangler worker build, node scripts/verify-timeline-release.mjs against readonly production schema. Exact joined source review, artifact inventory hash match, readonly live checks, unchanged13 migrations/14 catalog tables required. Stop for divergence, ownership crossing, missing isolation/review gate; preserve failed attempts. Official format reference https://timeline.knightlab.com/docs/json-format.html verified via official indexed documentation on2026-09-12; no vendored renderer/schema claim.
