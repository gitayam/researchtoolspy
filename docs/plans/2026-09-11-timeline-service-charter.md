# Timeline scoped service tranche

Baseline: `d33b9bac9b2aaaf85b008f7c3bb6a29ce1fbe064`, freshly fetched equal origin/main and gitlab/main. Integration: `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911`. Dirty primary remains untouched. User authorizes implementation, non-force main pushes to both remotes and deployment as stages finish; this supersedes earlier tranche publication exclusions.

Outcome: existing credential lifecycle admits exact `timeline.read` and `timeline.write` scopes for the existing durable routes. Read does not grant write; write does not grant GET. Stateless extraction remains community.research.execute. No new credentials are provisioned, rotated or widened in production. Existing service principal owns exactly its private TEAM workspace and active intake investigation; no human membership or guest fallback. No public timelines, branching, external-product changes or new provisioning UI.

Contract: existing human routes and payloads remain compatible. Reserved service bearer namespace uses existing HMAC authentication, environment/audience/current-or-next token validity and bindings, integrations-enabled gate, then exact operation scope. All errors on timeline routes retain timeline-artifact-error.v1. Service failures never fall through to human credentials. Reads hide inaccessible artifact existence. Replays revalidate current exact token and scope before returning stored success; stable service principal user ID keeps legitimate rotation retry identity. Every service mutation batch begins with an unconditional SQL assertion of the exact presented token ID/hash/client/environment, database-time validity, scope and every current service binding invariant. An SQL error aborts the entire batch. Revision trigger continues human authorization and gains a live service binding/write-token branch; direct SQL is privileged and request-specific authorization is enforced by the batch assertion. No token digest or secret is retained in timeline history or responses.

Migration 0013 widens only the existing token scope CHECK and adjusts the revision authorization trigger. Preserve all existing scope rows, foreign keys, indexes, triggers and history; whole migration plus tracker runs atomically. Never edit applied0009–0012. New scopes must not be provisioned before compatible application deployment; older applications reject unknown token scopes, so rollback requires compatible code or separately authorized token remediation, never implicit scope removal.

Discovery: additive optional `timelineRead`/`timelineWrite` capabilities, `timelineArtifact: timeline-artifact.v1` contract version when either available, actual durable limits only when available. Omit unavailable extensions for strict older clients. Existing persistentWorkspace/artifactRead meanings remain unchanged. Runtime requires D1, enabled integrations and valid scoped principal, independently of model key.

Native cooperative ownership: backend provider owns timeline-artifact-auth.ts, timeline-artifact-store.ts, timeline-artifact-contract.ts, new0013 migration and new timeline-service-d1.spec.ts. Discovery provider owns integration-contract.ts, integrations/capabilities.ts and their two community integration test files. Owner owns charter, validation/release scripts, remaining integration fixes and docs. Independent reviewer read-only. Providers do not commit, execute candidate code on host or access production. External authenticated model lanes remain unqualified.

Acceptance: credential-free no-network pinned Playwright Docker validator only. Full scripts/verify-timeline.mjs including new actualD1 service matrix; all five type checks, browser Chromium/mobileSafari, Vite build; Pages Functions build and guarded production-schema rehearsal. Test read/write separation, both workspaces, disabled flag, malformed/revoked/expired tokens, no human fallback, exact-token rotation/replacement, current-scope replay, transaction-time revocation/scope/binding races, rollback, old populated scope/history preservation and migration failure rollback. Review exact joined SHA before main publication. Publisher uploads prebuilt bytes only; fresh backup and schema/inventory before migration, then readonly production smoke. Persist redacted receipts with exact source/runtime and distinct Git/migration/deployment states.
