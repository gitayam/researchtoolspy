# Bounded chunked extraction import

Baseline: `463d2976538afef6179b30f477eb4c771d465153`, freshly equal origin/main and gitlab/main. Integration `/Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911` is clean; primary checkout remains untouched. User authorized stage implementation, clean non-force pushes to both main branches and deployment after review. Existing `.team/project.json`, CLAUDE and team-orchestrator instructions apply; candidate execution only in the established credential-free network-none Docker validator. External authenticated worker lanes remain unqualified.

Outcome: the existing stored-passage import UI accepts complete larger Content Research extractions reconstructed from their stored chunks, while rejecting partial or inconsistent chunk sets. This is a user-visible partial TL-04 extension, not a new source store, migration, source fetch, model call, credential, authenticated provenance claim or review service.

Keep the request/response wire shape, deterministic source/passage IDs, original whole-content hash, exact unique quote, current human/owner/private-workspace/write authorization, retention rules and memory-only opened-private UI unchanged. Complete short records still use their exact stored text and ignore unrelated chunks. Larger records must have the writer's exact suffix `\n\n[Content truncated - see content_chunks table for full text]` after exactly 102400 UTF-16 units. Reconstruct only 3–8 chunks, indexes 0 through n-1, each nonfinal chunk exactly 51200 UTF-16 units and final chunk 1–51200. `chunk_size` must equal JS string length (the existing writer's units), each stored lowercase SHA256 must match its chunk's UTF-8 encoding, and the assembled well-formed Unicode text must match the parent whole-content SHA256. The first 102400 units must equal the parent prefix exactly. Reject malformed Unicode, including legacy surrogate-split chunks that cannot reconstruct the original hash. No partial fallback or silent truncation.

Bound parent text and chunk retrieval before materialization: parent text no more than 102500 UTF-16 units/410000 UTF-8 bytes, at most 8 chunk rows, each at most 51200 units, assembled no more than 409600 UTF-16 units and 512000 UTF-8 bytes; JSON aggregation, if used, at most 1048576 bytes. Reject over-bounds with 413 and malformed/incomplete/hash/prefix mismatch with 400. Use one final authorized SELECT containing a bounded ordered chunk projection, so parent metadata and chunks are read in one SQLite snapshot under the final authority predicate. Never retrieve an unbounded chunk set or read chunks after a separately authorized metadata snapshot. Exact quote offsets may extend to 409600; the browser keeps all other response checks and re-resolves expectedContentHash before applying.

Ownership: native cooperative backend provider `tl00_recon` owns `functions/api/timeline-source-import.ts`, `tests/e2e/smoke/timeline-source-import-d1.spec.ts`, and a new `tests/e2e/smoke/timeline-chunk-import-d1.spec.ts`. No commits or execution. Owner owns browser fixtures, frontend response bound/help text, release verification, docs and integration commits. Independent `dependency_review` reviews exact joined SHA. Provider must stop before changing protocol/ownership or source trust semantics. Prompt amendments and hashes are recorded privately; no credentials or source-bearing logs in public receipts.

Acceptance: actual D1 tests cover valid chunk reconstruction, cross-boundary quote, missing/duplicate/out-of-order/extra chunks, per-chunk/parent hash mismatch, wrong size/prefix, Unicode split behavior, all byte/row limits and final authority change; existing source-import tests remain valid. Browser acceptance imports a passage beyond the old 102400 offset, saves/reopens it and preserves browser draft bytes. Compiled-worker rehearsal uses the actual production schema and synthetic chunks, comparing content_chunks plus the previous 13 source/timeline/credential table catalogs before and after. No production content writes. Full command: `node scripts/verify-timeline.mjs`; compile Pages Functions with pinned Wrangler and run `node scripts/verify-timeline-release.mjs`. Owner refreshes `snapshot.py` and verifies exact SHA/source before every Docker invocation. Publish only independently accepted identical build files; then record deployed/runtime SHA separately from documentation-only follow-up and keep TL-04 partial.
