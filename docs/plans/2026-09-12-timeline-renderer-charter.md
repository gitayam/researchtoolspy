# Self-hosted TimelineJS presentation

Baseline d875ea49699963dd1a6f6cfc7e7e14fe8227a5a9 freshly equal clean integration HEAD, origin/main and gitlab/main. Worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911, primary preserved. No divergence or changed-on-both seams. User authorization covers non-force pushes to both main refs and reviewed prebuilt production deployment. Prior runtime86202310b3f9e4b0a95487a88850b14d822346c3 deploymentd1a610f0-3876-4381-82ac-685730dfd32b.

User-visible optional TL-17 slice: from existing Export TimelineJS preview on /dashboard/tools/timeline, Open presentation displays actual pinned self-hosted TimelineJS3. Guest/local and private workspaces, explicit action, no flags. Same frozen selected-narrative projection, loss warnings, stale guard and matching companion. Beginning/latest opening option and a native accessible event list; return to export details without changing saved data. No public publication/revocation endpoint, new narrative revision/composite/plan types, API/auth/migration changes or third-party transfers. TL-17 remains partial.

Dependency: official @knight-lab/timelinejs3.9.13 npm artifact, upstream gitHead bdc928fe8cbf621575c8e335b197aadb0cfe1526, MPL-2.0, bundled DOMPurify3.4.12 notice retained. Verify registry sha512, vendor unmodified dist JS/CSS and license notices with committed per-file hashes/provenance. Own icon font served as data URL in separate override CSS, system text fonts; no CDN, Google font, spreadsheet, media or analytics fetch. No dependency install/build on host. Candidate runtime executes only pinned credential-free network-none validator. Optional renderer resources load only after explicit open, cap at100 eligible events with a clear notice; JSON exports/native Narrative stay available.

Frozen provider ownership ONLY new public/timelinejs/preview.html, preview.js, preview.css and tests/e2e/smoke/timeline-renderer-contract.spec.ts. Owner supplies public/timelinejs/preview-font.css; vendor at public/vendor/timelinejs/3.9.13/{timeline.js,timeline.css,LICENSE,timeline.js.LICENSE.txt,manifest.json}. Provider may read private upstream Timeline.js and upstream.tgz under /Users/sac/AgentWorkspaces/researchtoolspy-timeline-renderer-20260912; no execution/commits/network/credentials/other edits. Six contract tests for artifact hash/provenance, sandboxed shell CSP, bridge/whitelisted projection, local assets/font and pinned options. Owner owns React TimelineJSPreview.tsx, TimelineJSExport integration, browser tests, public/_headers minimal frame-src self change, verifier registration and docs. Independent source/runtime/artifact and visual/receipt reviewers. Immutable prompt and instruction hashes before dispatch, cooperative shared filesystem. External authenticated provider lanes unqualified.

Frame contract: parent renders iframe src /timelinejs/preview.html#<randomUUID>, sandbox=allow-scripts ONLY, referrerPolicy=no-referrer, title=TimelineJS narrative presentation. Opaque origin, no storage/parentDOM access, no popups/navigation/forms/download permissions. Child meta CSP default-src none; script-src self; style-src self unsafe-inline; font-src data:; img-src data:; connect-src none; frame-src none; object-src none; base-uri none; form-action none. Native parent policy only changes frame-src none to self; no remote frame allowlist. Classic external JS/CSS load locally, no inline JS or unsafe-eval. Wrapper loads vendor CSS then preview-font.css and preview.css, vendor JS then preview.js defer.

Message protocol: child posts {type:'timelinejs:ready',nonce:location.hash.slice(1)} to parent '*'; parent checks exact iframe.contentWindow, origin 'null' and nonce. Parent then posts {type:'timelinejs:render',nonce,timeline:<adapter timeline ONLY>,theme:'light'|'dark',startAtEnd:boolean} to that iframe '*'. Child accepts only parent source, matching nonce, one valid render message, nonempty <=100 events. Validate and copy only adapter fields title/events/scale human; reject URL strings/media/background/unexpected structural fields and invalid date bounds, unique IDs and text types. Child emits {type:'timelinejs:loaded'|'timelinejs:error',nonce} with no data/error text. Timeline instance uses local script_path, font:null, theme:null, language:'en', analytics IDs null, track_events:[], hash_bookmark:false, soundcite:false, start_at_end flag, duration0 for reduced motion. Readonly data clone; no companion/evidence graph passed. Parent loading/error timeout and explicit retry; unmount destroys whole frame on close/back/stale/theme/start option changes and removes message listeners/timer. Direct shell without parent remains explanatory. HTML content is escaped by existing adapter and sanitized by pinned upstream runtime; no inferred media URLs.

Acceptance in isolated /workspace: main type check, new six contract tests including source provenance/CSP declarations, actual pinned renderer Chromium/mobileSafari browser cases: chronological slides/year-month-day labels/stable IDs/chapter groups, beginning/latest, keyboard nav and native fallback, malicious text not executable, sandbox storage/parent isolation, zero nonlocal requests during renderer open, no lazy assets before open, failure/retry, empty/>100 disabled, stale state unmount, source/companion unchanged; representative light/dark desktop/mobile captures. Browser tests apply actual shipped root CSP to route response (Vite does not apply Pages _headers) to prove frame policy. Full five typechecks/all API/browser suites/Vite/pinned Wrangler worker build/compiled production-schema rehearsal, artifact hash match and readonly live assets/CSP/schema checks. Preserve failures; stop on divergence, missing isolation/review or ownership crossing. No complete accessibility/conformance or future publication claim.
