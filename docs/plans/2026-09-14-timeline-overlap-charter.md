# Find overlapping recorded dates

Baseline: 30edf633a51a54c87f40d12f9f2a7e62ab5d6be9, clean integration and freshly fetched origin/main + gitlab/main equal. Integration /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911; dirty primary remains untouched.

Outcome: TL-06 client-only overlap finder in Analyst timeline. Choose a current event and find other events whose recorded calendar windows could overlap it; distinguish separate ranges and dates that cannot be compared. Links focus existing event cards. Complete sequence, workspace JSON, current save state and narrative/presentation selections stay unchanged. No schema, migration, API, permission, dependency or renderer changes.

Semantics: reuse existing recordedBounds from timeline-timing.ts (owner exports existing private function without changing its behavior). Conservative half-open calendar extents preserve year/month/day/minute/second precision and inclusive recorded interval endpoint units. Intersecting outer extents justify only potential overlap, never confirmed concurrency, causality or evidence agreement. Adjacent nonintersecting units are separate. Do not infer times from placement, sequence, schedules or narrative timezone; timezone/DST mappings remain unresolved. A unique valid anchor is required; unknown/invalid anchor returns no comparison rows with a clear reason. Other missing/invalid dates or ambiguous IDs remain unresolved. Exclude only the uniquely selected anchor, retain input order, do not mutate inputs, compare one anchor in linear time.

Pure interface frozen: reviewTimelineOverlaps(events: readonly TimelineWorkspaceEvent[], anchorEventId: string) returns { anchorStatus: 'ready'|'missing-anchor'|'ambiguous-anchor'|'missing-date'|'invalid-date', rows: TimelineOverlapRow[] }. TimelineOverlapRow = { eventId: string, status: 'possible'|'disjoint'|'unresolved', reason: 'overlap'|'separate'|'missing-date'|'invalid-date'|'ambiguous-event' }. Invalid anchor => rows[]. Duplicate/blank candidate IDs => unresolved ambiguous-event. No definite-overlap output in this conservative slice.

UI: TimelineOverlapReview({events}) appears beside Timing review, collapsed by default, title Find overlapping dates. Explicit anchor select (no implicit first match), recorded label, counts and filter for Potential overlaps / Cannot compare / Separate dates / All other events; default potential. Explain partial dates indicate possible overlap only and recorded clocks are not timezone-normalized. Clear query control. Bounded20 rows with explicit Show more. Never link ambiguous IDs; valid links focus/scroll stable timelineEventAnchor. Invalid/deleted selection clear or unresolved with no stale results. Existing parent requestId remount resets query on import/workspace change. Long titles/endpoints wrap,44px controls, mobile/light/dark readability. Accessible labels and status summary.

Ownership: tl00_recon owns new src/lib/timeline-overlap.ts and tests/e2e/smoke/timeline-overlap-contract.spec.ts. validator_review owns new src/components/timeline/TimelineOverlapReview.tsx and tests/e2e/smoke/timeline-overlap-browser.spec.ts. Owner owns timeline-timing.ts export-only seam, TimelineResults.tsx mount/import, private validation/release helpers and docs. Native agents are cooperative editors sharing worktree, not isolated branches; no candidate execution, installs, Git or production actions by providers. Owner alone commits. Crossreview after providers quiescent, no self-acceptance.

Verification: exact committed SHA snapshot via established trusted snapshot.py; pinned Docker supervisor951e88d8b3c2c934de367407ce3269bbdebfcebf5a731a64f10a630366d52639 and image mcr.microsoft.com/playwright@sha256:9bd26ad900bb5e0f4dee75839e957a89ae89c2b7ab1e76050e559790e946b948, networknone, readonlysource/deps, noauth/socket, capdropALL/no-new-privileges/stopped inspection. All candidate tests/builds/scripts only there. Focus new overlap/temporal/timing/interval contracts; newbrowser + timing/interval browser, durable interval save/history and sharing regressions both Chromium/mobileSafari. Use node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts explicitfiles/projects --trace=off --retries=0, serial partitions. Five TypeScript checks, Vite/Pages build and existing compiled actual-production-schema gates in already-applied0015 mode. Freeze exact command inventory at candidate. New screenshot captures must be fully visible and settled before acceptance. Browser fixtures cover partial/interval/unknown dates, controls/navigation, edits/deletion, unchanged v1/v2 exports and no network mutations. No unrelated exhaustive suite claim.

Release: continuing user authorization permits clean nonforce HEAD:refs/heads/main on origin and gitlab and prebuilt Pages researchtoolspy main deployment after independent source/runtime/artifact/visual acceptance. No production migration or research publication in this stage. Fresh read-only production schema/prefix15/catalog410rows/15tables, secret names and deployments before/after; no credential/config widening. Reuse trusted wrapper/staging/smokes; new helper adaptations independently reviewed. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/, dependencies. Never source scripts/cop-api.sh. Controller pilot publication remains plan-only; owner release separately authorized. Stop on divergence/schema drift/unsafe execution or semantic uncertainty; preserve failures and exact evidence. Record partial TL06, priorruntime rollback source240252f08/deployment355ab8b0, separate runtime/docs/main/deployment state.
