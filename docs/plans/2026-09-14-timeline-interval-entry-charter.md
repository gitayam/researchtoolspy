# Recorded interval authoring — local workspace v2

Baseline 5a3a629e1f416be8198262e785b0445c5a0c618c freshly fetched equal clean integration HEAD/origin/main/gitlab/main. Shared worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911. User-visible TL-06 slice: Analyst Add/Edit event can record an inclusive end date/time alongside its existing start, preserve precision, show a range, and round-trip local drafts/JSON. No invented dates or durations. Primary checkout untouched.

Contract: TimelineWorkspaceEvent adds recordedEnd?:CalendarLabel (date required, optional matching precision and time). Existing eventDate/eventTime/datePrecision remain the start; extraction/original stays v1. A new exported workspaceVersionForEvents(events) from timeline-workspace-codec chooses timeline-workspace.v2 iff any recordedEnd exists, otherwise v1. TimelineWorkspaceExport schemaVersion union v1|v2. Decoder accepts explicit v2 with same strict schema plus recordedEnd; v1 rejects recordedEnd. Use parseCalendarTemporalClaim for endpoints and ordering, plus existing authoring Gregorian minimum 1000 for end. Start required when end present; no partial-date clock endpoints. Inclusive unit extent, reversed ranges reject; overlapping endpoint precision allowed with clear uncertainty copy. No endpoint coercion. Export selects workspace version from events. Browser draft envelopes use timeline-browser-draft.v2 for interval workspaces and v1 otherwise; restore both explicitly with matching workspace version and reject intervals in v1 envelopes, preserving unreadable draft recovery. Existing interval-free exports remain v1; no automatic extraction v2 or server capability advertisement.

UI: optional checkbox Record an end date in Add/Edit; end date text accepts YYYY/YYYY-MM/YYYY-MM-DD, plus optional End time using native time field. End date precision shown and compact native calendar/month/year choice encouraged by reusing existing recorded-date component only if IDs/labels unique. Existing start selectors and labels unchanged. No default today; toggling end off explicitly removes interval only on Save; Cancel preserves all. Validate before any change; blank/malformed/reversed range leaves dialog open. Show Recorded interval in event card via existing temporal label; source original untouched. Changing end participates in contentChanged and evidence-review invalidation/confirmation exactly like start. Explain local draft and JSON backup support; private workspace saving and TimelineJS interval rendering are not yet supported. No silent flattening: adapter omits intervals with an explicit reason even with a presentation schedule. Other selected events still export, complete backup keeps intervals.

Owner seams: timeline-durable prepare save rejects v2 before any request, server validArtifactPayload requires payload.schemaVersion===kind to prevent v2 under v1. Existing history v1 remains readable; readBrowserSnapshot also rejects mismatched kind/schema. Durable panel disables new saves/copy for interval v2 and states Export JSON to keep intervals; previously prepared retry semantics remain unchanged and clearly about previous save. Evidence basis includes recordedEnd only when present, preserving old basis bytes. Timing review uses core interval extent for intervals and existing bounds for singles. Text labels show complete start/end; sorting remains recorded start and existing sequence. No schema migration or production DB mutations.

Ownership: tl00_recon owns src/types/timeline-workspace.ts, src/lib/timeline-workspace-codec.ts, src/components/timeline/TimelineResults.tsx, src/components/timeline/TimelineRecordedDate.tsx, src/pages/tools/TimelineAnalysisPage.tsx only. validator_review owns tests/e2e/smoke/timeline-interval-contract.spec.ts and timeline-interval-browser.spec.ts only. Owner owns src/lib/timeline-workspace.ts, timeline-timing.ts, timeline-evidence.ts, timeline-durable.ts, timeline-timelinejs.ts, functions/api/_shared/timeline-artifact-contract.ts, src/components/timeline/TimelineDurablePanel.tsx, docs/private release helpers. dependency_review independent read-only review. Native cooperative editors no commits/candidate execution. Owner commits exact paths after quiescence. Read CLAUDE.md (do not source auth helper), .team/project.json, foundation charter and this charter. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/. External dispatch blocked.

Verification: existing pinned credential-free network-none Docker snapshot/supervisor only, cwd /workspace. node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts --project=chromium for contracts; browser both Chromium/mobile-safari. New tests prove v1 strict rejection/v2 full round trip/end precision/reversal/unknown/legacy preservation, evidence invalidation, durable/server rejection and TimelineJS omission with backup retained. Browser actual import/edit/save/cancel/invalid/native fields/export/reimport/reload and downloaded values; representative mobile/desktop range UI captures. Preserve prior 77 cases plus focused existing durable/evidence/local-draft regressions as appropriate. Five typechecks/build/compiled actual production schema gate with fresh read-only inventory. Independent exact-source/runtime/artifact/visual acceptance; unchanged public tree retains edge replay exemption with live byte checks.

Authority: user authorized clean non-force origin/main and gitlab/main pushes and prebuilt Pages production deploy after stage acceptance; separate from pilot plan-only publisher. No data/migration/config changes. Stop on divergent refs, unsafe validator, uncertain contract, scope expansion. Both URLs read-only postflight; docs-only final sync. TL-06 stays in progress. Next stage: versioned durable interval history and TimelineJS endpoint support, then circa/clocks/extraction v2. This bounded local release must never claim those pending capabilities.
