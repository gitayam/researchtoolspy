# Timeline workflow consolidation

[Timeline](https://researchtools.net/dashboard/tools/timeline) now puts analyst content before setup and import. The compact private workspace/save bar keeps save state visible; **Saved versions and links** holds saved previews, revision history, reopening and limits, with raw IDs under **Revision details**. These disclosures preserve retry and history state. Guests see a concise local-draft status.

**Present** directly opens an eligible frozen selection of 1–100 dated events, labeled **Current edits**, with omissions visible. Empty or over-limit selections open the existing explanation instead. Saved and historical presentations stay separate. Setup, guidance and the event finder remain available through explicit disclosures; keyboard and DOM order follow the visible workflow. Existing save/auth/history and export identity are preserved. No new API, schema, permission, persistence model or public sharing is introduced. **TL-17 remains partial.**

Accepted source `1fccfb3051a6b0144c7ed24ae1327bb385a21352` landed on both main refs and deployed as `ab6d7fc7-aaa8-4e7c-9b87-28533dccf7a7` at [the accepted build](https://ab6d7fc7.researchtoolspy.pages.dev). This is a documentation-only follow-up to that runtime.

Planned isolated regression partitions passed 114 UI-suite checks (including six pure codec/identity checks) and 4 workspace-ordering contract cases across Chromium and mobile Safari, plus 19 Chromium contract checks (eight renderer, six adapter and five history) and five type checks. Frontend/worker builds and compiled actual-production-schema rehearsal passed. The full unrelated API suite, extraction quality and user testing are not claimed. Exact per-file/project counts, deduplicated completed cases and preserved failures appear in the [JSON receipt](./2026-09-12-timeline-workflow-release-receipt.json).

Initial focused checks passed before Present received stronger visual emphasis. Source 27880f561 passed all 137 planned checks, but independent visual review found mobile slide text clipped by the added omission notice. Source cd1e8d3db restored the headline; a stronger full-text bounds assertion still caught a clipped paragraph. Source 2eb209c72 then restored the complete fixture headline and paragraphs through compact spacing and provenance, confirmed by a focused mobile check. Two full renderer runs at that then-current source each hit a loopback route.fetch socket reset on a different locale module before a Chromium application assertion; the other 11 cases passed in each run. Final source 1fccfb305 adds one documented ECONNRESET retry for non-API GETs in the test harness; HTTP errors and exhausted retries still fail, and all application assertions remain intact. The exact server-side reset cause remains unproven. The focused transport check and complete planned regression partitions passed on this final source. Earlier successful and failed runs are preserved separately and excluded from final counts. Immediately after deployment, one deployment asset returned 404 and the custom-domain timeline page still referenced the prior asset set. Those initial responses were preserved. The asset became available, and repeating the unchanged 30 live checks on both URLs passed without another deployment.

Preserved successful superseded runs: workflow-ui at 27880f5615641483bfeebd7fb5c7a4cb5efbe498, workflow-durable at 27880f5615641483bfeebd7fb5c7a4cb5efbe498, workflow-renderer at 27880f5615641483bfeebd7fb5c7a4cb5efbe498, workflow-contracts at 27880f5615641483bfeebd7fb5c7a4cb5efbe498, workflow-mobile-fit at 2eb209c72bd9df55f4fd849e539f2776f51eaaa2, release-workflow-build at 2eb209c72bd9df55f4fd849e539f2776f51eaaa2. Their receipt and log fingerprints are retained in the JSON record; none contributes to accepted-source validation counts. Passing test results alone did not establish release acceptance.

All 155 staged files were independently compared; inventory SHA256 `745a7e76a66805c1c8dbb653660bd9ef9b82bbe7cd4a7567d9aa86b041cc8f33`. Prebuilt deployment used `--no-bundle`. Thirty read-only checks passed at each deployment/production URL, including exact owned renderer asset bytes. Renderer assets and headers are unchanged, so no new edge-browser replay was performed. All 13 migrations and 386 catalog entries across 14 tables remain unchanged; existing secret names are preserved.

Previous deployment `bf86e234-77eb-46a9-8dfe-a382e3c639bf` reads identical data and retains history and preview/export capabilities, but lacks the consolidated layout and direct current-edit Present action. Preserve snapshots and history. Primary checkout untouched; visual coverage is representative, not an exhaustive accessibility audit.
