# Durable recorded intervals

Baseline: 212bd5514fb0349ac0e07d00a167fadbbcd284d5, freshly fetched and identical on origin/main and gitlab/main. Clean integration worktree /Users/sac/AgentWorkspaces/researchtoolspy-timeline-foundation-20260911. Dirty primary checkout remains untouched.

Outcome: signed-in users at /dashboard/tools/timeline can save recorded intervals to a private workspace, reopen them and inspect both older single-date revisions and newer interval revisions. Removing an interval can save a subsequent v1 snapshot without losing earlier interval history. Delivery is user-visible TL-06, not new temporal extraction or collaboration scope.

Contracts: extend artifact payload and version support to timeline-workspace.v2 using the existing strict workspace codec, same 60 KiB canonical snapshot and 64 KiB request limits. The submitted kind must exactly equal payload.schemaVersion. Retain stable browser-workspace object ID, artifact ID, revision sequencing, canonical hashes, idempotency and current-identity checks. A put may transition only within the workspace family {timeline-workspace.v1,timeline-workspace.v2}; candidate/workspace transitions remain409. timeline_objects.kind records immutable creation kind. Version schema_version and manifest kind record each version's actual format. SQL version-kind trigger accepts equality or both kinds in that explicit workspace family. Both directions v1↔v2 are supported. Old object/version/payload/hash/revision/replay bytes are not rewritten. Tombstone, no-replace, no-resurrection and immutable-history constraints remain enforced. Unknown future versions and mismatched kinds fail closed. Existing human/service scopes, grants and privacy remain unchanged.

Storage: new forward migration0015 only; never edit0001–0014. Reuse the established atomic deferred-FK table reconstruction pattern from0012 to extend the two kind constraints and the version-kind trigger, preserving every existing row and dependent catalog. SQL trigger CASE expressions must be parenthesized for D1 remote parser compatibility. No new user data, credentials, public links or research publications. Full-chain and actual production0014-prefix rehearsal must prove rollback of failed DDL plus tracker, retry, foreign keys, immutable rows/hashes and unrelated catalogs. Owner applies migration only after exact-source acceptance and fresh matching production inventory/catalog. Publication remains separate.

UI: remove interval-only save/copy blockers and outdated unsupported copy. Preserve disabled states for authorization, busy, conflict, stale workspace and exact pending retry. A retry sends its original frozen body/key even if open edits change v1/v2; saved state must not falsely mark newer edits saved. New saves use decoded schemaVersion. Saved and historical readers validate both exact manifest/payload kinds, payload and revision hashes, complete one-object snapshot and bounds. Preview/export of historical v1/v2 must not change current unsaved work. Private content must not enter automatic local drafts.

Ownership: tl00_recon provider owns functions/api/_shared/timeline-artifact-contract.ts, timeline-artifact-store.ts, schema/managed-migrations/0015_timeline_workspace_intervals.sql and tests/e2e/smoke/timeline-durable-interval-d1.spec.ts. validator_review provider owns src/lib/timeline-durable.ts, src/components/timeline/TimelineDurablePanel.tsx and tests/e2e/smoke/timeline-durable-interval-browser.spec.ts. dependency_review provider owns scripts/verify-timeline-release.mjs after owner communicates current production input contract. Owner owns remaining seams, outdated interval-contract assertions, migration-chain tests, tests/e2e/smoke/timeline-durable-interval-contract.spec.ts, TimelineResults interval help text, private orchestration and docs. Cross-review after providers are quiescent; reviewers never accept their own authored changes. Native agents are cooperative editors sharing this worktree, not isolated branches. Owner alone commits. No candidate execution, tests, installs, pushes or production actions by providers.

Execution: .team/project.json guest-only policy applies. All candidate tests/builds/scripts run only in established pinned credential-free network-none Docker validator; source/dependencies read-only, no auth/socket, cap-drop ALL, no-new-privileges and stopped-container inspection. Existing trusted supervisor and dependency volume; no external model lane qualified. Serial browser partitions with trace=off, retries0 and unchanged assertions/timeouts; retain screenshots/errors and re-enable trace only to diagnose concrete failure. Test commands use node node_modules/playwright/cli.js test --config=playwright.timeline.config.ts with explicit Chromium/mobile-Safari projects and file paths. Include durable/history/snapshot/artifact/service/migration/interval contracts and real D1, browser save/reopen/history/retry/conflict, five TypeScript checks, Vite/Pages worker and actual-schema compiled gates. Exact inventory and executable commands freeze at committed candidate; no unrelated exhaustive-suite claim.

Authority: earlier explicit user instruction authorizes owner clean nonforce HEAD:refs/heads/main to origin and gitlab plus prebuilt researchtoolspy Pages main deployment after acceptance. Necessary0015 application requires concrete independently reviewed SQL, production schema-only preflight and isolated full/upgrade rehearsal. No widening credentials/config, no user research publication, no unrelated writes. Prohibited .team/, .env*, deploy.sh, wrangler.toml, workers/, containers/, dependencies. Never source scripts/cop-api.sh. Controller pilot publication remains plan-only; owner publication is separately authorized. Preserve failures and record source/ref/migration/deployment states, exact artifacts and rollback limits. Stop for divergence, unsafe execution, schema drift or broken compatibility; resolve findings before release.
